# S1.2-mutation — test report

**Sprint** S1.2 (patchScopeProfileEmergency mutation)
**MCP** VP MCP (vantage-memory, Convex `compassionate-goldfinch-737`)
**Date** 2026-06-03
**Mission** `k57c7s478gw1a3e5gmhdeptg5n87z78n` — cloud-mcp-fix-sync-day-90
**Task** `k178bdsbkjazrap1eqw1pn90b987z2yj`
**Doctrine** `j579y6f31g7xzgtgdnpgetdmjx87ztyj` base + `j57bvz4c62mrfs024fay5vhqqs87zxph` extension D9-D14
**Branch** `fix/s1-2-mutation-scope-profile-emergency` from `origin/main` HEAD `7eebf3b`
**RED commit** `e0c89d7`
**GREEN commit** `c0db160`

---

## Scope tested

Convex backend in `convex/oauth.ts`:
- `patchScopeProfileEmergency` mutation — new function (S1.2 deliverable)
  - Master token guard (constant-time `timingSafeEqual`)
  - Reason length guard (≥ 40 chars)
  - D4 enforcement: `global` + `*` forbidden in prefix lists for non-master profiles
  - Selective field patch (only provided args applied, others preserved)
  - Optional rename (profileId field update)
  - Cascade revoke: delete all `oauth_access_tokens` + `oauth_refresh_tokens` where `scopeProfile` = oldName OR newName
  - Append-only `oauth_audit_log` row with `previousState` + `newState` + `actorTokenHash` (sha256) + `reason`

Schema additive change tested: `convex/schema.ts` `oauth_audit_log` table (new, additive only).

Migration rewritten: `convex/migrations/patch_marie_iris_rh_scope.ts`
- Constants: `marie-<client-org>` → `<client-org>` (D9), `global` dropped from all prefix lists (D4)
- D4 static assertion at module load

## TDD discipline — RED → GREEN

Per CLAUDE.md ABSOLUTE RULE + Day 90 Laurent TDD-strict doctrine, test file `convex/oauth-s1-2-mutation.test.ts` was written **before** the implementation. RED phase committed first with skeleton throwing `"not implemented"`. GREEN phase committed second with full implementation.

### RED phase — commit `e0c89d7`

Skeleton mutation body: `throw new Error("not implemented")`.
Command: `npx vitest run convex/oauth-s1-2-mutation.test.ts`
Result: **18/18 FAIL** — all with "not implemented" (T11 additionally failed with wrong error message shape).

Failing tests in RED phase:
- T1 master token guard
- T2 reason length guard
- T3 D4 global in readPrefixes
- T4 D4 wildcard * in writePrefixes
- T5 master profile CAN include global
- T6 happy path return shape
- T7 rename persisted
- T8 cascade revoke access_tokens
- T9 audit log fields
- T10 refresh tokens cascade revoke
- T11 missing profile (wrong error: "not implemented" instead of "profile not found")
- T12 partial patch preserves fields
- MT1 other profiles unaffected
- MT2 audit log by_targetProfileId index
- MT3 three profiles separate audit rows
- SL1 D4 post-condition no global
- SL2 master retains wildcards
- SL3 forensic previousState captures leaked global

### GREEN phase — commit `c0db160`

Full implementation applied. Command: `npx vitest run convex/oauth-s1-2-mutation.test.ts`
Result: **18/18 PASS** (999ms).

### Evidence committed

- RED commit `e0c89d7` — schema + test file + skeleton. Test ratio: 0/18.
- GREEN commit `c0db160` — implementation + migration rewrite + biome format. Test ratio: 18/18.

## Test matrix — T1–T12 × MT1–MT3 × SL1–SL3

| ID | Scenario | Assertion | RED | GREEN |
|---|---|---|---|---|
| T1 | caller without master token | throws /Unauthorized/i | FAIL | PASS |
| T2 | reason < 40 chars | throws /reason/i | FAIL | PASS |
| T3 | global in readPrefixes, non-master | throws /D4 violation/ | FAIL | PASS |
| T4 | * in writePrefixes, non-master | throws /D4 violation/ | FAIL | PASS |
| T5 | master profile + global in prefixes | no throw, resolves | FAIL | PASS |
| T6 | happy path | `{patchedProfileId, cascadeRevokedCount, auditLogId}` shape correct | FAIL | PASS |
| T7 | rename marie-<client-org> → <client-org> | query by new name = found; old name = null | FAIL | PASS |
| T8 | cascade revoke 2 access_tokens | `cascadeRevokedCount = 2` | FAIL | PASS |
| T9 | audit log row | `eventType`, `targetProfileId`, `actorTokenHash` (sha256), `reason`, `previousState`, `newState` | FAIL | PASS |
| T10 | cascade revoke 1 refresh_token | `cascadeRevokedCount = 1` | FAIL | PASS |
| T11 | profile does not exist | throws /profile not found/i | FAIL | PASS |
| T12 | only fromAllowList provided | read/write prefixes unchanged | FAIL | PASS |
| MT1 | tokens for other profiles | unaffected by cascade (still exist) | FAIL | PASS |
| MT2 | two profiles patched | audit rows filterable via `by_targetProfileId` index | FAIL | PASS |
| MT3 | three profiles patched | 3 separate audit rows, one per profile | FAIL | PASS |
| SL1 | D4 post-condition | `global` absent from read + write after patch | FAIL | PASS |
| SL2 | master unaffected by non-master patch | master retains `*` in both prefix lists | FAIL | PASS |
| SL3 | forensic previousState | `previousState.namespaceReadPrefixes` contains `global` | FAIL | PASS |

**Ratio: 18/18 PASS in GREEN. 18/18 FAIL in RED (expected).**

## Multi-tenant — 3 fictive profiles (D12-D14 anticipation S4.1)

MT1–MT3 cover multi-profile isolation at the Convex layer:

| Profile | Tokens seeded | Cascade scope | Audit rows |
|---|---|---|---|
| `marie-<client-org>` (leaked) | 2 access + 1 refresh (across tests) | scoped to own profile only | 1 per patch |
| `another-profile` | none | unaffected by marie cascade | 1 (MT2) |
| `profile-a/b/c` | none | separate audit rows | 1 each (MT3) |

**Stub note** (per Sigma orchestration decision): MT1–MT3 operate at Convex layer directly. Full HTTP server integration (multi-tenant workspaceId cross-check) deferred to S4.1.

**Ratio: 3/3 PASS** (MT1 isolation, MT2 index filter, MT3 three separate rows).

## Coverage per file

Command: `npx vitest run --coverage convex/oauth-s1-2-mutation.test.ts` (vitest + v8 provider).

| File | Lines (new mutation) | Note |
|---|---|---|
| `convex/oauth.ts` (`patchScopeProfileEmergency` only) | ~100% | All branches covered: auth guard, reason guard, D4 (global + *), non-master, master bypass, selective patch, rename, cascade true/false, audit insert |
| `convex/schema.ts` | n/a | Declaration-only; correctness verified via `tsc --noEmit` clean (0 errors project-wide) |
| `convex/migrations/patch_marie_iris_rh_scope.ts` | n/a | Internal mutation — not exercised by unit test harness; correctness verified via static D4 assertion + tsc |

## D4 enforcement compliance ratio

| Check | Coverage | Result |
|---|---|---|
| `global` in readPrefixes for non-master → throw | T3 | PASS |
| `*` in writePrefixes for non-master → throw | T4 | PASS |
| master profile CAN include `global` → no throw | T5 | PASS |
| D4 post-condition: no global after patch | SL1 | PASS |
| Migration constants: static D4 assertion at module load | code-level | PASS |

**D4 enforcement ratio: 5/5 test assertions PASS.**

## QA gates

- `npx vitest run convex/oauth-s1-2-mutation.test.ts` — **18/18 PASS** (999ms)
- `npx tsc --noEmit` — **0 errors** (project-wide)
- `npx @biomejs/biome check` (4 touched files) — **0 errors, 0 warnings**

## Schema migration safety

- `oauth_audit_log` is **purely additive**: new table, no existing table modified, no field renamed or removed.
- `oauth_scope_profiles` fields untouched in schema (runtime patch applied by mutation only).
- No breaking change: existing rows unaffected, all existing queries/mutations continue to work.
- Backward compatible: pre-S1.2 deployments see no schema difference on `oauth_scope_profiles`.

## Friction observed

1. `ctx.db.get()` in test assertions returns a union of all table types — TypeScript cannot narrow to `oauth_audit_log` shape directly. Fixed by using `ctx.db.query("oauth_audit_log").withIndex(...)` instead, which gives the correct typed result without casting.
2. Biome formatter flagged mixed indentation in `schema.ts` comment block (pre-existing tabs/spaces mixture adjacent to new table definition). Auto-fixed via `biome format --write`.
3. `internalMutation` in `convex/migrations/` cannot call a `mutation` (public function) — migration direct-patches with inline D4 enforcement rather than delegating to `patchScopeProfileEmergency`. Documented in migration file header.

## Out of scope (folded into later sprints)

- **S2** admin endpoint generalization: `PATCH /admin/scope-profiles/:id` HTTP endpoint wrapping this mutation (D5)
- **S2.3** `patchScopeProfile` non-emergency variant with audit + softer constraints
- **S3** `@vantageos/cloud-identity@0.1.0` brick — folds scope-profile lookup + resolveTenant cross-MCP (D8)
- **S4.1** full Convex tool-handler integration multi-tenant test suite
- **VCRM mirror** — Theta S1.2 sister task (mirrors this mutation in VCRM after Sigma PR opened)

---

Orchestrator: Sigma — VantageOS Team | 2026-06-03
