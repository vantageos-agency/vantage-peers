## What

Ships the npm surface for R-3 slice 2. #322 threaded the cursor through four MCP list tools (`list_plugins`, `list_rules`, `list_components`, `search_components`) at the boundary but did **not** bump the package version, so `bunx` clients still serve **1.17.1** — the pre-cursor wrapper. This bumps `@vantageos/vantage-registry-mcp` to **1.18.0** and folds Eta's oversized-error correction from the #322 verdict, so the release commit carries both as Pi and Eta directed.

The prod-skew that would have broken the four tools at publish is already gone: the vantage-registry **production** Convex (`vibrant-ibex-858`) now carries #319/#320 (deployed + proved this session under task k17dyzwa), so a 1.18.0 client sending `paginationOpts` is accepted, not refused.

## The two changes

- **`mcp-server/package.json`** — 1.17.1 → 1.18.0 (minor: cursor pagination added to four list tools).
- **`mcp-server/lib/paging.ts`** — Eta's oversized-error correction, "no cycle", riding this commit: `buildListEnvelope`'s three throw messages serialised the **entire** paginate result with `JSON.stringify(result)` (≈ 414,852 bytes on a full 200-row page — potentially the largest thing the tool emits, in a mission about envelope safety). A new `describeResultShape(result)` prints `keys=[…]` + `page=array(N)`/typeof + `isDone` typeof + `continueCursor` present/null/absent — the **shape, not the payload** — matching what the bare-array branch already did with `length ${result.length}`. Behaviour is unchanged; only the message text shrinks.
- **`mcp-server/server.js`** — the committed bundle, regenerated by `npm run build` (zero-drift).

## SELF-GATE:

- refs: head `git ls-remote origin refs/heads/release/mcp-1.18.0-cursor-boundary` → `5296fcd75c3b025fc09acafe558a378b8a9ead84`; base `origin/main` (`git merge-base --is-ancestor origin/main HEAD` → confirmed). Eta release-gate `k178scfh2kdj9eb9vc1zjm3hz58csxjr` — APPROVED @ e00303f, "PUBLISH HOLD LIFTS ON MERGE", oversized-error correction named as riding the next paging.ts commit. Prod deploy proof `k17dyzwa2x6xmn8fw23zpxw4z58csrgp` (done, vibrant-ibex-858 carries #319/#320).
- counts: `git diff --stat origin/main...5296fcd` → 4 files, +69/-10 (paging.ts, package.json, server.js, CHANGELOG.md), mcp-server/ only. `grep -c "JSON.stringify(result)" mcp-server/lib/paging.ts` → **0** (was 3). `node -p "require('./mcp-server/package.json').version"` → **1.18.0**; `npm view @vantageos/vantage-registry-mcp version` → **1.17.1** (pre-publish). `npx vitest run --project mcp` → **359 passed** (21 files) — re-run firsthand on the pushed head. `npm run build` → `git status --porcelain mcp-server/server.js` empty after rebuild (committed bundle == build(server.ts), zero drift).
- standard: backend-standard **R-3/R-30** pinned at elpiarthera/ElPi-Corp origin/main `f62c4c04`: the cursor already reaches the boundary (#322); this ships it. The oversized-error correction is Eta's, from the #322 verdict — the throw path now reports the shape it received, recoverable in seconds, never the whole payload. The 32 envelope cases (`mcp-server/tests/cursor-slice2-envelope.test.ts`) — three throws + two legitimate-empty positive controls + malformed-shape guards — all remain green, proving the matched substrings (`bare array`, `'isDone'`, `contradictory`, `length 200`) were preserved.
- scope:

Orchestrator: Omega — VantageOS Team | 2026-08-19

