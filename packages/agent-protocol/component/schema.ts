import { defineSchema } from "convex/server";

// Phase A scaffold — empty schema. Phase B will move the 11 agent-protocol
// tables from vantage-memory's convex/schema.ts into here:
//   tasks, missions, messages, messageReceipts, briefingNotes, diary,
//   profiles, peers, recurringTasks, taskDependencies, missionTemplates.
export default defineSchema({});
