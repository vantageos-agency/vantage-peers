import { defineSchema } from "convex/server";

// Phase A scaffold — empty schema. Phase B will move memories + episodes
// from vantage-memory's convex/schema.ts into here.
export default defineSchema({});
