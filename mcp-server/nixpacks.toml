[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["bun install"]

# Build phase kept as type-check sanity gate.
[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run server-http.ts"
