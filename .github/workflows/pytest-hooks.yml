name: Pytest hooks

# A workflow that does not run and a workflow that passes look identical on the
# checks page: neither shows a red cross. Day 131 measured that: `push` was pinned
# to `main`, so on a branch with no open PR only the grep gate ever fired, and an
# ABSENCE of verdict was read as a verdict. Triggering on every branch is half the
# cure; the other half is branch protection (task k17cnkxkm8xaz6d5tq1nxtxjtx8agq84),
# which makes the absence BLOCK instead of merely being invisible.
#
# `pull_request` (target `main`) is present but FORK-ONLY BY CONSTRUCTION. Every
# required-context job carries the guard `if: github.event_name != 'pull_request' ||
# github.event.pull_request.head.repo.fork == true`. Consequence table:
#   - same-repo push  -> push run only (pull_request job skipped)
#   - same-repo PR     -> already covered by its push; pull_request job skipped
#   - FORK PR          -> no push exists, so the pull_request job runs and reports
# So fork PRs ARE checked again (a blocked fork PR sits in the merge queue — they are
# part of the fleet flow, still Pi-gated at merge) with ZERO duplicate runs.
on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

concurrency:
  group: pytest-hooks-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest-hooks:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == true
    name: pytest tests/hooks/
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pytest
        run: pip install pytest

      - name: Run hook tests
        run: pytest tests/hooks/ -v
