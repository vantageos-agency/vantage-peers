# Changelog

## [Unreleased]

### Fixed
- **The participants backfill migration is batched + self-scheduling so it cannot throw the Convex read-byte limit (P0, task `k17byc17kzyyra886h6v08ky3n8cqfqm`).** The one-shot form merged this morning (#1201) did `ctx.db.query("briefingNotes").collect()` and **threw on production**: `Too many bytes read in a single function execution (limit: 16777216 bytes)`. Convex reads cannot project fields, so `collect()` loads every note's kilobyte-scale `content` — the field the loop never uses — and the whole-table read exceeds 16 MiB; the table stayed EMPTY and the participants-read fix stayed inert. Fix: each transaction processes at most `BACKFILL_BATCH_SIZE=16` rows via `.paginate()`, then reschedules itself with the continuation cursor via `ctx.scheduler.runAfter` until `isDone` — no single execution's read footprint depends on corpus size, only on BATCH (16 × the ~1 MiB Convex document cap = the whole 16 MiB budget as the absolute worst case; real content is kilobytes). `syncParticipantIndex` stays the single writer, idempotent per-page and across a resume (delete-then-reinsert by `by_note`). TDD closes the harness gap the class `runtime-limit-proof-before-merge` names (convex-test does not enforce the runtime byte limit): a new threshold-reaching fixture seeds 300 notes × 64 KB = 19,660,800 bytes (asserted `> 16,777,216`) and drains it fully via bounded 16-row pages, plus an idempotence-across-resume pole (full scheduled drain twice + a mid-corpus cursor resume, junction count unchanged). Verified firsthand: vitest 4/4 on the migration suite, full suite 2904 passed / 42 skipped, tsc 0. Real dev deployment (`efficient-guineapig-356`): a 68-note corpus drains to 84 junction rows via the real scheduler across pages (page 1 = 16, `isDone=false`, then continuations), stable; a second run leaves it unchanged (84 → 84). The prod run remains a separate Pi authorization.

### Added
- **`scripts/reap.py` — one shared reaper, replacing two copies of half a tool, with a probe that can go red (task `k17aekks0by4f9j885485h2gms8cjsfm`).** `scripts/reap_worktrees.py` existed TWICE, byte-identical (`sha256 fc65efc4…`), here and in `vantage-memory`: two copies of one thing means one silently wins and no reader knows which. The deletion primitive it needed lived in one station's tree under that station's name. Both become one part. **Two coverage holes closed, both measured on the shared host at 90% disk, not argued.** (1) `git log @{u}..HEAD` — the command `close-day` Step 4 itself prescribes — exits non-zero and prints NOTHING on a branch with no upstream: 30 of 71 repositories were in exactly that state, so the check meant to answer *"does this hold work that exists nowhere else"* answered *no* for every one of them, and eleven genuinely held such work. An absence of output was read as an absence of risk. Replaced by `git remote` (no remote at all → the entire history is here) plus `git log HEAD --not --remotes` (commits reaching no remote-tracking ref, upstream configured or not). (2) `find -name .git -type d` cannot see a registered worktree, whose `.git` is a **file** — four missed, in silence; `enumerate_repositories` walks both forms. **Archives are proven by RESTORATION, never by `git bundle verify`**: a bundle written `--not --remotes` needs prerequisites that live in the directory about to be deleted, so verify reports failure exactly when it matters — after the deletion it was supposed to authorise. The proof is `git clone --filter=blob:none <origin>` → `git bundle unbundle` → `git cat-file -e <head>`. Scope grows from worktrees to worktrees **and session scratchpads**, whose root is derived from the working directory, never typed. Three states throughout: `holds_unreachable_commits` returns True / False / **None**, and every `None` keeps — *"I could not tell whether this landed"* and *"this is safe to delete"* never produce the same action. Every run emits a reconciled `examined = kept + removed`, and a run whose removals do not equal its reapable count refuses to report success. `scripts/sandbox-rm.sh` (the de-named primitive, roots from `SANDBOX_RM_ROOTS`) + its 4/4 probe; `tests/plugin/test_reap.py` 5/5 on REAL repositories the fixture builds, each mutation asserted landed, the old form asserted going quiet on the very case the new one flags. `scripts/reap_worktrees.py` and its test are deleted here; the `vantage-memory` copy goes in its own pull request, and `close-day` Step 4 calling this part instead of describing a method is task `k17evr8k57ekv8yqv1sb1r3t718cja9v`.
- **`block_task` now requires a block to name the task charged to unblock it — anonymous blocking refused server-side (task `k177s4k501682y61m4jvqe69f58cdw20`, points 1–3).** VantagePeers protocol defect (Cloud + Self-host): an orchestrator could declare itself blocked with no one charged to lift it — a block note is a journal, a task in the responder's queue is a commitment. Pi's audit measured three live cases (Themis, Eta, an Omega/Hephaistos client block open since 2026-07-24). Reuses the server-side `complete_task` closure-gate pattern (the refusal lives on the server, so it holds for every station without distribution). `convex/tasks.ts` new `blockTask` mutation validates at call time: a cited `blockedOnTaskId` must (a) exist, (b) be neither `done` nor `cancelled`, (c) be assigned to someone OTHER than the blocked task's own assignee — else the call is REFUSED with a message NAMING what is wrong and how to fix it (`BLOCKED_ON_TASK_NOT_FOUND` / `BLOCKED_ON_TASK_CLOSED` / `BLOCKED_ON_OWN_TASK`). A block WITHOUT a link stays possible only for an obstacle nobody owns, and REQUIRES an explicit `# blocked-on-nobody: <reason>` marker in `reason` (`BLOCKED_LINK_REQUIRED` otherwise) — forbidding *anonymous* blocking, never blocking itself. Reciprocal (closes the loop): `unblockWaitersOn`, called from BOTH `complete` and `update(status="done")`, sweeps every task that named the now-done task back to `todo` and notifies its assignee via the existing messages/messageReceipts path. `convex/schema.ts` adds `blockedOnTaskId`/`blockedOnNobodyReason` (both optional, so pre-existing `blocked` rows stay valid) + a `by_blockedOnTaskId` index. MCP (RULE #24): the `block_task` tool gains `blockedOnTaskId` and now calls `tasks:blockTask` (preserving `blockedBy`→`dependsOn`, `reason`, `callerOrchestrator`). Migration inventory: read-only `tasks:listUnlinkedBlocked` query lists pre-existing linkless `blocked` rows (the invisible-debt list) — run post-deploy via `npx convex run tasks:listUnlinkedBlocked`. TDD strict, both poles: `convex/__tests__/blockTaskReciprocal.test.ts` 8/8 (the six required refuse/accept cases + reciprocal-via-`update` + migration inventory). Full suite `npx vitest run` 2854 passed / 42 skipped / 0, `tsc --noEmit` 0 (root + mcp-server), `mcp-server` `npm run build` 0 incl. the `postbuild` boot-check (block_task still registers on the strict server). **BREAKING at deploy (not at merge): once deployed, every anonymous `block_task` (bare `reason`, no link, no marker) is refused — including `dispatch-task-start`'s Bucket-B stale-sweep; the deploy MUST be sequenced with a `dispatch-task-start` skill update or every orchestrator's blocking breaks.** Point 4 of the parent task (a "peers blocked on me" count in `check_messages`) is intentionally EXCLUDED — folding a task-derived field into `checkNewMessagesEnvelope` is the Day-156 reader-skew class (`railway-mcp-redeploy.md`); it ships separately as a dedicated reader-first tool.
- **`review-pr` skill — the reviewer's routine, with "update before verdict" mechanized (task `k17ed64y1g1zpm2bemhk6m862n8cbwsp`, Point 1).** Root cause Pi measured: `required_status_checks.strict = true` + squash merge makes a PR fall "behind base" the instant any *other* PR merges, so a verdict requested on a stale head is a wasted cycle — five review cycles were redone for zero content change in one day, plus the #1183-behind-#1182 incident. The flow rule "rebase before requesting a verdict, one PR in gate at a time" was a human instruction laid over a mechanism that contradicts it. This skill mechanizes it into the reviewer's routine: Step 1 runs `gh pr update-branch` (allowed: `allow_update_branch = true`) BEFORE any gating, Step 2 reads back the head OBTAINED, Steps 3–4 gate and pin the verdict to that obtained head — never the stale head the request cited. A late branch is never a send-back; only a real defect or an unresolvable merge conflict is `REVISE`. Reviewer-side companion to `request-gate` (out-of-git transport) and `open-pr` (requester flow); does not touch `request-gate` (its scope is artifact transport, not PR verdicts — Pi arbitrated). The structural closure is GitHub's merge queue (Point 2 of the same task); this routine is the immediate net until, and alongside, that queue. Published to VR canonical (`get_skill_content name=review-pr`) so ALL stations pull one artefact; the local `.claude/skills/review-pr/SKILL.md` is its byte-exact mirror. `.claude/skills/review-pr/SKILL.md` (91 lines).
- **`missionTemplates.listNames` query — the LIVE source of truth the mission-template guard was missing (task `k174hk6z94rmy91m6c600zbsgh8cbj2r`).** Root cause Pi lived: the `enforce-mission-template.py` guard refused `orchestrator-config-update-v1` — a template that genuinely EXISTS in the table (id `md7frv956ypvjakzyf4y0j0etx8cb8x3`) — because the only sources it could probe were a hardcoded whitelist or a cached/static manifest, both of which drift the instant a new template is upserted. The guard code (v2.0.0, dynamic) was already correct; the gap was purely backend — `convex/missionTemplates.ts` exposed only `getByName`, so the probe had no live table to interrogate. `listNames` (`args: {}`, `returns: v.array(v.string())`, `.take(1000)`) returns every non-deleted template name, mirroring `getByName`'s soft-delete filter (`deletedAt !== undefined` invisible). Canonical wiring documented in the hook docstring: `MISSION_TEMPLATE_PROBE_CMD='npx convex run missionTemplates:listNames --prod'` — the live table replaces the static manifest (named as the anti-pattern). TDD strict two-faced: `test_red3_real_template_refused_against_stale_cached_list` (RED — reproduces Pi's defect: real template refused against a stale 8-name set) → `test_green3_real_template_accepted_against_live_source` (GREEN — accepted when the live 9-name set is probed) + `test_two_faced_probe_invented_name_still_refused_with_live_source` (an invented name absent from the live set is still refused — a guard that accepts everything guards nothing). Hook suite 18/18 (9 pre-existing + 3 new + others), `tsc --noEmit` 0. No inline template-name whitelist remains in the guard code path (only the `GENERIC_TEMPLATE` policy constant). RULE #24: `listNames` serves the hook's shell probe (`npx convex run`), not a client-facing MCP surface — no MCP tool mirror needed (a thin `list_mission_templates` is a possible later fast-follow, not in scope). Per-station probe env wiring (e.g. Pi's chromebook) is the completing step outside this repo.

- **`missionTemplates` soft-delete — closes the permanent-residue verification-probe gap (task `k1731hck9j37fa5ba4k858qvwd8cb609`).** Mission templates had no delete of any kind, so every verification probe (e.g. `_probe-1180-brief`) became a permanent production row. Reuses `memories.softDeleteMemory`'s audit-preserving motif — PATCH a flag, never `ctx.db.delete` — instead of inventing a second delete mechanism. `convex/schema.ts` adds `missionTemplates.deletedAt: v.optional(v.number())` (a timestamp, not a bare boolean like memories' `isLatest`, so the row also records *when* it was retired without a second field). `convex/missionTemplates.ts`: new `softDelete` mutation (accepts `templateId` or `name`, throws `"... not found"` if neither resolves, mirroring `softDeleteMemory`'s not-found guard); `getByName` now filters out `deletedAt !== undefined` rows (invisible to reads, like a superseded memory); `instantiateTemplateIntoMission` treats a soft-deleted template as not found. MCP: new `soft_delete_mission_template` tool (`kind: "master"`, mirrors `soft_delete_memory`'s guard) — RULE #24 schema mirror. TDD strict RED→GREEN: `convex/__tests__/missionTemplates.softDelete.test.ts` (5 tests: soft-deleted invisible to getByName, non-deleted template stays visible after an unrelated delete, not-found throws, `templateId`-based delete, `instantiateTemplateIntoMission` refuses a soft-deleted template) — reverting the `getByName` filter reproduced RED (3/5 failing) before GREEN 5/5. Non-regression: `missionTemplates.test.ts` + `__tests__/missionTemplates*.test.ts` 14/14, `tsc --noEmit` 0, `mcp-server` `npm run build` 0.

- **`missionTemplates.brief` — a mission template now carries its own reusable "cadrage" (task `k17esb9hk6c5wam6grfe356ew18cbjs1`).** Root cause: `missions.brief` already existed, but `missionTemplates` had no `brief` field, so the same cadrage had to be retyped at every instantiation (Pi instantiated `daily-passation-v1` seven times in one day, seven copied briefs — two authorities for one thing, nothing flags the day they diverge). Chose form (a) — a `brief` field ON the template, reused at instantiation — over form (b) — a new combined create-mission-and-steps op — because `instantiate_template_into_mission` already exists and only needed a minimal extension, not a new operation. `convex/schema.ts` adds `missionTemplates.brief` (optional); `missionTemplates.upsert` accepts/stores it; `instantiateTemplateIntoMission` copies `template.brief` onto the target mission's `brief` ONLY when the mission doesn't already have one — instance-specific fields (pilot, project, name, dates) supplied at `missions.create` are never touched. MCP: `update_mission_template` and `instantiate_template_into_mission` tool descriptions/schemas mirror the new field (RULE #24 schema mirror). TDD strict RED→GREEN: `convex/__tests__/missionTemplates.brief.test.ts` (4 tests: inherits template brief when mission has none, instance-specific fields untouched, caller-supplied brief never overwritten, template without a brief leaves mission brief unset) — GREEN 4/4. Non-regression: `missionTemplates.test.ts` + `__tests__/missions*.test.ts` 23/23, full suite `npx vitest run` 2820 passed / 42 skipped, `tsc --noEmit` 0.

### Fixed
- **Backfill migration: the participants index was EMPTY in production, so the #1200 grant fix was inert for every pre-existing note (task `k178gg7wp3cre87mgw60trtpfh8cqfn3`, Day-166).** #1200's read consults the `briefingNoteParticipants` junction table, populated only by the create/update sync — nothing backfilled the rows that predated the deploy, so on a live system EVERY existing note was unreadable to its participants (a scoped participant got "not found" on both a note it participates in AND one it does not — identical, so the probe could not even discriminate). `convex/migrations.ts` gains `backfillBriefingNoteParticipants` (one-shot, idempotent): it loops every `briefingNotes` row and calls the SAME `syncParticipantIndex` (now exported from `convex/briefingNotes.ts:23`) that create/update use — no second writer. Idempotent because the sync deletes-then-reinserts per note (a re-run leaves the row count unchanged). **Two classes this closes, worth carrying (Pi task `k17ahxrzmce75cd7jnpmc9xmzh8cpsr8`): (1) a DERIVED structure — junction table, side index, denormalised field, counter, search index — shipped without its migration is filled only by the write path, so rows predating it are never visited (green, inert); (2) a fixture reachable only through the write path is structurally INCAPABLE of the pre-write RED — every test calls `create` which syncs, so the suite cannot construct the failing case.** TDD strict: `convex/__tests__/backfillBriefingNoteParticipants.test.ts` builds the state no create-based fixture can (direct `ctx.db.insert` bypassing the sync) — asserts the participant is DENIED pre-migration (and a non-participant identically, proving the pre-migration ambiguity), runs the migration, asserts the participant now READS while the non-participant still gets null; plus an idempotency test (migration run twice, junction count unchanged). tsc 0, full suite 2902 passed / 42 skipped, DEV-proven (69 notes -> 86 junction rows, re-run 86 -> 86, scoped read works). **Prod run is a separate pi-authorized step; #1200 stays verified-NOT-activated until the migration runs in prod and prometheus reads a note it participates in but did not create, under its own scoped identity.**
- **A briefing note's `participants` array now grants read access — resolved IN-QUERY, not a post-query handler filter (task `k175ga65p654z200ydj7s8qv5s8cnxfc`, Day-165).** A note shared via `participants` was unreadable by a SCOPED participant: `get`/`list`/`search` scoped visibility by `createdBy IN fromAllowList` (a post-query handler filter — the R-11 anti-pattern) and NEVER consulted `participants`; it worked only for `master` (which bypasses the filter), masking the bug until a scoped external seat (prometheus on Grok) hit it — sharing was inert for exactly the members it should serve. Fix moves visibility INTO the Convex query: new junction table `briefingNoteParticipants` + index `by_participant_note` (kept in sync by `create`/`update`/`deleteBriefingNote`); `convex/briefingNotes.ts` `get`/`list`/`searchBriefingNotesByKeyword` gain `callerCanRead()` = creator OR an index-range lookup on `by_participant_note` (never the `participants` array field, never a post-query scan — R-11-aligned), with `master` + legacy unscoped calls preserved; `mcp-server/src/tools.ts` threads `master` + `callerIdentities` into the query while preserving the pre-existing MCP-layer cross-tenant defense-in-depth (no leak). TDD strict under a SCOPED identity (master masks the bug — the measurement-integrity lesson): 8/8 new tests RED-first then GREEN, one feeding a deliberately STALE `participants` array to prove the INDEX (not the array) is the source of truth; full suite 2891 passed / 42 skipped, `tsc --noEmit` 0 (root + mcp-server), DEV-proven (scoped participant reads a non-owned note, unrelated identity gets null). Write-RBAC (createdBy-only for update/delete) unchanged. **BREAKING at deploy (not at merge): needs the Convex deploy (new `by_participant_note` index) + the Railway MCP reader redeploy before a scoped participant can read.** The standard-side capitalization (§2 `grant_paths`, task `k172vg05eg0zrny5cqev0q46698cn1c2`) rides separately.
- **`list_tasks` cursor pagination no longer returns an empty page before the true end, and `bulk_complete_tasks` has a route past its own cap (task `k171rbm2txe42jxzddyqakbg7n8ch7zr`).** Two shipped VP-tool defects Pi measured firsthand on live data (Cloud + Self-host). **Defect 2 (the serious one — fails silently in the reassuring direction):** `convex/tasks.ts` `runTasksList` (L441) and `listByMission` (L1519) computed `needsWideScan = createdBy || updatedSince` — OMITTING `createdBefore` — so a cursor-only call set `fetchCap = limit`, fetched only the `limit` newest rows, then dropped every row `>= before` in memory (the cursor filter ran AFTER `slice(0, limit)`): page 2 came back `[]` while the real page-2 rows were never fetched. An empty page reads as end-of-list, so a caller silently paginated a truncated queue (Pi published a wrong total twice before catching it). Fix: push the cursor anchor into the index RANGE (`.lt("_creationTime", before)`, the implicit last index field) on the index-backed single-status branches — correct AND unbounded, so "exhausted" is now distinct from "hit the scan cap"; for **multi-status and no-status** branches, which query a compound index (`by_assignee = ["assignedTo","status"]`) that returns rows ordered by `status` THEN `_creationTime`, a post-filter preserved that scrambled order and `slice(0, limit)` grabbed a status-skewed set that was never the true N-most-recent of the filtered union (Eta REVISE on PR #1194 — the first pass fixed only the single-status branch and wrongly claimed the whole tool terminated; a claim true for one path of three is exactly what let the original defect through) — fixed by always widening these branches and re-sorting the filtered superset by `_creationTime` desc BEFORE the slice, then applying the cursor; the loud `SCAN_CAP_EXCEEDED` throw kept. **Defect 1 (`bulkComplete` refused at its own purpose):** the mutation threw `BULK_HARD_CAP_EXCEEDED: ... Narrow your filter and retry` when matched > 500, but both filter fields (`autoGeneratedOnly`, `assignedTo`) were already at their narrowest — an un-followable instruction on the tool's own primary use case (cron-spam cleanup), and the scan-stop-at-cap+1 count was a sentinel indistinguishable from a true total. Fix: add a `status` filter dimension (a caller can narrow to the open pile), the live path now DRAINS in cap-sized batches and returns `remaining` instead of throwing (repeated calls terminate — NO external purge script, per Pi's explicit refusal), and the count is explicitly labelled truncated (`cappedAt`/`remaining`) vs exact. **Activation gap closed (RULE #24 mirror, caught by verifying past the Convex layer):** the MCP `bulk_complete_tasks` filter zod schema (`mcp-server/src/tools.ts`) was a plain nested `z.object` that SILENTLY STRIPS unknown keys — so `filter.status` worked in vitest (which calls Convex directly) but was dropped for real MCP clients; the field is now declared (`status: z.enum(["todo","in_progress","review","blocked"])`, matching the Convex contract), the stale "throws BULK_HARD_CAP_EXCEEDED, narrow and retry" description rewritten to the drain/`remaining`/`status` contract, and the `Returns {...}` line updated. **Class sweep (Pi's priority — an empty value meaning both "could not continue" and "nothing left" is not confined to these paths):** all `createdBefore` readers audited; `convex/missions.ts` (L248) and `convex/briefingNotes.ts` (L139) STILL carry the identical silent-empty defect (needsWideScan omits createdBefore + slice-before-filter), and ~9 siblings widen correctly but lack the loud cap-overflow throw — tracked as fast-follow `k17fyg55yee3mrxegwq13an69x8cg8em`, which also flags that the prior "pagination class closed — carriers = 0" claim (mission `k574p02mghppex86fpnk578n0x8cd0py`, lot 2, below) was over-stated: that sweep verified each reader HAS a scan cap, not that `needsWideScan` triggers for the cursor case. TDD strict, both poles: `convex/__tests__/listTasksCursorPagination.test.ts` + `convex/__tests__/bulkCompleteScale.test.ts` reproduced RED against the unfixed code (page-2 truncation `expected 50 to be 250`; `BULK_HARD_CAP_EXCEEDED`; `Unexpected field 'status'`) then GREEN. Eta's adversarial probe (adopted verbatim): a cursor walk over `status:["todo","in_progress"]` limit=5 across 12 alternating-status tasks returned 5/12 on `main` and 7/12 on the first-pass head (partial page read as exhausted), and now walks 12/12 with an explicit assertion that page 1 carries the N-most-recent of the union, not the newest of a single status. Verified firsthand (not on subagent word): the changed test files 10/10, full `bunx vitest run convex/` 1058 passed / 0 failed, `tsc --noEmit` 0 (root + mcp-server), `mcp-server` `npm run build` 0 incl. `postbuild` boot-check PASS. No `convex/schema.ts` change / no new index (reused existing indexes, range-pushed on the implicit `_creationTime`).
- **Restore #1189's unknown-param loud refusal, this time with a build-start-call boot check gating it — the P0 (task `k17at41v7e6re4ht9wbf3cvdah8cepjc` restore, revert #1191 reverted).** #1189 shipped `defineTool` wrapping every tool's zod shape in `z.object(shape).strict()` and handing it to `server.tool(name, description, strictSchema, ...)`. It PASSED source-level vitest (2847 tests) yet the DEPLOYED server 500'd on `initialize`/`tools/list`/every tool call — a fleet-wide outage, reverted via #1191. **Root cause, measured against the installed `@modelcontextprotocol/sdk@1.29.0`:** the deprecated `server.tool(...)` positional overload disambiguates "raw shape" vs "annotations" via `isZodRawShapeCompat(firstArg)`, which returns `false` for an already-built `.strict()` ZodObject (it IS a schema instance, not a raw-shape record) — the SDK then mis-parses it as `ToolAnnotations` and throws `Tool <name> expected a Zod schema or ToolAnnotations, but received an unrecognized object` at REGISTRATION time, before the server can boot. A server that cannot boot is indistinguishable from one that works when only source is tested — exactly where every #1189 proof stopped. **Fix:** `mcp-server/src/registerTool.ts` `defineTool` now registers via `server.registerTool(name, { description, inputSchema: strictSchema, annotations }, handler)` — the SDK's non-deprecated config-object entry point, which accepts an already-built Zod schema instance directly (`getZodSchemaObject` returns a schema instance as-is instead of routing it through the raw-shape/annotations disambiguation), so registration boots AND the SDK's own `validateToolInput` applies strict rejection before the handler runs. `mcp-server/src/tools.ts`'s CORE-tool-exposure masking wrapper (`registerTools`) now intercepts both `server.tool` and `server.registerTool`. **New gate (the actual fix for the incident, not just the registration bug):** `mcp-server/scripts/boot-check.mjs` BUILDS nothing itself but STARTS the real built `dist/server.js` over its real stdio JSON-RPC transport and asserts 4 calls in order — `initialize` succeeds, `tools/list` succeeds non-empty, a well-formed `whoami` call succeeds (MUST_PASS pole), an amputated `whoami` call carrying an undeclared key is refused NAMING the key via zod's `unrecognized_keys` issue (MUST_BLOCK pole). Wired as `package.json` `postbuild` (npm lifecycle auto-runs it after every `npm run build`, including transitively via `prepublishOnly`) — cannot be skipped by a human forgetting a manual step. TDD strict, bipolar: RED reproduced firsthand by temporarily restoring the unfixed `server.tool(strictSchema, ...)` call — built server crashed at boot with the exact incident stack trace (`Tool store_memory expected a Zod schema or ToolAnnotations...`); GREEN after the `registerTool` fix — all 4 calls pass. Restored the reverted `set-summary-unknown-param-loud-refusal.test.ts` unchanged (still valid against `buildStrictInputSchema`, unaffected by the registration-entry-point change). Updated 20+ test-file duck-typed `McpServer` stubs (`src/__tests__/*`, `test/*`, `src/tools/__tests__/*`) and the `test/support/mcp-stub-loader.mjs` registration-point interception loader to also capture `registerTool(...)` calls, not just the legacy `tool(...)` overload. Full suite `npx vitest run` (root) 2846 passed / 42 skipped / 0 failed, `tsc --noEmit` 0 (root + mcp-server), `npm run build` (mcp-server) 0 including the new `postbuild` boot-check gate.
- **The publication guard decides on the ARTIFACT, not the command prose — closes the ordinary-publish hole and the wrong-repo hash (tasks `k178g6rfyjpzw44bvsnfxh4acn8cfrge` + `k17773tm3kjk9f4nayhf0a0qx98cbt8d`).** `enforce-eta-approval-before-npm-publish.py` (v1.3.3 → v1.4.0) gated the whole hook on `is_fleet_publish(command)` matching command PROSE (package name typed in the command), so the most ordinary publish forms passed **unguarded** (measured rc=0 against a fleet package): bare `npm publish` from the package dir, `npm publish --access public`, `npm --workspace=@vantageos/pkg publish`, `pnpm -F pkg publish`, `jsr publish`, `deno publish`. Extending the motif list would only chase forms — Pi's explicit refusal condition. Root fix (`derive-never-type` applied to the guard itself): detect the publish VERB broadly via the shared `_lib/command_predicate` tokenizer (`strip_transparent` normalizes every package-manager `publish` invocation to a head token regardless of flags/workspace form; `jsr`/`deno` added), then decide fleet-ness by reading `package.json#name` in the resolved publish dir — the artifact, not the command. A publish verb whose dir/`package.json` can't be read is REFUSED loud (`REFUSED-UNREADABLE`), never a silent passthrough. Also fixes the wrong-repo defect: `resolve_publish_dir`'s leading-`cd` regex matched only the `cd <path>⏎npm publish` newline form, not the ordinary `cd <path> && npm publish` `&&`-joined form (it fell back to the session root, comparing HEAD against the wrong repo — the exact reason `@vantageos/data-lake` was structurally unpublishable); the regex now matches both forms. Companion `enforce-pi-authorization-before-prod-deploy.py` gains `strip_quoted_strings` (ported verbatim from the publish guard, applied before deploy-detection when the command carries no interpreter wrapper) so a prod-deploy command CITED inside a balanced quoted string no longer false-blocks legitimate work. TWO-FACED probes, bite-proofed firsthand on the live hooks: MUST_BLOCK the 6 ordinary forms + the `&&`-joined wrong-repo form + a wrong-HEAD publish; MUST_PASS a non-fleet publish from its own dir + a publish verb inside a quoted commit message + the positive control `npm publish @vantageos/mosaic-blocks` still blocks; deploy guard: quoted-prose passes, real unquoted deploy still blocks. Self-test 51/54 (the 3 failures — T15/T19-D1/T26-D2 — pre-exist on v1.3.3 as obsolete age-window assertions; v1.4.0 additionally FIXED T40-D5). Reuses the existing tracked `_lib/command_predicate` — no new dependency. **Declared residual:** a truly unterminated quote citing a deploy command still false-blocks (an unbalanced-quote regex cannot strip an unclosed string) — distinct from the balanced-quote case fixed here. Catalogue re-publish of the guard (byte-exact, prod) is a follow-up requiring the prod write secret.
- **The pagination class is closed: no paginated read of this DB can return an empty page before the true end (mission `k574p02mghppex86fpnk578n0x8cd0py`, lot 2 — task `k17as54sd910zvfqerrv3zyp898cdrv4`).** Lot 1 fixed `list_peers`; this closes the whole CLASS behind it. **Class definition:** any paginated read applying its `createdBefore` cursor bound AFTER an unbounded `.take(N)` (instead of pushing the bound into the fetch), so rows older than the cursor anchor are never fetched — an empty page returned before the end, indiscernible from "nothing left". **The perimeter is the sweep, not a list:** `grep -rn "createdBefore" convex/*.ts | grep -v __tests__` enumerated every paginated reader; each was cross-checked against its `.take(`. The audit's candidate list was the control — no table surfaced outside it. **All 10 remaining tables were CARRIERS, 0 healthy** (13 reader functions): `diary.list`, `fixPatterns.listByProject`/`listAll`, `errorMonitor.listErrors`, `mandates.list`, `messages.listMessages`, `issues.listByProject`/`listByOrchestrator`/`listByStatus`, `recurringTasks.list`, plus the fixed-buffer sub-shape (`limit*4+10` cursor-window, distinct but same class) in `businessUnits.list`, `components.list`, `githubRepoMapping.list`. **Fix by REUSE, never a variant** (variant proliferation is what created the class): every carrier now consumes the published PR #635 wide-scan-cap mechanism (`<THING>_LIST_SCAN_CAP`, `needsWideScan`/`fetchCap`), the same one lot 1 applied to `profiles`. TDD strict: 13 RED tests (`convex/__tests__/pagination-class-sweep-*.test.ts`, one per table — seed >1 page, paginate to no-cursor, assert the union contains every seeded id) reproduced the defect, then GREEN. **CLASS counter: carriers remaining = 0** — verified by sweep, every `createdBefore` reader now carries a scan cap. MUST_PASS held: no table fabricates a non-existent row; default limits + envelope-cap protections intact. Full suite `bunx vitest run` 2840 passed / 42 skipped / 0 failed, `tsc --noEmit -p convex/tsconfig.json` 0. No schema change (query handlers only) → no MCP mirror needed.
- **The daily hand-over index no longer dies every morning, and `list_peers` stops dropping rows (mission `k574p02mghppex86fpnk578n0x8cd0py`, lot 1 — task `k1787gbnj6hm70jm2qn9xh498x8cccx2`).** Two defects served the same function — an orchestrator finding its previous session's index at daybreak — and shipping one without the other leaves it broken: an index intact but unreachable is no better than one erased. **DEFECT 1 (index vs live summary shared one field):** `close-day` step 9 wrote the end-of-day index via `set_summary` → `profiles:updateDynamic`, which persisted it into `dynamic.currentTask` — the SAME single field the next morning's startup summary overwrites before `daily-start` step 3 reads it back. Class: durable state written into a field another routine legitimately overwrites (last-writer-wins, no separate slot), guaranteed-wrong every morning on every station. Fix: `convex/schema.ts` adds a dedicated `dynamic.endOfDayIndex` (optional), written **only** when explicitly passed — never seeded from `currentTask` (a seed would freeze the index to an arbitrary live status, the exact looks-valid-but-wrong state this mission eliminates); `currentTask` keeps updating the visible live summary on every call (MUST_PASS, guard against over-correction). Undefined until `close-day` writes it is the honest "no index yet" state. **DEFECT 2 (`list_peers` paginated an empty page before the true end):** `convex/profiles.ts` `listProfiles` re-ran an unbounded `.take(N)` per page then post-filtered `createdBefore`, so rows older than the cursor anchor were never fetched (`pi`/`eta` vanished from page 2). Fix by REUSE, not a second mechanism: consumes the published PR #635 wide-scan-cap pattern (`PROFILES_LIST_SCAN_CAP`, `needsWideScan`/`fetchCap`, mirrors `tasks.ts`/`missions.ts`/`briefingNotes.ts`). MCP (RULE #24 schema mirror): `mcp-server/src/tools.ts` `set_summary` gains an optional `endOfDayIndex` pass-through. TDD strict, both RED written from the named causes then GREEN: `convex/__tests__/profiles.summaryIndexClobber.test.ts` (index written via explicit `endOfDayIndex` survives the morning `currentTask` write; live summary still refreshed) and `convex/__tests__/profiles.listPeersPaginationGap.test.ts` (12 peers, paginate to end, union contains every id). Full suite `bunx vitest run` 2827 passed / 42 skipped / 0 failed, `tsc --noEmit` 0, `mcp-server` `npm run build` 0. **Hand-off (coordinator, on routine republish):** `close-day` step 9 passes `endOfDayIndex=` to `set_summary`; `daily-start` step 3 reads `dynamic.endOfDayIndex` (not `currentTask`). The defect-2 CLASS (10+ other `list_*` queries sharing the post-filter shape) is scoped to lot 2, out of this delivery.
- **PR template: close the two "typed-truth" traps + add a declared-residual section (task `k171vhn24rz7npqgmc7p3zeydd8b0v7d`).** The task's original premise (a bare 241-byte local template with no self-control block) was already superseded — the local `.github/PULL_REQUEST_TEMPLATE.md` was updated (Aug 4) to a 2092-byte version carrying a richer `SELF-GATE (STEP 0)` block (Claim / Replayable evidence / bite-proof) than the org template, and the hardcoded `29/29` ratio trap was already gone (placeholder `N` + "cite ratio"). The residual real trap remained: `Closes #` hard-typed in Related Issues invites auto-closing an issue on merge, before any activation proof. Fixed: `Closes #` → `Related: #` with a comment stating an issue closes on a **measured** activation (prod read-back), never on merge — reserve `Closes #<n>` for the case where the merge itself IS the activation (docs/template PRs). Also ported the org template's `## Residual, declared` section (absent locally) so every PR names what it knowingly leaves open. The local's richer SELF-GATE (STEP 0) — which Eta's `grep -ciE 'self.?gate'` step-0 gate targets — is deliberately KEPT over the org's simpler block (downgrading would regress the review contract). Proof: `grep -c SELF-GATE` → 2, `grep -E '[0-9]+/[0-9]+'` → none (no typed ratio), no bare `^Closes #`.
- **A review approval survives a head-hash change when the reviewed content did not move (k179q540).** `enforce-eta-approval-before-npm-publish.py` now carries an Eta approval forward on a SHA mismatch when the reviewed content is invariant, handling both cases distinctly: single-commit (squash) merge → whole-tree identity; rebase onto an advanced main → file-by-file on the touched files only (derived from git via `merge-base` + `diff --name-only`, never typed). Refuses and names the changed file otherwise. Closes the class where a whole review cycle was paid for a zero-content head-hash change (six occurrences across two repos in one morning). 3-case RED→GREEN TDD.
- **fix(convex): per-row error isolation in `recurringTasks:processDueTasks` — closes recurring 24h+ cron failure (#1167).** The cron looped over every due recurring row with no per-row try/catch, so a single poison row — e.g. a malformed `cronExpression` that makes `getNextRunTime` throw (`recurringTasks.ts:32`) — aborted the whole `internalMutation`; zero tasks were created and the cron surfaced the generic Convex error "Your request couldn't be completed. Try again later." every 15 min indefinitely. Fix: (1) `getNextRunTime` is now computed FIRST inside the loop, before any insert, so a bad cron throws before a task is written (a poison row creates no task at all — the catch does not roll back an insert that already succeeded); (2) the per-row body (next-run + insert + patch) is wrapped in try/catch — on error it `console.error`s the offending `recurring._id`, increments a `failed` counter, and `continue`s; (3) the handler returns `{ created, failed }`. One poison row can no longer block the good rows. TDD RED→GREEN: `convex/__tests__/recurringTasks.processDueTasks.isolation.test.ts` seeds two due rows (one valid cron, one malformed inserted directly to bypass create-time validation) and asserts processDueTasks does not throw, `created===1`, `failed===1`, the good row created exactly one todo task and the poison row created none — the test discriminated a first-iteration ordering defect (insert-before-nextRun) then GREEN 1/1. Non-regression: recurringTasks suite 22/22, `tsc --noEmit` 0. Branch `fix/recurring-processduetasks-isolation-1167`.

### Changed
- **`check-messages` skill re-synced byte-exact to VR canonical `3.3.0` (contentVersion 1.0.21, `sha256 b568aba…`) — BOTH copies.** Pi published `3.3.0` removing the "chain at most ONCE per cron firing" cap (root cause of orchestrators stopping after one task while the queue still had unblocked work — the numbered-cap-instead-of-derived-condition disease). Step 3.5→3 now chains while the queue yields an unblocked task, stopping on the derived condition (`unblocked-task-is-the-order.md`). This repo holds **two copies** of the skill and both had drifted: `.claude/skills/check-messages/SKILL.md` (the workspace runtime copy the cron reads) was a divergent `5.0.0` fork, and `plugin/skills/check-messages/SKILL.md` (the copy PACKAGED and shipped to clients via the `vantage-peers` plugin) was still `3.2.0` with the cap — the latter is what the `test_vr_parity` CI gate compares against VR canonical, so it failed on every PR until aligned. Both are now byte-exact with VR canonical. **Why two copies, and which is the source:** the source of truth is the VR canonical (`get_skill_content name=check-messages`); the two in-repo copies serve two distinct roles — runtime (`.claude/`) and client distribution (`plugin/`) — and the skill's own rule requires each to mirror VR byte-exact. A deeper root-fix (generate one copy from the other, or from VR, so they cannot diverge) is a tracked follow-up. Proof: `sha256sum` of BOTH copies == canonical `b568aba19c8cd5cb23f4d378543315aee8d5ed5247591cfe6a6eb2d81456a952`; `grep -ci "at most ONCE per cron firing"` → 0 in both.
- **`.claude/hooks/` is now version-controlled** (removed from `.gitignore`; all 44 hooks tracked). Guards were previously on-disk + VR-catalog only, never auditable in-repo.
- **ALL of `.claude` is now version-controlled** (task `k170vg5r`, Laurent directive). The `.gitignore` previously excluded `.claude/settings.json`, `settings.local.json`, `agents/`, `agent-memory/`, `references/`, `skills/` and the lock patterns, so agents/skills/settings were invisible, unreviewable, and wiped on the next branch switch (the FLAG-B class: a file that is gitignored-but-would-be-clobbered). The excludes are removed and replaced by a re-inclusion block placed AFTER the repo-wide `__pycache__/`/`*.pyc` rules so last-match-wins: `!.claude/` + `!.claude/**/` (the middle line is mandatory — without re-including the directories git never descends into them) + `!.claude/**`, then the only exceptions — regenerated caches and the run lock, not content: `.claude/**/__pycache__/`, `.claude/**/*.pyc`, `.claude/**/.pytest_cache/`, `.claude/scheduled_tasks.lock`. Secret pre-scan (values redacted) on all 137 would-be-tracked files: clean — `settings.local.json` `env` is `{VP_ORCHESTRATOR: sigma}` only, no key/token. `git status --porcelain --ignored .claude` non-exception ignored count 25→0; tracked `.claude` files 63→137; zero cache/lock staged; zero tracked-and-ignored (FLAG-B) file.

## [2.18.0] — 2026-08-11

(vantage-peers-mcp package release: S8 CORE tool-exposure filter — mission vp-mcp-alias-cleanup-v1)

### Changed
- **chore(mcp): expose only the CORE tool surface** (mission `vp-mcp-alias-cleanup-v1`, S8, merged #1174 @105b6a8). A data-driven allowlist (`mcp-server/tool-exposure.json`, `{"core":[...]}`) masks every tool whose `T2_verdict` is not `CORE` in `analysis/vantagepeers/vp-restructuring/vp-by-tool-day158.csv`. Masking ≠ deletion: non-CORE tools stay registered + handler-wired and are `disable()`d so `tools/list` does not advertise them; reverting = removing a line from the data file. Exposed = CORE ∩ registered = **66** tools; masked = **43** (registered 109). The exposure list is derived from the CSV, never typed; a name in the file matching no registered tool makes the server refuse to start, naming it. Applied at the single shared `registerTools` surface, so both stdio and HTTP transports (online clients) inherit the reduced surface. Tests: `test/tool-exposure.test.ts` 2/2, full suite 1071 passed / 0 failed, tsc 0.

## [2.17.0] — 2026-08-11

(vantage-peers-mcp package release: 14 duplicate alias tools removed — mission vp-mcp-alias-cleanup-v1)

### Removed
- **chore(mcp): remove 14 duplicate alias tools, keeping the fleet-used survivor of each pair** (mission `vp-mcp-alias-cleanup-v1`, S2; arbitration `elpi-corp/analysis/vantagepeers/vp-restructuring/S1-arbitrated-pairs-day159.md`). The survivor of each pair was chosen by **fleet call-site usage, never the code's `DEPRECATED ALIAS` label** (which was inverted for the memory-search tools — the code labelled the fleet-primary `recall`/`text_search` as the aliases). Removed → survivor kept: `create_diary`→`write_diary`, `create_fix_attempt`→`add_fix_attempt`, `check_fix`→`validate_fix`, `check_mandate_spending`→`validate_mandate_spending`, `delete_repo_mapping`→`remove_repo_mapping`, `register_repo_mapping`→`add_repo_mapping`, `delete_deployment`→`remove_deployment`, `register_deployment`→`add_deployment`, `update_summary`→`set_summary`, `create_task_dependency`→`add_task_dependency`, `search_components_by_keyword`→`search_components`, `search_fix_patterns_by_semantic`→`search_fix_patterns`, `search_memories_by_semantic`→`recall`, `search_memories_by_keyword`→`text_search`. Context-token saving: **1347 tokens (11.1%)** of the tool-list surface. TDD strict RED→GREEN: `mcp-server/src/__tests__/alias-cleanup-day159.test.ts` asserts each survivor IS registered and each condemned is NOT — RED 14 fail against current code → GREEN 28/28. Registered tool count 123→109. `npm run build` (tsc) exit 0. Surviving tool descriptions/examples that cross-referenced a deleted twin were cleaned. Deletion trace retained in `elpi-corp/analysis/vantagepeers/vp-restructuring/vp-by-tool-day158.csv` (status `SUPPRIMÉ`).

### Fixed
- **fix(security): `send_message channel="broadcast"` is now bounded to the emitter's tenant — closes a cross-tenant leak** (mission `fix-broadcast-org-scoped-v1`, Pi-ruled Option B, zero migration). The broadcast branch of `convex/messages.ts` `sendMessage` resolved recipients as `ctx.db.query("profiles").collect()` — every registered orchestrator across ALL tenants — so an internal-tenant emitter's broadcast reached external CLIENT orchestrators (e.g. victor/Marie, proxima/Ismaël). Fix resolves the emitter via `withOrgScope(ctx)` **fail-closed** (never the client-supplied `args.tenantId`): a **master/internal** emitter (identity present, resolved to master via the `CLERK_SERVICE_ACCOUNT_USER_ID` carve-out at `lib/auth.ts:111-121` — the MCP server forwards this identity) fans out to all orchestrators MINUS the union of every ACTIVE `client_org_mapping.allowedOrchestrators` (`schema.ts` `by_isActive`), so an internal broadcast never reaches a client; a **client-scoped** emitter fans out only to `scope.allowedOrchestrators` (its own tenant); an **anonymous/no-identity** caller resolves to `isMaster=false` + `allowedOrchestrators=[]` → zero recipients → the existing zero-recipient bounce fires (fail-closed, no fail-open path to the internal fleet). No `allowNoIdentityMaster` (consistent with the Day-156 SEC-AUDIT). DMs, intra-tenant comma-lists, and intra-tenant broadcasts are unchanged. TDD strict RED→GREEN: `convex/__tests__/broadcast-org-scoped.test.ts` — cross-tenant A→B not received, master→client excluded, anonymous fail-closed bounce, intra-tenant received; `git stash` impl → 3 failed → restore → pass. The master fleet-wide branch is gated on `isMaster && orgSlug === null` (TRUE internal only — a client org with the `["*"]` read sentinel is NOT the internal master and falls to the fail-closed client branch), and the internal-exclusion set considers ALL `client_org_mapping` rows (active AND inactive) so a disabled client's orchestrators are never re-absorbed into the internal broadcast pool. Full suite `npx vitest run convex/` 1007/1007 (101 files), `tsc --noEmit` 0 on convex + mcp-server. Branch `feat/broadcast-org-scoped`.
- **fix(auth): de-hardcode the Clerk issuer domain in `convex/auth.config.ts` — read `process.env.CLERK_JWT_ISSUER_DOMAIN`, never a literal** (task `k1730kpyw3nwtds5ewhtd68nah8c123r`, no-hardcoded-business-knowledge violation). `auth.config.ts` hardcoded `domain: "https://sharp-sponge-67.clerk.accounts.dev"` — the real JWT issuer, but graven into source. Fix: `domain: process.env.CLERK_JWT_ISSUER_DOMAIN` (no fallback literal — "jamais en dur"; the canonical Convex+Clerk pattern), `applicationID: "convex"` unchanged (fixed JWT-template name, not tenant knowledge). **Anti-re-freeze sequence (Pi-ordered)**: the env var `CLERK_JWT_ISSUER_DOMAIN` was set to exactly `https://sharp-sponge-67.clerk.accounts.dev` on BOTH Convex deployments (dev `efficient-guineapig-356` + prod `compassionate-goldfinch-737`, readback-confirmed) BEFORE this code change — so reading the env yields identical behaviour to the removed literal (zero behaviour change, zero re-freeze). This also corrected dev's stale `CLERK_JWT_ISSUER_DOMAIN=clerk.vantageos.agency` (a wrong value that was never read while the literal stood) and added the var on prod (was absent). TDD: `convex/__tests__/auth-config-no-hardcode.test.ts` — (A) source-level anti-hardcode assertion (no `sharp-sponge-67`/`clerk.accounts.dev` literal, references `process.env.CLERK_JWT_ISSUER_DOMAIN`), (B) behaviour-parity (env sentinel flows to `providers[0].domain`); RED 2/2 on the hardcoded file → GREEN 2/2 after. Product suite `npx vitest run convex mcp-server` 2767 passed / 0 failed (12 skipped), `tsc --noEmit` 0 — the only file-load failure is the vendored `resources/vantage-peers-dashboard/` (a separate UI repo mis-vendored into the tree, pre-existing, unrelated). Branch `sigma/dehardcode-auth-config-issuer`.
- **fix(security): forward per-request identity from MCP server to Convex — closes P0 regression from #1156 (flotte + external clients locked out).** `mcp-server/server-http.ts`'s `/mcp` handler built a PLAIN `new ConvexHttpClient(tenant.convexUrl)` per request — no identity ever attached — so after #1156 removed the `allowNoIdentityMaster` fail-open carve-out (correctly closing the cross-tenant hole), every `/mcp` call reached Convex with `ctx.auth.getUserIdentity() === null` and hit `withOrgScope`'s fail-closed branch: `RBAC_DENIED: Missing scope ... for org "null"` for the fleet AND every legitimate external client. Fix: extracted `selectConvexClientForRequest` (`mcp-server/src/authenticatedConvexClient.ts`) — the per-request Convex client now always carries an identity, selected by how the caller authenticated (`mcp-server/src/auth.ts` `bearerAuthMiddleware`): (1) master / (2) OAuth-scoped / (3) DCR / (4) legacy mcpTenants bearer → the MCP server's own Clerk service-account identity (`createServiceAccountConvexClient`, `withOrgScope`'s `CLERK_SERVICE_ACCOUNT_USER_ID` carve-out → `isMaster=true`; isolation for the non-master variants among these is enforced at the MCP tool layer via `guardRead`/`guardWrite`/`checkNamespaceRead`/`checkNamespaceWrite` — unchanged, pre-existing behaviour, not widened by this fix); (2.5) Clerk-team org-scoped client → the caller's OWN verified Clerk JWT is now forwarded (`OAuthContext.clerkJwt`, new field set only on this path, `client.setAuth(oauthCtx.clerkJwt)`) — genuine Convex-layer per-org isolation, a strict narrowing versus both pre- and post-#1156 behaviour for this path. TDD: 3 poles in `mcp-server/src/__tests__/mcp-identity-forwarding.test.ts` (6 tests) — service-account selection (flotte/master), Clerk-JWT forwarding + non-cross-contamination between two callers' tokens, and a regression guard that reproduces the exact pre-fix "always plain, never setAuth" shape and proves the pole-2/3 assertions go RED against it (demonstrated live: reinstated the pre-fix shape, 5/6 red, reverted, 6/6 green). Full suite: `npx vitest run mcp-server convex` 2765/2765 passed (12 skipped, 1 pre-existing unrelated failure in `resources/vantage-peers-dashboard` outside this change's scope), `tsc --noEmit` 0 errors on both packages. Branch `sigma/fix-mcp-identity-forwarding`. Activation requires prod env vars not yet set: Convex prod (`compassionate-goldfinch-737`) needs `CLERK_SERVICE_ACCOUNT_USER_ID`; MCP Railway prod needs `CLERK_SECRET_KEY` + `CLERK_SERVICE_ACCOUNT_USER_ID` (+ optional `CLERK_DOMAIN`/`CLERK_JWT_TEMPLATE`, default `"convex"`) — Sigma applies these under Pi token, not part of this commit.
- **fix(security): close `allowNoIdentityMaster` cross-tenant hole on 17 client-reachable public queries (SEC-AUDIT Day 156).** Auth model established first: MCP-server traffic never reaches Convex without identity (`mcp-server/src/authenticatedConvexClient.ts` fail-closed — refuses to send `query`/`mutation`/`action` unless a fresh service-account token minted); so `allowNoIdentityMaster: true` (`convex/lib/auth.ts` withOrgScope) was reachable **only by a genuinely anonymous, unauthenticated direct caller of the public Convex API** — no MCP tool, no dashboard, no Clerk session required, just the deployment URL. All 17 of the 24 grepped `allowNoIdentityMaster` occurrences were public `query` exports (`briefingNotes.searchBriefingNotesByKeyword`, `dashboard.getDashboardSummary`/`getProjectSummary`, `diary.list`, `memories.getMemory`/`listMemories`, `messages.listMessages`/`listByChannel`/`searchMessagesByKeyword`, `missions.list`, `stats.orchestratorStats`/`fleetStats`, `tasks.list`/`listPaginated`/`billingSummaryByProject`/`taskDurationDistribution`/`searchTasksByKeyword`) — CLIENT-ATTEIGNABLE, verdict proven per-site. Fix: removed `allowNoIdentityMaster: true` from all 17, restoring `withOrgScope`'s documented fail-closed default (no identity → `isMaster=false`, `scopes=[]` → `requireScope` throws `RBAC_DENIED`). Two sites (`missions.list`, `tasks.list`) had one genuine internal-fleet caller — `convex/http.ts`'s GitHub webhook handler (HMAC-signature-verified, not Clerk-gated) — refactored into `runMissionsList`/`runTasksList` shared handlers plus new `internalQuery` `missions.listForWebhook` / `tasks.listForWebhook` (structurally unreachable from any `api.*` client) called by `http.ts` instead. TDD: new `convex/__tests__/allowNoIdentityMaster-reachability.test.ts` (17 tests, RED against pre-fix code pasted in task evidence — GREEN after), 18 pre-existing test files updated from the stale "no-identity ⇒ MCP/CLI master" assumption to the recognized service-account identity (`CLERK_SERVICE_ACCOUNT_USER_ID` test env) or explicit `RBAC_DENIED` assertions. Bipolar proof: reintroduced `allowNoIdentityMaster: true` on `tasks.list` → RED (org-b task leaked to anonymous caller) → reverted → GREEN. Full suite `npx vitest run convex/` 971/971 passed (96 files), `tsc --noEmit` 0 errors. Branch `sigma/sec-audit-nomaster-reachability` (PR #1156). Prod activation follows #1156 merge via a dedicated Pi-authorized deploy.
- **docs(auth): correct stale `withOrgScope` docstring — no-org master is a named service-account grant, never granted by absence** (follow-up to #1123 / `239fc67`; false-P0 root cause for VP task `k171fsvf8738gxdgr9cj2cjwhd8b2qds`). The docstring for `withOrgScope` (`convex/lib/auth.ts`) still claimed `No org attached (identity present) → isMaster=true (existing Alpha callers, unchanged)`. That line was made **false** by #1123: a no-org identity now resolves to `isMaster=true` **only** when its `subject` matches `CLERK_SERVICE_ACCOUNT_USER_ID` (the MCP service account), and **any other** no-org identity is refused with `RBAC_DENIED` via `requireTenantId`. The stale comment mis-signalled a live fail-open (reading `sed -n '55,125p'` surfaces the comment before the gated code beneath it), triggering a false P0 and a wasted specialist dispatch. Comment-only; **no behaviour change** — cross-tenant deny tests already green (`convex/lib/auth.test.ts` 14/14, incl. `no org, ARBITRARY identity → refused` and `no org, recognized service account → master scope`).
- **fix(mcp): scope-guard `mark_as_read` + `bulk_complete_tasks` — close cross-owner / identity-spoof holes** (task `k179nrp3apj700pm0h1ckewm2h8b3nz7`). Of the 13 tools flagged as unguarded, an in-body audit found **11 already guarded** (wrapper `kind` or in-body `scopeFilter*`) and register-time enforcement (`registerTool.ts` mandatory `scope`) already present; only two were genuinely unguarded. `mark_as_read` accepted caller-supplied `receiptIds` with **no ownership check** at either layer (`convex/messages.ts:markAsRead` validated ID format, never owner) — any caller could mark another orchestrator's mail read; it now takes an optional `callerOrchestrator` and each resolved `receipt.recipient` must match or the mutation throws `RBAC_DENIED` and mutates nothing (MCP side promoted to `kind:"from"`, wrapper-enforced). `bulk_complete_tasks` accepted a spoofable `callerOrchestrator`; it now calls `guardFrom(callerOrchestrator)` before dispatch. Scope boundary respected: **no change to `convex/lib/auth.ts`** (owned by the multi-tenant-fail-closed mission). Recount 0/117 unguarded (origin/main = 2, proving the instrument). TDD: 3 new test files (`convex/__tests__/messages.markAsRead.crossOwner.test.ts`, `mcp-server/src/__tests__/mark-as-read-scope-guard.test.ts`, `bulk-complete-tasks-scope-guard.test.ts`) RED-before/GREEN-after + bipolar control (neutralize guard → RED at named line → restore → `diff` empty → GREEN); `npx vitest run convex mcp-server` 2732 passed, `tsc --noEmit` 0 both packages. No prod deploy (prod deploy-frozen) — code + tests only.

### Added
- **feat(tasks,missions): terminal `cancelled` status — creator-only, mandatory reason, excluded from actionable queues** (mission `vp-fix-cancelled-status-v1` `k576vya59gycygdaq09wsczpq18c5qh0`, Pi-dispatched). Class of failure: a task/mission created in error had no clean retirement — `delete_task` is blocked in prod, `complete_task` falsifies it as `done`, and `blocked` is dead limbo. Fix adds a `cancelled` literal to BOTH the `tasks` and `missions` status unions (`convex/schema.ts`) plus optional `cancelledBy`/`cancelReason` fields; the `update` mutations in `convex/tasks.ts` and `convex/missions.ts` special-case the cancelled transition with a **creator-only** RBAC gate (mirrors `deleteTask` — the row's `createdBy` or `system`, NOT the assignee) and reject an empty/whitespace reason with `CANCEL_REASON_REQUIRED`. A cancelled row is naturally excluded from the `open`/`active` aliases (which enumerate actionable statuses explicitly) and never counted as done. `mcp-server/src/tools.ts` exposes `cancelReason` on `update_task` and `callerOrchestrator`+`cancelReason` on `update_mission` (RULE #24 schema mirror). The `delete_task` guard message (`.claude/hooks/block-delete-on-prod.py`) is rewritten to redirect to the cancel path instead of `complete_task`. TDD strict: `convex/__tests__/cancelledStatus.test.ts` (8 tests) — creator-cancel stores creator+reason, non-creator/assignee refused, empty reason refused, cancelled excluded from open/active + included in all, for tasks AND missions; RED→GREEN proven by stashing the impl (8/8 fail → 8/8 pass). Build gate: `npx vitest run convex/` 986/986, `tsc --noEmit` 0 on convex+mcp-server. DEV-verified live on `efficient-guineapig-356` (deploy-target readback confirmed): creator-cancel accepted, assignee refused with `RBAC_DENIED: Only <creator> or system can cancel`, empty reason → `CANCEL_REASON_REQUIRED`. Branch `feat/vp-cancelled-status-v1`; PROD deploy gated on Pi `[PROD-DEPLOY-AUTHORIZED]` + QA (mission task T3).
- **feat(tasks): concurrent in_progress tasks per orchestrator, bounded to one per distinct `project` (repo/stream)** (mission `vp-concurrent-active-tasks-per-stream-v1` `k57bmyk5fk511h96gmt1v5b8q58c2qgf`, task `k178h4schdhvwa8qf8k7d6se958c2mj2`, Laurent decision Day 156). Class of failure: the server rule "one in_progress task per orchestrator" blocked real multi-repo parallelism — an orchestrator running N background sub-agents on N distinct repos could not track N concurrent in_progress tasks each with its own chrono. Fix relaxes the guard, bounded: **one in_progress task per orchestrator PER DISTINCT `project`**, never unlimited inline work. Two coupled enforcement layers, both bound on `project` derived from VP state (zero MCP-server coupling): (1) SERVER source of truth — `convex/tasks.ts` `start` mutation now queries index `by_assignee_project` `[assignedTo, project, status]` (already in schema, no migration) and throws `TASK_START_BLOCKED` only on a different in_progress task in the SAME `project`; the error payload now names the `project`. (2) CLIENT — `.claude/hooks/enforce-irp-sequence.py` (v2→v4) self-derives the started task's `project` from VP by `taskId` (new `probe_task_via_cmd`/`probe_task_via_http`, mirroring the existing in_progress probe) and bounds its conflict check the same way; fails open + says so on stderr when the probe is unreachable. Invariants intact: `startedAt`/`completedAt`/`actualMinutes` stay per-task (zero fabricated chrono); instant start→complete stays banned by the separate billable-time-tracking guard (`convex/lib/taskClosureGate.ts`, untouched); `project === undefined` tasks share one "default" stream → still mutually exclusive (conservative default, preserves pre-relaxation behavior; only projected tasks gain concurrency); `"system"` caller stays exempt. TDD strict RED→GREEN with **real-usage concurrent test** (Laurent mandate): `convex/__tests__/tasks-concurrent-per-project.test.ts` (5 tests) — 2 RED→GREEN (unit distinct-project start; real-usage concurrent lifecycle proving distinct `actualMinutes` 15 vs 30) + 3 bounds (same-project blocked, instant start→complete `TASK_NEVER_STARTED_BILLABLE`, null-project mutually exclusive); `.claude/hooks/tests/test_enforce_irp_sequence.py` +4 self-derived-project tests. Full `npx vitest run convex/` 978/978 passed, `tsc --noEmit` 0. **Live activation proof on DEV** (`efficient-guineapig-356`, deploy-target readback confirmed): 2 tasks distinct projects same orchestrator → both `start` concurrently (was `TASK_START_BLOCKED` before) ; same-project 3rd → `TASK_START_BLOCKED ... in project "vp-concurrent-smoke-a"` ; smoke data created+deleted (present→absent confirmed). Hook change ships via VantageRegistry (`.claude/hooks/` is git-ignored, not in the PR diff). Branch `sigma/vp-concurrent-active-tasks-per-stream`.
- **feat(factory): executable deploy preflight + versioned env manifest — ends env-var re-discovery at every deploy** (task `k172pw4v5xwgsytfbva19663h18c0mzg`). Class of failure: each deploy re-discovered which env-var names exist where, because names diverge across environments (dev `CLERK_JWT_ISSUER_DOMAIN` vs prod legacy `CLERK_ISSUER_URL`; prod convex deploy key is the BARE `CONVEX_DEPLOY_KEY` not a `_VANTAGE_PEERS` suffix; Clerk creds suffixed `_VANTAGE_PEERS` in `.env.local`), and creds are scattered (`.env.local` + Convex env + Railway env). Fix = DATA source-of-truth + EXECUTABLE gate: `deploy/env-manifest.json` (per project × environment: exact required var NAMES for Convex dev/prod + Railway prod, `deployKeyEnvVar`, `localCredsMap` logical→real to absorb divergence — names only, no values, no-hardcoded-business-knowledge as data) and `scripts/deploy-preflight.mjs` (pure `resolvePreflight({manifest,project,target,presentNames})` core + CLI that gathers present NAMES from the TARGET deployment's `convex env list` (deploy key read from `.env.local`, never printed) / `railway variables`, reports each required var PRESENT(source)/MISSING(exact name), and `process.exit(1)` BLOCK naming the missing var(s); never prints any value). TDD bipolar `scripts/__tests__/deploy-preflight.test.mjs` (3 tests): MISSING-named, PRESENT-not-false-missing, divergence-absorbed (logical→bare-name). Firsthand: real `node scripts/deploy-preflight.mjs vantage-peers convex-prod` → 5/5 PRESENT (source convex:prod) exit 0; forcing the core `ok:true` → Test A RED naming `CLERK_JWT_ISSUER_DOMAIN` → restored GREEN 3/3; `tsc --noEmit` clean; 0 value-leaks. Branch `sigma/deploy-preflight-env-manifest`.

- **feat(messaging): `send_message` bounces on a zero-recipient channel — no more silent send-to-nobody** (task `k17dr97dwpe07n9zfgzzypkfm18bv6ws`, prereq audit `k171v7j2720hzxebbfde0dh4rd8bvepc`). The Convex `sendMessage` mutation (`convex/messages.ts:49-95`) previously split a non-broadcast `channel` into tokens and inserted a receipt row per token **without checking any membership table** — a phantom channel (unknown role, non-existent instance, reserved word like `"direct"`, empty string, unknown comma-list part) resolved to zero real recipients yet still returned a `messageId` with no error (the mutation `returns: v.id("messages")` had no delivery signal). Now the non-broadcast branch resolves every non-self token against the `profiles` table (the same authority the broadcast branch already uses — `orchestratorId`/`instanceId`, `convex/schema.ts:118-142`) and throws an actionable `ConvexError` **before any receipt is written** (`delivered=0` by construction; a returned `messageId` now implies `delivered≥1`). Error names the offending channel + restates valid forms (`<role existant> | <instance> | broadcast | liste "eta,pi"`). The recipient set is **derived** from the org, never assumed delivered (doctrine derive-never-type); bounce triggers on **empty-resolution**, not a hardcoded denylist. MCP call site (`mcp-server/src/tools.ts:2936-2943`) needed no change — the existing catch routes `error.data` verbatim. TDD: `convex/__tests__/sendMessage.bounceEmptyRecipients.test.ts` RED 4-fail (silent send proven on `direct`/`role-inexistant`/empty/`eta,talos`) → GREEN 8/8 incl MUST_DELIVER (broadcast, real role, real instance, `eta,pi`). Blast-radius collateral: seeded minimal `profiles` fixtures in 13 existing tests. `vitest run convex mcp-server` 2733 passed, `tsc --noEmit` 0. **Deploy-gating note**: `profiles` is now the authority for recipient existence — verified via `list_peers` that all active roles+instances are registered (~50 rows); a send to a real-but-unregistered recipient would bounce, so activation is gated on profiles-completeness. No prod deploy (frozen).
- **tooling: byte-safe file→VR upsert path (`scripts/vr_upsert_content.py`) — no hand-retype of large hooks/skills** (task `k17dj6j8ev935ce4nptzzdqj558bym9k`, doctrine derive-never-type). `upsert_hook_content`/`upsert_skill_content` take content INLINE; a 71KB hook cannot be hand-retyped without risking a byte mismatch (hash ≠ source). New stdlib-only CLI `python3 scripts/vr_upsert_content.py --kind {hook,skill} --name <item> --file <path> [--version <semver>] [--dry-run]` reuses `scripts/vr_plugin_parity.py`'s VR JSON-RPC transport: reads the file bytes verbatim → `upsert_*_content` → reads back via `get_*_content` → asserts `sha256(local) == sha256(VR-returned) == VR contentHash`, fail-loud non-zero on any mismatch/absent/unreachable (never a silent pass). Raw sha256 (no trailing-newline strip) — matches VR's `contentHash` convention; verified `content_hash(enforce-eta-approval-before-npm-publish.py) == 56f03965…`. `--dry-run` = read-only verify (no write token). Never embeds a token; the WRITE token is supplied by the RULE #30 single-writer at invocation. Tests `tests/plugin/test_vr_upsert_content.py` (6 passed, offline, monkeypatch the `_vr_rpc_call` seam) incl the FAIL-bite (VR returns different content → non-zero) + absent → non-zero + dry-run-no-token + write-token-required.
- **feat(messaging): `check_messages` now surfaces a server-derived `pendingOnYou[]` block — closing the fleet-freeze blind spot** (mission pi-pending-on-me-queue-v1, task `k176bjye4kvpgg0qf6fkrneq558btx7c`). The messaging "silence contract" made a peer BLOCKED on the caller's decision invisible; now every `check_messages` call PULLS that queue, exactly as it already pulls `staleInProgress`. New `computePendingOnYou(ctx, caller, now)` in `convex/lib/taskClosureGate.ts` mirrors `computeStaleInProgress`: it derives (never stores) the `status="blocked"` tasks whose unblock authority is the caller (signal: `task.createdBy === caller` — always-present, unforgeable-by-omission, distinct from the assignee-side `staleInProgress`; title-marker parsing like `[PROD-DEPLOY-AUTHORIZED]` was deliberately dropped in favor of `createdBy` per robustness). Each entry: full 32-char `taskId`, title, waiting `assignee`, derived `age`. Bounded — `by_status` index, newest-first, `.take(200)` + in-memory `createdBy` filter, never unbounded `.collect()`. Wired into the `checkNewMessagesEnvelope` return (validator added) and passed through the MCP `check_messages` tool (`mcp-server/src/tools.ts`), including when there are zero unread messages (the early-return was extended so the block still reaches the caller). TDD: `convex/__tests__/pendingOnYou.test.ts` bipolar — POS (caller-created blocked task listed with full id) + NEG (blocked task created by someone else NOT listed); `pendingOnYou`+`staleInProgress` 5/5, `tsc --noEmit` 0 in both packages. convex-reviewer SHIP-WITH-NITS (fast-follow filed: a `by_createdBy_status` index / truncation flag to remove the global-window false-negative risk under heavy fleet load, and `orgId` scoping on both derived queues — a pre-existing gap carried from `computeStaleInProgress`).
  - **Day 152 SLA-AGE addition**: each `pendingOnYou` entry now also carries `cyclesWaiting`/`slaBreached` (Laurent decision: breach = `SLA_BREACH_CYCLES = 3` cycles, cycle period config-driven via `taskClosureConfig["pendingOnYouCycleMs"]`, default `DEFAULT_PENDING_ON_YOU_CYCLE_MS = 1_800_000` i.e. 30 min, mirroring `getStaleInProgressThresholdMs`); MCP `check_messages` now renders SLA-breached entries in a distinct `⚠️ SLA-BREACHED pendingOnYou` alert section before the normal list. TDD: `convex/__tests__/pendingOnYou.test.ts` extended to 4/4 green (new POS 3-cycles-breached + NEG 1-cycle-not-breached), `tsc --noEmit` 0 errors in both `convex/` and `mcp-server/`.
  - **Day 154 dormant-tag exclusion** (task `k17fj34st7jp61tx1va2x46qq98btfxc`, doctrine `derive-never-type`): deliberately-parked tasks are excluded from `pendingOnYou`/`slaBreached` via a structured, dedicated `tags` marker (`DORMANT_TAGS = {"dormant","parked","deferred"}`, case-insensitive) — never a title-substring heuristic (explicitly rejected). New `isDormant(task)` in `convex/lib/taskClosureGate.ts`; dormant tasks appear in neither `pendingOnYou` nor its SLA-breached subset. TDD: `convex/__tests__/pendingOnYou.test.ts` extended to 7/7 green (NEG `tags:["dormant"]`, NEG `tags:["parked"]`, POS regression guard with non-dormant tag), `tsc --noEmit` 0.
  - **Day 156 SLA-output cap — the full `pendingOnYou[]` array is REMOVED from the envelope (measurement-integrity: volume drowns the signal)** (task `k17786e5nfxg32ypmwvdezyj0h8btnnn`, doctrine `derive-never-type`). `checkNewMessagesEnvelope` was returning ALL ~110 `pendingOnYou` entries on every `check_messages` call, flooding the MCP response every 3-min cron tick. The envelope return now carries a SUMMARY, never the full dump: `pendingOnYouTotal: number` (count of all pendingOnYou entries), `slaBreachedTotal: number` (count of SLA-breached entries), `slaBreachedTop: PendingOnYouEntry[]` (top-N breached entries sorted by `cyclesWaiting` DESC, capped — no task is lost: the total says how many, the top-N says which to act on first). `computePendingOnYou` itself is UNCHANGED (still returns the full bounded `.take(200)` list; summarization happens in the envelope handler). New `getSlaBreachedTopN(ctx)` in `convex/lib/taskClosureGate.ts` mirrors `getPendingOnYouCycleMs`/`getStaleInProgressThresholdMs` exactly (indexed `by_key` unique lookup on `taskClosureConfig["slaBreachedTopN"]`, `Number.isFinite && >0` guard, default `SLA_BREACHED_TOP_N_DEFAULT = 10`). MCP `check_messages` (`mcp-server/src/tools.ts`) updated to render `pendingOnYou: N total, M SLA-breached (top K shown)` followed by JSON of `slaBreachedTop` only, plus `(+R more not shown)` when `slaBreachedTotal > slaBreachedTop.length` — the server already filtered/sorted/capped, the tool only renders. TDD: `convex/__tests__/pendingOnYou.test.ts` — all prior assertions migrated to the new shape, plus a new bipolar CAP case (under-cap: 3 seeded → `slaBreachedTop.length===3`/`slaBreachedTotal===3`; over-cap: 12 seeded with distinct increasing ages → `slaBreachedTop.length===10`, `slaBreachedTotal===12`, sorted `cyclesWaiting` DESC, 2 smallest-age tasks absent from top-10) — 11/11 green. `tsc --noEmit` 0 errors in both `convex/` and `mcp-server/`.
- **feat(stats): `fleetStats` — real server-side VP fleet totals, no SCAN_CAP floors** (VP task `k177xw1r3qr5fkv3dr5f2jcfhs8bqz7f`). MCP `list_missions`/`list_tasks` paginate with a SCAN_CAP (~2000 rows) and can only ever report floors ("at least N"), never true totals. New `convex/stats.ts` `fleetStats` query counts `bus` (businessUnits) total, `missions` total + per-status (brainstorm/plan/execute/validate/complete), `tasks` total + per-status (todo/in_progress/review/blocked/done), and `missionTemplates` total — all server-side, where a single tool call has no MCP response cap. Counting uses `for await (const row of query)` — Convex's own idiom for unbounded enumeration — never `.collect()`/`.take()`, so it cannot exceed the 16MB single-execution limit regardless of table size; a manual `.paginate()`-loop per status was tried first and rejected after confirming against the real dev deployment that Convex allows only one `.paginate()` cursor per function execution. Every status enum literal is explicit (0 for empty, never an omitted key). New `scripts/vp-fleet-stats.mjs` — thin, idempotent, re-runnable — prints a human table + raw JSON, never a token. TDD: `convex/__tests__/stats.fleetStats.test.ts` 3/3 green, including a positive-control seed of exactly 3 `in_progress` tasks + 16 `validate` missions (Day 150 known-real prod shape). Confirmed executing against the real dev deployment (`efficient-guineapig-356`): bus=9, missionTemplates=5, missions.total=159, tasks.total=3379.
- **Amendment — `fleetStats.messages` (Laurent scope extension, same VP task `k177xw1r3qr5fkv3dr5f2jcfhs8bqz7f`, same PR #1136)**. Added a `messages` block: `total` (streamed count of the `messages` table) + `byReadStatus: { read, unread }`. Modeling decision: read state is NOT on `messages` itself — it lives on `messageReceipts.readAt` (`undefined` = unread, ms-epoch = read; see `convex/schema.ts`), because messaging is fan-out (one receipt row per recipient per message), so `byReadStatus` counts receipts, the correct unit for "how many read/unread notifications exist fleet-wide" — `messages.total` still counts distinct messages. `by_recipient_unread`/`by_instance_unread` both require an equality-prefix this fleet-wide count doesn't have, so the new `countReceiptsByReadStatusStreamed` helper streams the full `messageReceipts` table via the same `for await` idiom as the rest of `fleetStats` — never `.collect()`/`.take()`. Explicit `{ read: 0, unread: 0 }` when no receipts exist (measurement-integrity). TDD: `convex/__tests__/stats.fleetStats.test.ts` extended to 5/5 green (T4 exact 2-read/3-unread split across 2 messages + 5 receipts; T4b explicit-0 empty case), all 3 prior assertions unchanged and still green. `scripts/vp-fleet-stats.mjs` prints the new `messages` block.
- **feat(mcp): tool registration authorization is now REQUIRED by the compiler, not by the author remembering — all 123 registrations migrated through a mandatory-scope wrapper** (mission `vp-multitenant-zero-hole-v1`, S2). The raw `server.tool(...)` API let a tool ship without any caller-scope check; a forgotten guard was a silent cross-tenant hole (re-measured on `main` post-#1128: 10 true holes + 2 unscoped OKF export/import). New `defineTool(server, ctx, scope, name, description, schema, annotations?, handler)` in `mcp-server/src/registerTool.ts` promotes `scope: ToolScope` to a **required** positional — omitting it, an unknown `kind`, or `public` without a `reason` all fail `tsc` (proven by `@ts-expect-error` type-cases gated by `mcp-server/tsconfig.typecheck-scope.json`, negative control flips them to `TS2578`). There is **no permissive default**: the open case is an explicit, grep-able `{ kind: "public", reason }`. The wrapper APPLIES the shared identity layer (`checkNamespaceRead/Write`, `checkFromAllowed`, `isMasterScope` from `auth.ts` → `@vantageos/cloud-identity`), never re-authors it. **All 123 registrations** (117 `tools.ts` + 6 `tools/*.ts`) flow through it; the 106 already-guarded tools keep byte-identical handler bodies (behavior-preserving — the pre-check duplicates a gate the handler still runs), and 10 previously-unguarded tools each received an explicit scope decision (6 intended tightenings on fleet-internal ops: `billing_summary_by_project`, `link_commit_to_issue`, `verify_issue`, `improvisation_digest` → master; `create_task_dependency`, `update_summary` → from; plus `export_okf_bundle`→read, `import_okf_bundle`→write). Source-parsing tripwire tests updated to be registration-shape-agnostic. Full suite 2614 passed / 0 failed / 12 skipped; `tsc --noEmit` 0; `npm run typecheck:scope` 0; biome 0 errors.

### Fixed
- **fix(messaging): `pendingOnYou`/`slaBreached` REMOVED from the `check_messages` envelope — messages are messages, not a task list** (Laurent categorical decision, task `k17c4ejer172fgj9t1h027hswn8bvv4w`: "les messages ce sont des messages, pas de liste de tâches dans un retour check message"). `checkNewMessagesEnvelope` (`convex/messages.ts`) no longer returns `pendingOnYouTotal`/`slaBreachedTotal`/`slaBreachedTop`; the `computePendingOnYou(...)`/`getSlaBreachedTopN(...)` calls and the now-unused `pendingOnYouEntryValidator` were dropped from `messages.ts`. This mechanically removes the `slaBreachedTop.length` crash path on undefined and the fleet-wide context/quota pollution from every 3-min `check_messages` cron tick returning up to 110 pendingOnYou entries. `mcp-server/src/tools.ts` `check_messages` handler no longer destructures or renders the `pendingOnYou`/SLA-breached block. `staleInProgress` (Day 130, separate feature) is UNCHANGED in both the envelope and the tool. The lib functions (`computePendingOnYou`, `getSlaBreachedTopN`, `isDormant`, `DORMANT_TAGS`, etc. in `convex/lib/taskClosureGate.ts`) are left intact, unwired — a future dedicated opt-in `list_pending_on_me` tool may reuse them. TDD: `convex/__tests__/pendingOnYou.test.ts` rewritten — new regression guard asserts the envelope's keys are exactly `messages`/`truncated`/`nextSince`/`staleInProgress` with no pendingOnYou/slaBreached property; `computePendingOnYou` retained as lib-level tests (POS/NEG authority, SLA-age, dormant-tag exclusion) called directly, not through the envelope. `tsc --noEmit` 0 in both `convex/` and `mcp-server/`.
- **fix(errorMonitor): filter-rule duplication prevented + the admin mutations hardened to internal-only** — the prod `errorMonitorFilterRules` table had accumulated **11 identical active rules** (`functionName:"*"`, `(Uncaught ConvexError|ArgumentValidationError)`, `skip`, priority 100) from repeated `addFilterRule` calls, which insert unconditionally (`seedDefaultRules` was already empty-guarded, so it was NOT the source). Two fixes: **(1) `addFilterRule` is now idempotent** — before insert it checks (bounded `.take(500)` on `by_active`) for an existing ACTIVE rule with the same `(functionName, errorMessageRegex, regexFlags, severity)` tuple and returns that row's `_id` instead of inserting a duplicate; new `dedupeFilterRules` internalMutation collapses an existing duplicate group to its oldest row (pages `DEDUPE_PAGE_SIZE=500` up to `DEDUPE_MAX_ROWS=5000`, warns + stops on cap, idempotent) — the authoritative cleanup for the prod bloat. **(2) Security: `addFilterRule` and `disableFilterRule` converted from public `mutation` to `internalMutation`** — they were a public, unauthenticated suppression surface (any client could insert a broad `"*"` skip rule and silence ALL GitHub-issue creation, DoS-adjacent), the exact gap `setPendingAliasReleases` was hardened for (Eta delta-review PR #530). A repo-wide grep confirmed no live public caller (mcp-server/src + convex, only test code) — admin/runtime usage via `npx convex run … --prod` (deploy key) still works on `internalMutation`. TDD: idempotency (identical tuple → one active row, same `_id`; distinct tuples still insert) + dedupe (11 identical → 1 survives + second-run no-op + distinct untouched) with RED-before-GREEN (pre-fix, two identical adds create two rows); `convex/errorMonitorFilters.test.ts` 58/58, `tsc --noEmit` 0. convex-reviewer BLOCK (public-mutation surface) → fixed → re-review SHIP. Task `k177wm2b0tw06gvnfvrmxvgbjd8bvkmn`.
- **fix(security, #1134 follow-up): okfBundle IMPORT dedup is now tenant-scoped — closing a cross-tenant visibility side-channel** — `_findBriefingByTitleAndContent` and `_findTaskByTitleAndDescription` in `convex/okfBundle.ts` paginated `by_topic` / `by_status` with NO tenant scope, so an import from tenant A whose title+content matched a row owned by tenant B was skipped as a "duplicate" — the presence/absence of that skip leaked the existence of a title+content in another tenant. Pre-existing (the #1134 PR only bounded the read); named by the multi-tenant convex-reviewer. Both helpers now scope to the caller's `orgId` via `.withIndex("by_orgId", q => q.eq("orgId", expectedOrgIdForNamespace(namespace)))` + `matchesNamespaceScope()` post-filter — the exact pattern the export path (`_fetchBriefingNotesForBundle`/`_fetchTasksForBundle`) and `_findMemoryByContent` already use, reusing the existing `by_orgId` index (no schema change). Callers in `convex/okfBundleNode.ts` thread `namespace` through; `_insertImportedBriefing`/`_insertImportedTask` now write `orgId` (same derivation) so a same-tenant re-import still finds its own prior row. The #1134 bounded pagination (BUNDLE_PAGE_SIZE, cursor across executions, fail-closed 4096-hop ceiling) is preserved. TDD: bipolar cross-tenant tests in `convex/__tests__/okfBundleImportDedupBounded.test.ts` — tenant A vs B (briefing + task) and master vs org-scoped each assert cross-tenant lookup → null while same-tenant lookup still finds its own row (dedup not disabled); 9/9 on the file, full okfBundle suite 60/60, `tsc --noEmit` 0. convex-reviewer verdict SHIP-WITH-NITS (0 blocker/major; minors — fail-closed hardening of `expectedOrgIdForNamespace`, an insert→dedup roundtrip test — tracked as a fast-follow, not merge-gating since the auth layer already closes the practical exploit path). Task `k175emjxz6d1pdb5qjv824m9sd8bs6fj`.
- **fix(#1132): stale pre-rename Convex function-name errors demoted to `log-only` so they stop minting duplicate GitHub issues** — the recurring `Could not find public function for 'X'` Error Monitor noise (issues #1006/#1007/#1008/#1009/#1015/#1017/#1018/#1070/#834, consolidated into #1132) came from 9 stale identifiers (`tasks:listTasks`, `memories:getById`, `memories:list`, `memories:get`, `memories:recall`, `diary:getById`, `licenses:get`, `issues:get`, `missionTemplates:getMissionTemplateByName`). **Diagnosed and named**: the caller-side defect is ALREADY fixed — an exact-match grep across `mcp-server/src` + `convex` found zero live references; current callers use the correct names (`memories:getMemory`, `memories:listMemories`, `issues:getByRepoNumber`, `tasks:list`, …), all of which exist. The residual noise now originates from old-deployed / external clients calling the pre-rename mappings, which our code cannot fix. Remaining action (#1132 item 3): a new `DEFAULT_FILTER_RULES` entry in `convex/errorMonitorFilters.ts` with `functionName: "*"` and an `errorMessageRegex` that matches ONLY those 9 exact names (each delimited by the literal single-quotes in the error string), `severity: "log-only"`, `priority: 80`. The regex is anchored so a genuinely-new missing function (any other name) and a prefix near-miss (`memories:getFoo`) still return `create-issue` — proven by negative-control tests. Additive only: `evaluateFilter` semantics and existing rules unchanged. TDD: `convex/errorMonitorFilters.test.ts` 52/52 (all 9 names → log-only + negative controls); `tsc --noEmit` 0. convex-reviewer verdict SHIP (0 blocker/major).
- **chore(codegen): `convex/_generated/api.d.ts` regenerated to match our own toolchain + a `check:generated` guardrail so it can never drift silently again** — the committed file was stale against the Convex CLI we actually run (1.42.1): it carried 401 lines of the OLD verbose inline `components: { rag: { chunks: {…} }, agentEngine: {…} }` form, while the current CLI emits the compact `rag: import("@convex-dev/rag/_generated/component.js").ComponentApi<"rag">` form, and it was missing the `okfBundleDurable` module entry entirely (a function file added to `convex/` without re-running codegen). **Named, not excused**: this is our code, not a "pre-existing" third-party artifact — regenerated via `npx convex codegen` (only `api.d.ts` moved; `server.*`/`dataModel.d.ts`/`api.js`/`ai/` were already current), `tsc --noEmit` 0 errors. Guardrail: new `check:generated` script (`convex codegen && git diff --exit-code convex/_generated`) proven RED (stale HEAD → exit 1, 405-line diff) then GREEN (regenerated → exit 0); new `.github/workflows/check-generated.yml` with a **secret-free hard gate** (module-coverage check using `find convex -name '*.ts' -not -path 'convex/_generated/*'` — a RECURSIVE glob, so it also catches missing NESTED modules under `convex/lib/`, `convex/migrations/`, `convex/_helpers/`, not just top-level ones — asserting every module has a matching `api.d.ts` import path; catches the missing-module class like `okfBundleDurable` with no network) plus a full `convex codegen` drift-check gated on an optional `CONVEX_DEPLOY_KEY_DEV` secret (soft-skips with a `::warning::` until provisioned, because codegen requires a live deployment — proven not offline-capable: `env -i` fails with "No CONVEX_DEPLOYMENT set"). Root cause of the drift class: codegen output is toolchain-version- and module-list-dependent, so any hand-committed `_generated` goes stale the moment the CLI bumps or a module is added anywhere in the tree, incl. nested dirs; the guardrail makes that drift fail a check instead of riding silently in `git status`. Fix follow-up (convex-reviewer BLOCKER on PR #1138): the original module-coverage glob was `convex/*.ts` (non-recursive) and silently passed on a missing nested module — proven RED (hiding `lib_auth`'s api.d.ts entry → exit 1) and GREEN (current api.d.ts → exit 0) after the recursive fix. Task `k172w7y84jaan5xvymsnyjrj0d8bs8kn`.
- **fix(#1135): `issueStats.calculateStats` no longer crashes on a network failure — the GitHub issues `fetch()` is wrapped, turning an uncaught "fetch failed" into a logged, graceful per-repo skip** — the issues `fetch()` (`convex/issueStats.ts:42`) + its `.json()` parse (`:60`) were unguarded: the code checked `issuesResp.ok` (which only handles a *completed* HTTP-error response) but a THROWN network failure (DNS/ECONNREFUSED/timeout/TLS) or a malformed-body `.json()` propagated as an uncaught "fetch failed" and crashed the `internalAction`, filed by the Error Monitor as a bug. Now both are inside one try/catch → on throw, `console.error(...)` + `return null`, exactly matching the existing `!issuesResp.ok` posture (which already returns null), so `calculateAllRepos` (which loops over repos and does not inspect the return) degrades gracefully per-repo instead of aborting the whole cron. Consistent with the already-wrapped comments fetch (`:88`). No behavior change on the success or `!ok` paths — the fix only ADDS the thrown-error branch. TDD: new `convex/issueStats.test.ts` (2 tests: a rejecting `fetch` and a throwing `.json()`) — RED on the old code (uncaught rejection), GREEN after (returns null); full suite 2828 passed. `tsc --noEmit` 0. convex-reviewer verdict SHIP-WITH-NITS (no blocker/major; nits: observability parity on the pre-existing comments-catch, a regression test for the `!ok` path — both non-blocking). Mission `k576nbe2pwv5xfzd4jbpr881p58bqf6q`.
- **fix(okfBundle): the IMPORT dedup helpers now read in bounded pages instead of an unbounded `.collect()` — closing the 16MB single-execution risk (#1134)**. `convex/okfBundle.ts` `_findMemoryByContent`/`_findBriefingByTitleAndContent`/`_findTaskByTitleAndDescription` each ran `ctx.db.query(...).withIndex(...).collect()` over a whole namespace/table, then `.find()` in memory — on a large corpus this exceeds Convex's 16 777 216-byte single-execution read ceiling and crashes `import_okf_bundle` before any insert. (The EXPORT path was already paginated in a prior PR and is untouched; the code comment at `okfBundle.ts:421` had explicitly deferred this "index optimisation … to a separate PR" — this is that PR.) The three helpers now take `paginationOpts` and return `{ id, isDone, continueCursor }` via `.paginate()` bounded to `BUNDLE_PAGE_SIZE=256`/call (explicit `returns: DEDUP_RESULT_VALIDATOR`); a new `findExistingIdByPaginating()` in `convex/okfBundleNode.ts` drives the cursor loop **across separate function invocations** and short-circuits on first match — the key point being that the 16MB ceiling is cumulative *within one execution*, so looping `.paginate()` inside a single handler would not have fixed it, only splitting into separate bounded executions does. Dedup semantics preserved exactly (memory: namespace+isLatest+content; briefing: title+content; task: title+description); no cross-namespace leakage; no schema change (reuses `by_namespace`/`by_topic`/`by_status`). TDD: new `convex/__tests__/okfBundleImportDedupBounded.test.ts` 6/6 — RED via `git stash` shows the old code rejects `paginationOpts` outright (`Validator error: Unexpected field paginationOpts`), i.e. it had no bounding mechanism at all; GREEN caps each read regardless of table size. Full suite 2824/2918 (delta = exactly the 6 new tests; the 52 pre-existing failures are unrelated `resources/vantage-peers-dashboard` fixture-path tests, identical both sides); `tsc --noEmit` clean on touched files. **Named, not glossed**: `convex-test` does not enforce the real 16MB runtime ceiling, so RED proves the absence of any bound rather than reproducing a production crash — the literal live proof belongs to the DEV-deploy activation read. VP mission `k57952cv6t1qm9r6ayd3bxt4rh8bpb93`.
- **repair(deploy): the `clerk-jwt-smoke` guard is REPAIRED, not retired — its missing script now exists and its intention runs post-deploy as the G3 activation read** (Day 142, branch `sigma/retire-clerk-jwt-smoke-hook-day142`). Doctrine correction, Laurent verbatim: *"on ne coupe pas, on corrige"* — a guard carries a real lesson (a Clerk issuer/audience misconfig silently breaks every tenant's MCP access at once); the defect is fixed, the intention kept. Three defects, three repairs, none a retirement: **(1)** the demanded artifact producer `mcp-server/scripts/clerk-jwt-smoke.js` **never existed** — it is now **created** as a live bipolar G3 verifier: a headless Clerk JWT is minted with no browser via the sign-in-token *ticket* strategy (`@clerk/backend` `signInTokens.createSignInToken` → FAPI `/v1/client/sign_ins?strategy=ticket` → FAPI `.../tokens/convex`, feasibility proven real against `@clerk/backend`), then exercises `convex/lib/auth.ts` `withOrgScope` on the deployed env — **pole A** (no-org non-service identity) must be refused `RBAC_DENIED`, **pole B** (the `CLERK_SERVICE_ACCOUNT_USER_ID` subject) must be granted master; `passed` is true only when both poles behave AND issuer/audience parity holds. It **fails loud** (`could not measure … EXIT=1`) on any missing precondition — "could not measure" never looks like "clean" — and never fabricates a pass. **(2)** the broken trigger shape (`--prod` is not a real convex flag) is not re-registered as a PreToolUse gate — a pre-deploy hook structurally *cannot* prove a deployed env — so the guard's intention is **relocated post-deploy** into the `deploy-vp` skill, where it runs as the G3 activation read at each pole against the just-deployed deployment. **(3)** the abandoned archive (`.claude/hooks/archived/enforce-clerk-jwt-smoke-prod.py.retired` + its test) is **removed** — the retirement narrative is superseded. The dev G3-green (both poles, proven with a disposable self-consistent service-account, Pi-authorized) is the evidence required before the prod `# pi-authorized` token is issued, so the guard stays enforced structurally at the prod gate. Measured finding filed separately (`k178d47xfbp1jensh984jbay1n8b6ad7`): `CLERK_SERVICE_ACCOUNT_USER_ID` is provisioned on no reachable env (`.env.local`, dev Convex) despite the service-account contract (commit `a520565`) — a Verification≠Activation gap that blocks the prod activation, not the door logic. Pi task `k170bvdtz33rdwwzn08y9e2k958b6q91`; QA on the script: biome 0, tsc 0.
- **fix(authz): ownership derived from the target row, not the caller's claim — five guards closed, one class named** — the S0 cross-tenant isolation campaign found `convex/tasks.ts` `update`/`complete`/`start` wrapped their entire RBAC check in `if (args.callerOrchestrator !== undefined)`: omitting the argument skipped verification instead of failing it, letting any caller mutate another tenant's task by simply not passing an identity. `convex/businessUnits.ts` `update` had no ownership check at all — the MCP tool checked the *new value* being written against the caller's own OAuth allowlist, never against the row's actual owner, so supplying one's own identity was enough to rewrite anyone's business unit. Fixed at the resource-derived layer (a shared `assertTaskCallerAuthorized` helper for the three task mutations, plus `deleteTask`, `businessUnits.update` checked against `bu.orchestratorId`) and mirrored at the MCP boundary by making `callerOrchestrator` required on `update_task`, `complete_task`, `start_task`, `delete_task`, `block_task`, `add_task_dependency`, `update_bu`. Class sweep (`grep` for the same shape across `convex/*.ts`, positive control confirmed) found the identical pattern already live in `briefingNotes.deleteBriefingNote`, `diary.deleteDiary`, and `messages.deleteMessage` (wired to the live `delete_message` tool) — all three closed in the same delivery. One site reviewed and left as-is by design: `tasks.ts` `bulkComplete`'s conditional is only reachable on the read-only dry-run preview path, live writes already require `callerOrchestrator` via a pre-existing gate. **Second assertion held throughout**: creator/assignee/owner and `"system"` still pass unconditionally — regression-proofed in the same suite as the exploit tests, not asserted separately. **RED before GREEN**: reverting the fix on `tasks.ts`+`businessUnits.ts` turns 7/13 red (`update_task`/`start_task`/`deleteTask` mutate the other tenant's row; `businessUnits.update` rewrites it by supplying one's own identity); restoring returns 19/19 green. Bipolar probe on every fixed site: neutralize → grep-confirm the mutation landed → rerun red → restore → `diff` byte-identical to pre-neutralization. 2570 passed / 42 skipped, `tsc` 0 in both packages (one pre-existing unrelated failure confirmed present on `origin/main` before this change). Tasks `k17b9z5yjgd8301r6dfawefpzs8b3a03`, `k174pncsyc3ch7wmm7r0zp3ac58b2nye`.
- **test(updatedSince): the re-bound to `limit` on the widened branch now carries its own test — a named risk must arrive with its test, not after it** — the entry below widens the fetch before filtering, then re-bounds the page with `filtered.slice(0, limit)`. A reviewer's mutation **deleted that re-bound and the entire suite stayed green** — 2545 tests, not one red. A caller asking for `limit: 10` alongside `updatedSince` would have received up to 2001 rows, and nothing would have said so. The shipped code was correct; **its protection did not exist**, which is the same defect one level up: a guarantee nobody can lose noisily is a guarantee only until someone edits the line. Eight tests, one pair per site — with a matching population larger than `limit` the result must contain **exactly** `limit` rows, not "at most"; with a population smaller than `limit` it must return the whole population, because a handler that always returned exactly `limit` rows would otherwise carry the same green. **Bite proven per site, never inferred from a shared code shape**: `slice(0, limit)` deleted at `tasks.ts:559`, `tasks.ts:1207`, `missions.ts:325` and `briefingNotes.ts:172` in turn, each red naming its own site (`expected 18 to be 10`), each restoration leaving an empty diff. No production code changed, no cap value retyped — the bounds derive from the constants the suite already imports. 24/24 on the file, 2553 passed / 42 skipped (2545 + 8, zero regressions), `tsc` 0.
- **fix(mcp-server): the service-account variables the whole authenticated surface depends on are now NAMED where someone reads them before deploying, and the refusal is tested on both poles** — the first successful Railway deploy in seven days made every authenticated MCP call answer `{"error":"Authentication service unavailable"}` (503) while `/health` kept answering `ok`. Cause, confirmed from the build log rather than inferred — the line is verbatim the message the code emits: *"no service-account identity available (CLERK_SECRET_KEY / CLERK_SERVICE_ACCOUNT_USER_ID not configured) — refusing to send an unauthenticated Convex query()"*. **Neither variable had ever been provisioned**, and the deploy did not break anything: it *revealed* a dependency that had been unmet since 2026-07-14 and invisible because nothing shipped. **The fail-closed design is what made it visible** — the client refuses to send an unauthenticated query rather than falling back to anonymous, which `withOrgScope` would have treated as master; a silent anonymous read would have been far worse than a loud 503. Two things are fixed here, and neither is the missing value itself (that is a runtime secret): **(1)** both variables are now named in `.env.example` — the artifact someone reads *before* deploying — because `git grep` found all seven references confined to `mcp-server/src/`, i.e. the dependency existed only inside the code that consumes it, which is invisible until a deploy succeeds; **(2)** the refusal path now carries a test, where `git grep "Authentication service unavailable" -- mcp-server/src` previously matched `auth.ts` alone: **a total authentication outage of the client surface was covered by nothing and discovered in production**. The test asserts **both poles** — absent config refuses *and names the missing variables*, present config does not refuse — because a test that only shows refusal cannot distinguish a selective guard from one that refuses everything. Proven to bite: inverting the guard turns 6 of 8 red, restoring leaves `git status --porcelain` empty. 8/8 on the new suite, 2521 passed / 42 skipped (2513 + 8, zero regressions), `tsc` 0 in both packages. **Named, not glossed**: the Hono handler emitting the literal 503 is still not traversed — the tests stop at `loadConfig` and the proxy refusal, and the file says so at the call site. `/health` answering `ok` is **not** evidence the surface serves anyone: it is unauthenticated and answered throughout the outage.
- **fix(tasks, missions, briefingNotes): `updatedSince` filtered a page that had already been chosen, and returned the remainder as if it were the set** — found by running a paired control on the very instrument that had just been used to audit a closure. `list_tasks assignedTo=<r> status=done updatedSince=<t>` returned three rows; the same call with `status=all` returned **`[]`**, and with an older threshold returned five rows **all in `todo`, none closed** — while `get_task` proved those closed rows carried `updatedAt` far beyond the threshold. **Not an error, not a visible gap: a partial list wearing a complete list's clothes.** Cause is the class this repo closed on billing one PR earlier, one table over: `updatedSince` (and `createdBy`) were applied **in memory after `.take(limit)`**, so the page was chosen by creation order first and filtered second — any row edited recently but created outside that page was invisible. **Why this outranks its size**: it is the query an auditor reaches for to ask *"what moved since?"*, so it fails exactly when someone is checking whether work was recorded — and it did, in this session, on this author's own closures. The class was swept rather than the instance: the same shape was found and fixed in `tasks.list`, `tasks.listByMission`, `missions.list` and `briefingNotes.list`. The fetch now widens to a **named cap** before the filter and is re-bounded to `limit` afterwards. Where the widened scan itself exhausts the cap — the one case that could never be told apart from "complete" under a bare-array return — it now **throws**, naming the cap and how to narrow the query, rather than handing back a page indistinguishable from an answer. **Deliberately not done**: no field was added to the return shape. These four queries return bare arrays, and a `truncated` flag would break every `Array.isArray` consumer including the MCP wrapper — the loud failure was chosen over a shape change that breaks working callers. No new index, no schema change. **RED before GREEN against unmodified code**: 4 of 8 fail beforehand, one per site, with the seed placing the target row *beyond* the first page on purpose — a handful of seeded rows would have passed on the broken handler and proved nothing; the four negative-pole tests (no `updatedSince`, counts unchanged) pass throughout. 2529 passed / 42 skipped (2521 + 8, zero regressions), re-run independently of the author; `tsc` 0 in both packages. **Named, not glossed**: the cap is 2000 per widened fetch, and whether a heavy assignee crosses it in production has NOT been measured — if it does, the result is a loud refusal rather than a wrong answer, which is the intended trade.
- **fix(mcp-server): the deployed lockfile is regenerated the way the builder reads it — isolated from the monorepo parent — and a CI guard now replays that exact install layer** — Railway had failed **10 consecutive deploys** between 2026-07-14 and 2026-07-21 while continuing to serve the 2026-07-14 build: a failed deploy does not stop the running container, so **nothing cried**. Every merged fix looked shipped and reached no caller; the observable symptom was `list_broadcast_status` returning `[]` on a message that provably had recipients — a crash replaced by a silent false statement. Root cause, measured rather than reasoned: the builder is RAILPACK with `rootDirectory=/mcp-server`, and its install layer copies **only** `mcp-server/package.json` + `mcp-server/bun.lock` into an otherwise empty directory, then runs `bun install --frozen-lockfile`. Outside the monorepo bun cannot walk up to the workspace root, so it reads `mcp-server/bun.lock` alone — a file last regenerated 2026-06-04, while `mcp-server/package.json` changed 2026-07-14 in `6153e7a`, **the first of the ten failures, minutes after the last success**. **Why four people measured green and none reproduced it**: every attempt ran inside a full clone, where bun resolves the *root* lockfile — which is healthy. Both bun versions (1.3.11 local, 1.3.14 Railway's) and both directories passed; the variable was never the version nor the directory but the **absence of the parent**. Three hypotheses were disproved before the right one: an unpinned bun version, a Docker layer-cache ordering, and an `installCommand` override — and the file the diagnosis first leaned on, `mcp-server/nixpacks.toml`, turned out to be **dead configuration RAILPACK never reads**; it is deleted here rather than left to mislead the next reader. RED, replayable by a third party in three lines: copy those two files alone into an empty directory, `bun install --frozen-lockfile` → `error: lockfile had changes, but lockfile is frozen`, byte-identical to the build log. GREEN after regenerating the lockfile **from an isolated tree**; the root install is unaffected (`Checked 277 installs across 354 packages (no changes)`). **The class is closed, not just the instance**: the same failure already occurred on 2026-06-04 and was hand-repaired by a commit literally titled *"resync mcp-server/bun.lock to unblock Railway prod build"* — repaired without a guard, so it returned six weeks later. A new CI job replays the isolated install layer on every push with bun pinned to Railway's version, and `/health` now carries the built commit SHA derived from the environment, falling back to the literal `"unknown"` — deliberately not SHA-shaped, because a server that cannot say what it serves must say so rather than invent it. `version: "2.16.0"` had not moved in seven days and discriminated nothing.
- **fix(messages, fixPatterns): a tool wrapper may not inject an argument the backend never declared, nor collapse an object payload into an empty list** — `list_broadcast_status` returned `Server Error` on **every** call, reproduced independently by three people. The issue title blamed the caller's `limit`; the caller was never the culprit. `mcp-server/src/tools.ts` sent `limit: limit ?? 20` on every invocation while `messages:listBroadcastStatus` declared only `{messageId, fields}`, so Convex rejected the extra field — **which is why omitting `limit` at the call site changed nothing: the wrapper supplied it anyway.** Diagnosing from the title would have produced a fix for a parameter nobody passed. **A second defect was hiding behind the crash, and it is the one that would have survived the fix**: the backend returns an object, but the wrapper ran `Array.isArray(status) ? status : []` and the declared output schema was `z.array(...)`. Repairing only the crash would have made the tool return `[]` — *"nobody read this broadcast"* instead of the real receipts — and nothing would have failed. Closing the issue there would have left a silent, permanently-empty function behind a green checkmark. Contract now agrees on all three surfaces: the object envelope is authoritative, `limit` is **declared and actually applied** to `receipts` with an explicit `truncated` flag so a capped list can never render identically to a complete one, and scope filtering acts on the receipts rather than on the envelope. `capListResponseBytes` is deliberately bypassed here: under byte pressure it swaps the payload for a differently-shaped envelope, which is the same disease. **The class sweep found a third live defect the previous partial fix had missed**: `fixPatterns:listByProject` received an undeclared `fields`, crashing the `project` path while the `listAll` path — patched earlier in isolation — kept working. Confirmed by paired control against production: `list_fix_patterns` without `project` returns rows, with `project` returns `Server Error`. Residual on the array-coercion sweep: 0 across the remaining 20 wrapper sites. **RED before GREEN against unmodified code**: 3/5 backend contract tests fail with `Unexpected field 'limit'`, and 5/7 wrapper tests fail on `Array.isArray(parsed)` and a missing `from` — each reproducing its defect exactly. Two pre-existing test files had encoded the stale array contract and were corrected to the real shape. 2509 passed / 42 skipped (+12, zero regressions), `tsc` 0 in both packages. Task `k177g17fxrgpnteqdpnqxvx2rx8arx32`, issue #1105.
- **fix(billing): the period must bound the SCAN, not filter it afterwards — and a negative duration is never summed into an invoice** — `billingSummaryByProject` is the refacturation base: an invoice is built from its output. It returned **zero for a period that demonstrably contained billable work**, so an invoice built on it would have billed nothing. The handler fetched the first 5000 `done` tasks **in index order** and only then compared `completedAt` to the requested window, in memory — so the scan covered the OLDEST rows and never reached recent ones. Proven by paired control against production rather than inferred: a recent window returned `{byProject: [], truncated: true}` while an old window returned 29 projects with rich totals, and a task with `actualMinutes` and a `completedAt` inside the empty window was absent from it. **`truncated: true` was worse than useless here**: it invites the caller to narrow the period and re-query, and narrowing does not move the scan — the flag described a cap that had nothing to do with the period asked for. The period (and `project`, when given) now bounds the QUERY through new compound indexes `by_status_completedAt` and `by_status_project_completedAt`, so a cap is finally a cap *on the period* and `truncated` means what it says. **Second defect, found by reading the output instead of trusting it**: one project returned `totalMinutes: -2377`. A negative billable duration is not a small error, it is an impossible value — corrupt rows were being summed straight into an invoice. Such rows are now **excluded and counted** in a new `invalidDurationTaskCount` rather than clamped to zero, because clamping hides the corruption just as effectively as summing it. **Third**: the MCP layer filtered `project` *after* receiving a truncated scan — the same disease one layer up, where another project's volume could empty the requested one; `project` is now pushed into the query. The query also had **no authorisation check at all** and now carries `withOrgScope`/`filterByOrgScope`, matching the internal-caller convention already used by its siblings. **RED before GREEN against unmodified code**, with the seed deliberately placing the target row *after* the scan cap in index order — a test seeding a handful of rows would have passed on the broken handler and proved nothing: 4 of 6 fail beforehand (`expected undefined to be 3`, `expected -2317 to be 60`, `expected true to be false`, `Unexpected field 'project' in object`). One residual is **named rather than quietly left**: `stats.ts` `orchestratorStats` carries the same shape and is worse — it filters a caller-supplied window in memory after an unindexed `.take()` and exposes no truncation flag at all. 2501 passed / 42 skipped (+4, zero regressions), `tsc` 0 in both packages. Task `k17f0kdkfpa2f0rnmcj5wreqsx8azma1`.
- **fix(tasks, missions, mandates): a query branch that receives a filter it cannot apply now REFUSES — it no longer answers a question nobody asked** — `list_tasks` accepted `project` alongside `assignedTo` and **silently discarded it**, returning the assignee's entire queue while the caller believed they held one repository's. Measured with a paired control rather than inferred: `project` alone filters (`project="<slug>"` → 5/5 rows carry it; a nonexistent slug → `[]`), but `assignedTo=<r> + project=<nonexistent slug>` → **5 rows + a cursor**. The cause is structural, not a typo: the handler is an if/else-if chain that selects ONE index, so the first matching branch wins and every other filter the caller passed is read and then dropped. **A silently wider answer is worse than an empty one — an empty result is visible, a plausible one is believed**, and this defect corrupts any number read through the tool, including numbers that feed a billing decision. Fixed with compound indexes (`by_assignee_project`, `by_instance_project`) so both filters are applied by the QUERY, not by post-filtering the page: narrowing an array after `.take(limit)` would hand back a truncated result wearing a complete result's clothes, trading a silent filter for a lying count. Unsupported combinations now throw, naming the combination. **The class was swept, not just the instance**: the same shape was found and fixed in `missions.list` (`project`+`pilot`) and `mandates.list` (`requestedBy`+`fulfilledBy`); `businessUnits`, `components` and `messages` were checked and are correct; one residual is **named rather than quietly left** — `recurringTasks.list` applies both filters but does so after `take`, sharing the truncation risk, tracked separately. **RED before GREEN, against unmodified code**: the negative-control tests fail 6/9 on the pre-fix handler and the mutation was asserted to have landed (compound-index anchor 2 → 0) before any verdict was read, so the red is behavioural. 2497 passed / 42 skipped (baseline 2484/42, +13, zero regressions), `tsc` 0. Task `k174mpqp5jc42bvy50s5ejgvp18azxw8`.

### Added
- **feat(billing): an instrument that measures the duration distribution, so an aberration threshold is DERIVED rather than typed** — `billingSummaryByProject` excludes negative `actualMinutes` and counts them in `invalidDurationTaskCount`. It has no view at all on aberrant **positive** values, and production carries them: a single task returns **74904 minutes**, another **64734** — weeks of wall-clock for one row, entering an invoice whole. The comment at that exclusion promised *"an impossible value"* while the code tested a **sign**; a promise wider than its check reads as coverage that does not exist. **A negative total is visible; an inflated one looks like work** — which makes the positive half the more dangerous one. Fixing it requires a threshold, and a threshold nobody derived is a number somebody typed, so this ships the measurement first and **corrects nothing**: new read-only `taskDurationDistribution` returns `count`, `percentiles` (p50/p75/p90/p95/p99/max), `negativeCount`, and — on the same population, because the two defects are measured together — `withProjectCount` / `withoutProjectCount`, plus `truncated`. The period (and `project`) bounds the QUERY through the compound indexes shipped and **built** by the previous entry, never a post-hoc filter over an unrelated scan. **`count === 0` does not render as a flat distribution**: all-zero percentiles read exactly like "every task took 0 minutes", which is a false measurement rather than the absence of one, so an explicit sentinel forces the caller to branch on `count` — the same rule this repo has been applying all week, that "I could not measure" and "it is clean" must never produce the same output. 8/8 on the new suite, 2521 passed / 42 skipped (2513 + 8, zero regressions, re-run independently of the author), `tsc` 0. **Stated rather than implied**: this is verified against synthetic fixtures only. The instrument is not proven until it reproduces a known production fact — the window whose billing summary shows one task at 74904 minutes must return a `max` at least that large — and that check belongs to activation, not to this merge.
- **feat(billing-gate): the billable-project list is written by ARGUMENT, so a client name can never enter this public repository** — the `taskClosureConfig` row `key="billableProjects"` decides whether the closure gate applies to a task, and its only write path was an args-less migration carrying a hardcoded array. Adding a billable client therefore meant editing source in a **public** repo — putting a client name into public git history permanently, which the purge work exists to prevent. The measured consequence was live: a second billable slug carried real client work, was absent from the row, and its tasks could be closed with no machine `startedAt` — silently, with billing derived from nothing. **The list is data, so it is now written as data**: new `setBillableProjects` internalMutation takes `projects` as a runtime argument, upserts the row idempotently, and returns `{previous, current}` — the caller proves what changed without a second read, so the return value **is** the evidence artifact rather than a claim about it. It **rejects an empty array** (`SET_BILLABLE_PROJECTS_EMPTY`): writing `[]` would make every project read as non-billable and silently disable the entire gate — the exact failure this feature exists to prevent, so it refuses loudly instead of quietly neutering itself. Reachable only via `convex run` with deployment credentials; it is an `internalMutation`, absent from the MCP tool surface and from every public Convex API, so no consumer contract moves. 20/20 on the gate + new suites, `tsc` 0, and `grep -rniE '<client>' convex/` returns **empty** — the tree is proven clean, not assumed clean; tests use fictitious slugs only. **Scope boundary, stated rather than implied**: this ships the mechanism, not a policy change — which projects are billable is a data write performed after deploy, and that write is what actually closes the hole. Task `k175xm4386dsexe3mz4g30nxxx8azr7a`.

### Fixed
- **fix(error-monitor): a probe is not an incident, and an unmeasured recurrence is not a recurrence** — issue #1088 was manufactured out of an operator's own smoke traffic during a release, and shipped titled `[RECURRING 24h+ — root cause not fixed]` on an error that was one second old and had never occurred before; it then spawned a 14-task mission containing a production-release task. **The dangerous half was the label, not the false alarm.** The recurrence tag was *inferred from group identity, never counted*: the group key deliberately collapses tail variance in order to dedup, so a fresh error could collide with a stale attested row and inherit its age. A severity label that lies makes a team drop everything for a ghost. `convex/errorMonitorRecurrence.ts` (new) now emits the escalation **only on a counted history** — occurrences tallied since the row was armed, against an attested prior issue — and files a **normal** issue when history cannot be established. It never files an escalation it cannot substantiate. Operator traffic inside a *declared* release window no longer manufactures incidents (`convex/errorMonitorDeployWindow.ts`, new; gated on a Convex env var, settable only via dashboard/CLI and unreachable from any function argument). **Both guards proven by mutation, not asserted**: restoring the old inferred logic reddens 3 tests including the #1088 integration repro; forcing the window guard to `false` reddens 1; both mutations compile (`tsc` 0 errors), so the reds are behavioural and not syntactic. Restored: 87/87 on the monitor suites, **788/788** across `convex/`. **Not blinded** — a genuinely fresh error still files a normal issue on threshold, and a genuinely re-measured recurrence still gets the RECURRING label; a monitor that smothers real alerts gets switched off, and a switched-off monitor watches nothing. **Known limitation, written in the code rather than papered over** (`errorMonitorDeployWindow.ts`): an *undeclared* operator probe remains indistinguishable from a real user hitting the same broken function — Convex log-stream entries carry no caller identity at all. No caller-supplied field was invented to close that gap; a field the caller fills in is a claim, not a fact. Task `k178cr9x6beq9zrvdmzmcjnmcd8afwc5`.

### Added
- **feat(messages): a live state is no longer typed into a message — it is REFERENCED, and resolved at send time** — twice in one day a peer asserted a state that had already changed: a PR reported `OPEN` when it was already `MERGED`, a package reported at one version when the sender had himself published the next one. The state is read at the START of writing, the message is long, and it leaves AFTER the world moved. **A hand-typed state is a lie on a delay.** The fix is not "read more carefully" — that holds for a week; it is the same fix already applied to hand-typed counts: the value no longer gets WRITTEN, it gets RESOLVED. `send_message` now takes reference tokens the SERVER resolves at the instant of sending (`{{pr:owner/repo#N}}`, `{{npm:pkg@tag}}`, `{{task:id}}`), and the delivered body carries the real value **and** the resolution timestamp — a proof with no date is a proof that expires in silence. The author cannot type the value, so the author cannot type it wrong. **Fail-closed**: if GitHub or the registry is unreachable, or the artifact does not exist, resolution throws and the message does not leave; it never falls back on the typed value, a cache, or an empty string. **Content proofs are deliberately untouched**: a test ratio, a diff, a re-read SHA, a verbatim quote do not expire — only the state of a LIVE artifact does, and a guard that confuses the two blocks legitimate work and gets routed around. Messages carrying no token pass through byte-identical (full backward compatibility). **Bipolar probe, all three poles, 22/22**: MUST_RESOLVE 4/4, MUST_PASS 7/7 with **false positives = 0**, MUST_FAIL_LOUD 7/7, plus two replays of the real incidents. **Proven by mutation**: short-circuiting the resolver to return the typed content reddens 13 of 22 — while the MUST_PASS seven correctly stay green, which is what proves the probe has two poles and not one; the mutation compiles (`tsc` 0 errors), so the red is behavioural, not syntactic. 1650/1662 across `mcp-server/`. Layer 2 (`artifacts/enforce-fresh-state-in-messages.py`) re-reads state typed in free prose and refuses a send that contradicts reality, citing BOTH values; it ships as an artifact and is **not installed** — hook files are Pi's single-writer domain. Its network fallback differs from Layer 1 on purpose, and the reason is written in the file: Layer 1 fails closed because the author explicitly asked for a live value; Layer 2 warns loudly and allows, because blocking every message when a hook's network call fails would manufacture the very outage it exists to prevent. Both are loud; neither is silent. Task `k17d8avestmyegtv1fntb9py418ad166`.

### Added
- **feat(infra): a worktree reaper that trusts the PR, not git ancestry — and deletes nothing it cannot vouch for** — Day 128 the VPS disk hit 100%; nothing removed finished worktrees and one was 827 MB by itself. The task said "repair the reaper": there was none, and saying so beat shipping a fix to a thing that does not exist. **The reflex check is the broken one.** `git branch --merged main` answers "no" for every SQUASH-merged branch, because a squash rewrites the commit and the original SHA is never an ancestor of main — measured here on 4 worktrees, two of them belonging to demonstrably MERGED PRs, and ancestry reported "not merged" for all four. A reaper built on that signal reaps **nothing, forever, while reporting that it looked**: the disk fills and the sensor says it is fine. The signal is therefore the **PR state**, the thing that actually decides whether the work landed; ancestry is a proxy, and the proxy is wrong precisely where we merge. **The safe default has a direction**: this tool DELETES, so its safe failure is to delete NOTHING — every uncertainty resolves to KEEP (PR state unresolvable, dirty tree, unpushed commits, the worktree we are running in). *"I could not tell whether this landed"* and *"this is safe to delete"* must never produce the same action; an unpushed commit lives in exactly one place on earth. Dry run is the default. Proven on the real machine: reaped the 826.9 MB merged worktree, kept the one with an OPEN PR, kept the one we run in, and kept two dirty trees — **one of which was MERGED and kept anyway**. **The dirt was not random**: those trees are dirty because of the TRACKED `.pyc` files that PR #1092 removes — versioned, so every test run rewrites them, so those trees are *permanently* dirty, so a reaper would never have collected them even once it existed. The published bytecode did not only leak; it also jammed the cleanup meant to keep the disk alive. One defect, two bills. `scripts/reap_worktrees.py`. Task `k17f5bq0xv2tkqzphzsvwsz9p98abkqm`.
- **fix(mcp-auth): the identity module was written, tested, and never called — wired, and the wire is proven** — the service-account replacement for the MCP↔backend shared secret existed with 5 green tests, and **nothing invoked it**: `git grep`, comments excluded, found zero call sites. `buildInternalClient()` returned a bare client with **no `setAuth`, no identity at all** — and on the backend, no identity means `withOrgScope` sets `isMaster=true`, i.e. *"all data returned unfiltered"*, **across tenants**. The branch as authored **removed the shared secret and wired nothing in its place**: merged, every MCP call would have become an unfiltered master call. That is not a security fix; it is the breach wearing the fix's clothes. **A module that is perfectly tested and never invoked is indistinguishable from a module that does not exist — except that it deleted the thing it was meant to replace.** The comment claimed *"calls .setAuth() … see server.ts"*; neither file changed — plumbing described but never built. Now wired at `mcp-server/src/auth.ts:107`. **And a second breach was hiding inside the fix itself**: the client fell back to `.clearAuth()` when the credential could not be minted, proceeding **unauthenticated and silently** on any auth failure — the same fail-open, one layer down, inside the thing built to close it. It now **throws before anything reaches the wire**: *"I could not authenticate"* and *"I am the system"* must never produce the same call. **Proven by mutation, and the mutation compiles** — which is the only reason the red means anything: unwiring the identity leaves `tsc` at 0 errors and turns one test red (`expected "setAuth" to be called 1 times, but got 0 times`); restored, 2/2 green. The first attempt at that mutation did **not** compile, so its red proved nothing — a red from a compile error is a coin flip read as a result. The test asserts the **outgoing call carries the identity**, not that the identity function exists: testing existence is precisely the mistake that produced the hole. 1635/1647 across `mcp-server/`, `tsc` 0. Task `k173crnt1wwqar3j0qe0ppqjzs8abwmq`.

### Added
- **feat(tenant): create the address before moving anyone into it — the org had no machine identity at all** — SEC-PURGE T-b1 migrates *nothing*; it creates the thing a migration needs and does not have. **The client's name is not a label stuck on their data — it is the ADDRESS the data lives at**: their memories are filed under `project/<org>`, and their three personas recognise each other *only* because they point at that same namespace. Remove the name and you remove the address. Pi's rule is right — the replacement slug is **derived, never invented** — so the machine identity to derive it from was sought, and **it does not exist**: `npx convex data client_org_mapping` → *"There are no documents in this table"*. The table **designed** to carry org identity is **empty in production**. And deriving from an OAuth profile would be worse than doing nothing: **a profile is not an org** — the client has three, so three ids would yield three slugs for one org, three disjoint namespaces, each persona cut off from the other two. **The outage, arrived at by faithfully following the rule.** The client's name is currently the tenant's **de facto primary key** — not an implementation detail, but the structure, and the reason the naive purge cut the client off. So: seed the org row (Convex generates the `_id`; nobody invents it, nobody types it) and derive `tenant-<id[0:12]>`. **Two findings while checking the work, and the second is the point.** (1) The derivation sliced the raw id **with no validation** — but that slug becomes `project/<slug>`, an *address*, and a broken address **does not fail loudly: it silently files the client's data where nobody will look**. Now normalise → slice → **assert the shape**; deriving correctly and *checking* the derivation are two different acts. (2) The test asserted `toBe("tenant-" + id.slice(0,12))` and **passed while producing `tenant-10000;client`** (the harness id alphabet contains `;`) — it did not merely miss the malformed address, **it certified it**, pinning it as the expected answer. *An assertion that restates the implementation cannot catch the implementation being wrong.* It now asserts the property the caller depends on: `/^tenant-[a-z0-9]{12}$/`. **Fail-closed**: no org row → `tenantSlug` throws; never an empty string, never a default — an empty slug would write client data to the root. **Proven by mutation, and the mutation compiles** (`tsc` 0, so the red is behavioural): hardcode the slug and the test that reddens is *"seeding two distinct orgs produces two distinct addresses"* — exactly the catastrophic case, **two clients sharing one address, each reading the other's data**. The probe catches the worst thing that can happen, not merely the likeliest. **Idempotence is the decisive test**: replaying the seed creates no second row and does not change the `_id` — **an id that moves is an address that moves**, a silent client outage the day someone re-runs the seed. 791/791 across `convex/`, `tsc` 0. Zero writes to memories, namespaces or OAuth profiles. Task `k17dm4pz7q5mrsd9qkypgfzj8n8ah8pq`.
- **feat(guard): a branch name is publication — a git ref carrying a client identity is refused at push** — Day 128, four branches named after clients reached the PUBLIC repo. The code inside them was clean. **The leak was the NAME**: visible in `git ls-remote`, in the PR title, in every notification e-mail, in the branch dropdown, to anyone on earth. They were renamed by hand, and hands do not scale — the next one is always the one nobody remembered to look at. A ref is the **third surface**, after file contents and file names: it lives in `.git`, never appears in a diff, is never scanned, survives every content purge ever run — and it publishes itself the instant you type `git push`. The commit is not the publication event; the push is, hence pre-push. Vocabulary resolves from the same host config the leak guard uses (nothing hardcoded); unresolvable → **exit 2, refuse to judge**, because a machine with no host config is exactly where an unreviewed branch gets created and *"I could not check"* must never be waved through as *"this is clean"*. **Bipolar, 17/17**: MUST_BLOCK through every separator a ref can be built from (`-`, `_`, `/`, `.`, any case — `\b` alone is blind to the underscore, and a `[\s\-_]` class is blind to the dot; both holes closed and pinned), MUST_PASS on `fix/summaries-pagination`, `chore/client-side-rendering`, `feat/guineapig-77-example`, `DEPLOY_KEY_GUINEAPIG` and every branch pushed today — **zero false positives**, because a guard that refuses ordinary branch names is uninstalled within the day and then guards nothing. It never quotes the identity it finds: reporting a leak by reproducing it republishes it. **THE FINDING THAT MATTERS MORE THAN THE GUARD** — probed against the four branch names that *actually* leaked on Day 128, **two of the four pass**. Not because the guard is wrong: because **the vocabulary is incomplete**. One client first name that demonstrably reached public refs is absent from the host config, and the config carries FULL names while the leaked refs used the FIRST NAME ALONE — a two-word pattern cannot match a one-word ref. This is exactly the failure the config was created to fix: a real, active client identity missing from the list, so the guard prints PASSED over the very name it exists to catch. We moved the list out of the code and never finished filling it. **Deriving the source is worthless if the source has holes**, and every PASSED verdict the leak guard produced today is therefore *weaker than it looked* — not wrong, weaker: it checked what it knew, and it did not know enough. The registry is **not** edited unilaterally (an over-broad token manufactures false positives and gets the guard uninstalled; a missing one lets a leak through — that trade-off is the operator's call). `scripts/guard_git_refs.py`; pre-push hook shipped as an artifact, **not installed**. Task `k177543196q8g6n9ddgp3d6xr58abzsb`.

### Added
- **feat(plugin): the packaged plugin is now provably byte-exact with its VantageRegistry canonical, and can never ship client data — the SILENCE CONTRACT finally reaches what executes** — third attempt at the Day 127 fix, and the first that lands. **Why it kept failing**: the contract was written, reviewed and committed into the *workspace* skill, while the file that actually RUNS is a copy frozen at packaging time. Presence, name and location prove nothing; only execution proves. Measured, not assumed: of 53 `check-messages/SKILL.md` copies on the host, the 3 loaded at runtime carried the contract (hand-patched — a bandaid), and **all 5 PLUGIN SOURCES — the ones that get packaged — did not**. The next plugin build would have overwritten the patched copies with the holed version and resurrected the loop. `plugin/skills/check-messages/SKILL.md` now carries it (origin/main: **0** occurrences → **2**), pulled from the VR canonical rather than hand-typed. **The gate** (`scripts/vr_plugin_parity.py`, `.github/workflows/plugin-vr-parity.yml`): every packaged skill/hook is compared to its VR canonical and CI FAILS on divergence. Coverage is derived from the **artifact** (the packaged directories), never a hand-maintained list — a hand list is the same disease one level up. No silent third state: each item is CHECKED, SKIPPED-with-a-written-reason (`session-end.py` — confirmed live to have no VR canonical), or PARITY_SUSPENDED. If VR is unreachable, a name is absent, or a response is malformed, the gate FAILS and names what it could not resolve — a guard that cannot reach its source of truth must never report success. Zero packaged items found = broken parser, not a clean repo → FAIL. **The leak guard** (`scripts/leak_guard.py`) and the design correction that matters: a parity gate alone would have **enforced a leak**. The VR canonical of `session-start` embedded a client directory — client orgs *and* their contact persons — and this plugin is PUBLIC and published; under pure parity, *removing* a client name would have counted as divergence → CI red forever, compelling the very leak it should prevent. So secrecy now **outranks** parity: client data in a canonical suspends parity for that item and fails the build loudly (naming the file and the remedy) instead of syncing it. The VR canonical was then purged — identity is now DERIVED from a host-side config file outside every repo and package, degrading loudly rather than silently mis-identifying. Internal identifiers already public on `main` (operator home path, instance ids) are TRACKED with file+line and burn down; they do **not** fail the build, because failing on day-one pre-existing state means the gate can never go green and someone disables it — and a disabled guard guards nothing. Word-boundary matching, not substring: `summaries`, `client-side`, `client delivery` are proven clean (a prior purge renamed "summaries" because it contains "marie"). **Bite-proved, each mutation asserted LANDED via grep before any verdict was read** (a probe that does not assert its own mutation landed is a coin flip read as a result): packaged file diverging → `GATE FAILED (DIVERGED)` naming it; client data injected → `CLIENT DATA — HARD BLOCK`; a NEW internal identifier → `FAILED — the baseline only burns down, never grows`; VR unreachable → FAIL. Each restored, zero residue. Final: leak guard PASSED, parity **11 match / 1 skipped-with-reason / 0 diverged**. Task `k17at7kta2ty5dy1tkytzp1s318acacw`.
- **feat(tasks, messages): task-closure discipline enforced server-side — billable closures require a real `startedAt`, stale `in_progress` surfaced on every check, billing consolidated from machine timestamps** — the enforcement lives in the backend, the canonical layer, not in local hooks (Day 128 fragmentation lesson). Billing derives from machine timestamps, never a hand-typed line (`derive-never-type`: do not type what the machine can read). **The gate**: `convex/lib/taskClosureGate.ts` (new) — closing a task whose `project` is billable REJECTS when `startedAt` is absent, with an actionable `TASK_NEVER_STARTED_BILLABLE` error (the task was never really started via `start_task`, so `actualMinutes` is uncomputable and billing would be false). When `startedAt` is present, `actualMinutes` is computed from `startedAt → completedAt`. A manual time breakdown in the `completionNote` is **optional** and is never parsed. Structured override: `// allow-no-time-line: <reason>` (≥6 chars). **Every closure path is gated, not just one** — `convex/tasks.ts` `complete` (:615), `update` (:556, the generic `status:"done"` patch) and `bulkComplete` (:1461). Gating only `complete` would have left `bulkComplete` blind, which is precisely the coverage-blind-spot class the fleet closed this week; the internal/cron closers (`createDeployTaskWithDedup`, `resolveStaleDeployTasks`, `closeReviewTasksForPr`, `errorMonitor.resolveStaleIrpMission`, `issueClosedSweepDb.cascadeCloseMission`, `migrations/dedup_stale_deploy_tasks.ts:90` — the last one surfaced by Eta's review, absent from the first inventory) are deliberately **not** gated — they are system bookkeeping, not human-logged billable work, and that exemption is written down here rather than left silent. **Fail-closed, never fail-open**: if the billable-projects config cannot be resolved for a task that carries a `project`, the gate throws `TASK_CLOSURE_CONFIG_UNRESOLVABLE` naming what it could not resolve — "I could not determine it" and "it is clean" must never produce the same green. **Billable projects are config, not a constant** (`no-hardcoded-business-knowledge`): new `taskClosureConfig` table (`convex/schema.ts:950-968`, key/value, mirroring `errorMonitorConfig`), seeded by `convex/migrations/seed_task_closure_config.ts` with `billableProjects: ["vantage-immo"]` and `staleInProgressThresholdMs: 86_400_000`. Adding a client later is a config row, not a code change. **`staleInProgress`**: `convex/messages.ts` `checkNewMessagesEnvelope` (:217) now returns the recipient's tasks left `in_progress` beyond the configured threshold (default 24h) — taskId, title, age — so every orchestrator sees its own overdue work on each check, with no extra cron. `checkNewMessages` (:80) is **deliberately left byte-identical**: it is a frozen contract for vp-mcp <2.12.0 callers, the MCP server calls only the envelope (`mcp-server/src/tools.ts:2776`), so adding a sibling field there would have broken old external clients for zero gain — a regression test now pins its bare-array shape so nobody re-breaks it. **Billing base, actually callable**: new Convex query `tasks:billingSummaryByProject` (sums `actualMinutes` per project over a period, `truncated` flag never swallowed) **and** its MCP tool `billing_summary_by_project` (`mcp-server/src/tools.ts`) — a consolidation query no one can invoke is not delivered (Verification ≠ Activation). **Proof.** **Eta's review caught a real hole in this very proof**: the `update` path was gated but never *proven* to bite — its gate could be deleted whole and **nothing reddened**. Gating three paths while testing only two reproduced, one level down, the exact coverage blind spot this gate exists to close — and `update` is the generic `status:"done"` patch, i.e. the MCP `update_task` call, the closure path most reachable by a human. Tests `(i)`/`(j)`/`(k)` now cover it: Eta's own mutation (`if (false && patch.status === "done")`, landing asserted at `convex/tasks.ts:611` before any verdict was read) reddens 2 of them where it previously reddened **zero**. Strict TDD, RED before GREEN: `convex/__tests__/taskClosureGate.test.ts`, `staleInProgress.test.ts`, `billingSummaryByProject.test.ts` (new) — RED 6 failing / 9 against unmodified code, then GREEN 11/11. **Bite-proved by mutation, and the mutation was asserted to have LANDED before any verdict was read** (a probe that does not assert its own mutation landed is a coin flip read as a result): blinding the gate's `startedAt` check (`if (false && …)`) reddens exactly the rejection tests — `(a)` via `complete`, `(e)` via `bulkComplete`, `(i)` via `update` — with `AssertionError: promise resolved instead of rejecting`, while the pass-through cases correctly stay green; restore leaves 0 markers and 11/11. Full root suite **2405 passed / 42 skipped (140 files)**, mcp-server suite **1084 passed / 12 skipped (31 files)**, `tsc --noEmit` 0 in both packages. **Known gap, stated rather than hidden**: `billableProjects` is seeded with `["vantage-immo"]` only. A second billable engagement named in the task brief has no corresponding project slug anywhere in this repo, so it is **not yet covered** by the gate; that config row must be patched with the real slug rather than guessed. Task `k17dhcmzqafve1ayzvh833kf558ae019`.

### Security
- **fix(auth): `withOrgScope` no-identity default flips fail-open → fail-closed; 4 client-facing handlers scoped; MCP legacy bearer path (4) no longer leaves `oauthContext` unset — STEP, not the multi-tenant model's completion** — `convex/lib/auth.ts` `withOrgScope(ctx, opts?)`: absence of a Clerk identity previously resolved unconditionally to `isMaster=true, allowedOrchestrators=["*"]`. It now defaults to fail-closed (`isMaster=false, allowedOrchestrators=[], scopes=[]`) unless the call site explicitly opts in via `{ allowNoIdentityMaster: true }` — the opt-in exists because Convex has no reliable signal to distinguish an MCP-server call without a JWT from an anonymous caller. The 10 existing internal call sites that pass the opt-in (`convex/tasks.ts`, `convex/missions.ts`, `convex/dashboard.ts`, `convex/stats.ts`, `convex/briefingNotes.ts`, internal `convex/messages.ts` paths, `convex/memories.ts`) are unaffected. Four previously-unscoped client-facing handlers now call `withOrgScope`/`filterByOrgScope`: `convex/memories.ts` (`listMemories`, `getMemory`), `convex/messages.ts` (`listByChannel`), `convex/diary.ts` (`list`). On the MCP side, `mcp-server/src/auth.ts` path (4) (legacy `mcpTenants` bearer) previously left `oauthContext` unset, which made every `tools.ts` guard (`guardRead`/`guardWrite`/`guardMasterOnly`) and every `checkNamespaceRead`/`checkNamespaceWrite`/`checkFromAllowed` predicate treat the request as unscoped/allowed; it now sets a deny-by-default context (`scopeProfile: "legacy-tenant-generic"`, empty `fromAllowList`/`namespaceReadPrefixes`/`namespaceWritePrefixes`, `isMaster: false`). New coverage: `convex/__tests__/multiTenantIsolation.test.ts` (5 tests: `withOrgScope` no-identity fail-closed unit test + cross-tenant read denial for `memories.listMemories`, `memories.getMemory`, `messages.listByChannel`, `diary.list`). Full Convex suite **2384 passed / 12 skipped, exit 0**. **This closes one fail-open gap; it is not the completion of the multi-tenant model** — the full contract (each tenant reads/writes strictly its own data everywhere) remains the product direction reaffirmed by Laurent and is tracked/realigned separately by Pi. Open follow-ups, not resolved by this fix: (a) a non-MCP anonymous call site explicitly passing `allowNoIdentityMaster: true` still resolves to master; (b) the legacy-tenant (path 4) e2e test now falls under an empty scope by design and needs re-provisioning via the OAuth scoped-token path with explicit prefixes; (c) whether a `global` namespace prefix belongs in Nadia's OAuth profile is undecided — TODO confirm intent with Laurent; (d) no real-network e2e against a live Clerk JWT has been run — isolation is proven at the `convex-test` harness level only. Doc: `docs/cloud/security-multi-tenant.md` §7. Task `k1759yh6mjqcgwq7am85acvqh18abbjd`.

### Fixed
- **fix(migration): the `origin` backfill could never finish — and its "done" signal was its stuck signal** — caught in PRODUCTION, mid-deploy, on the very first run. The migration shipped in #1089 took `.take(100)` over rows matching (`createdBy: "system"` AND `origin` undefined), patched only those whose title matched the webhook's `[Review]` format, and counted the rest as `skipped` — **leaving them untouched**. So they stayed candidates forever, and the next call re-fetched the same first 100 non-matching rows. It could never reach the `[Review]` tasks. Live proof against prod: `{ "skipped": 100, "updated": 0 }` — zero progress. **The lethal part was not the stall, it was the REPORT.** The instruction said "re-run until `updated: 0`". But **`updated: 0` is ALSO the stuck state**: "I made no progress" and "the work is finished" produced the *same output*. That first result would have been read as success, leaving every reviewer's `[Review]` task blocked in production — recreating the exact over-blocking bug the parent PR exists to fix. A migration whose completion signal is indistinguishable from its failure signal cannot be trusted, however correct its patching logic is. It is the same defect class as everything else this day: **"I could not determine it" and "it is clean" must never print the same thing.** **Fix**: a real cursor walk — every page advances, and `isDone` comes from the paginator itself, the one fact the caller cannot misread. The stop condition is `isDone`, **never `updated === 0`**; a page of zero updates is a normal page, not a finish line. Pages **2, 3, 4, 8 and 22** of the real run each returned `updated: 0` — exactly what the old code would have read as *done*. **Real run, prod, cursor walked to `isDone`: 39 pages, 1197 `[Review]` tasks backfilled, 6589 rows correctly left untouched.** Prod smoke after the walk: forged `createdBy: "system"` billable never-started -> REFUSED; human-created billable never-started -> REFUSED (gate intact); non-billable -> passes (zero false positive); a real backfilled `[Review]` task now carries `origin: "automation"` (reviewers unblocked). Deploy authorized by Pi under task `k174hfg3arfb5zy7y1pxc0x31d8afdmd`.
- **fix(tasks): the automation exemption is now UNFORGEABLE — `createdBy` was a caller-supplied string on the public MCP surface, so anyone could exempt themselves from billing with one word** — caught by Eta reviewing #1089. The first fix exempted automation-created tasks by reading `task.createdBy === "system"`. But `createdBy` is **not read from an artifact — it is TYPED BY THE CALLER**: `convex/schema.ts` `creatorValidator = v.string()` (any string), and `tasks.create` is a **public** mutation exposing `createdBy` as a caller argument. So `create_task({ project: "vantage-immo", createdBy: "system" })` bought a **lifetime exemption from the billing gate**. Eta's probe, executed: a billable task created by a human declaring themselves `"system"`, never started, closed **without rejection**. **I violated my own rule while quoting it.** I justified the fix with "the signal is already in the row and is READ, not re-typed" — but a caller-supplied string is not a derived fact, it is a **statement on one's honour**. The genuinely verifiable fact is **which code path created the row**: `createOrUpdateReviewTask` is an `internalMutation` (unreachable from the public surface); `tasks.create` is public. *That* cannot be typed by anyone. And the AUTO-3 non-regression test I was proud of tests a human **declaring themselves human** — it never tests a human **declaring themselves system**. That was precisely the hole. **Fix**: new internal-only field `origin: v.optional(v.literal("automation"))` (`convex/schema.ts`), written **only** by the webhook's `internalMutation` (`convex/tasks.ts:1905`) and **absent from the public `create` mutation's args** — a public caller cannot set it, which is what makes it unforgeable. The gate now reads `task.origin`, **never `createdBy`** (`convex/lib/taskClosureGate.ts:118-130`). **Stated rather than hidden**: the brief also asked for defense-in-depth (reject `createdBy: "system"` outright on the public surface). That was implemented, then **reverted** — it regressed 44 pre-existing tests, because `"system"` is a legitimate convention elsewhere in the codebase (RBAC-bypass semantics, stats/bridge fixtures). The billing bypass is fully closed by the `origin` gate independently of `createdBy`, so the reservation was dropped rather than forced through at the cost of breaking real behaviour. **Migration, because the trap was real**: existing `[Review]` rows have no `origin` and would have been re-blocked — recreating the very over-block just fixed, in prod, for every reviewer. `convex/migrations/backfill_review_task_origin.ts` (idempotent, batched) backfills them, narrowly scoped to rows matching BOTH `createdBy === "system"` AND the webhook's exact title pattern. That single use of `createdBy` is a **one-time historical-data repair on pre-fix rows, not an authorization control** — written down in the migration rather than left implicit. **Bite-proved by mutation, asserted LANDED before any verdict was read**: pointing the gate back at `createdBy` reddens **FORGE-1, FORGE-2 and FORGE-3** — all three closure paths (`complete`, `update`, `bulkComplete`), because a fix covering one path would reproduce the class. Suite **2414 passed / 0 failed**. Task `k17dhcmzqafve1ayzvh833kf558ae019`.
- **fix(leak-guard): the scan scope is DERIVED from what actually ships, not from two hand-written globs — the guard covered 19% of the published artifact and reported a clean bundle** — caught by Eta reviewing plugin PR #39, and he is right. The leak guard built its inventory from two hand-maintained globs (`plugin/skills/*/SKILL.md`, `plugin/hooks/*.py`) = **53 files**. The shipped artifact is **284 files** (`find`, no `.claudepluginignore`). So the "grep the bundle → 0 leaks" I reported **never scanned the bundle** — it scanned *what the guard looks at*. The maintainer's name was purged where the guard looks and survived everywhere else: under `references/`, `evals/`, `docs/` — all invisible to `skills/*/SKILL.md`. **This is the exact disease the PARITY guard does not have**: there, coverage is derived from the artifact (`iterdir`) — a constraint I wrote, proved, and shipped, and then failed to apply to the leak guard sitting *in the same PR*. "A hand-maintained list is the same disease one level up" is my own sentence, written before walking into it. Eta owns part of it too, and said so: on #1087 he verified the parity guard's inventory was derived and credited it, and did not apply the same check to its twin. **Fix**: scope is now derived (`os.walk` over the shipped tree); **CHECKED ∪ SKIPPED-with-a-written-reason = 100%** of shipped files — nothing ignored in silence, a binary is SKIPPED *with a reason*, not forgotten; zero files found = broken parser, not a clean repo → loud failure; the file count is derived, never written down; every finding is NAMED with file and line. **What the derived scan found, live, in the PUBLIC package**: (1) a real **client PRODUCTION deployment slug** in the plugin `CHANGELOG.md` — now TIER 1 (client data, hard block: a client's prod deployment is not a minor internal identifier); (2) the maintainer's first name in **10 shipped files** — Eta reported 3, the derived scan found 10. Both purged (redaction, not rewriting: the facts stay, the identifiers go). **Bite-proved, mutation asserted LANDED before any verdict was read**: a leak planted under `docs/` — invisible to the old globs — is now caught and named with file and line; restored, guard green across 284 derived files. **No over-purge**: `deploy-track`'s `guineapig-77` (a teaching example) and `DEPLOY_KEY_GUINEAPIG` (an env-var *name* — that skill's own text teaches the name is not the secret) are untouched and unflagged. A guard that mangles correct examples gets ripped out, and then it guards nothing. Tests **16/16**, including the case that closes the class: a leak under `references/` reddens the guard — that test fails on the old two-glob code. Task `k17at7kta2ty5dy1tkytzp1s318acacw`.
- **fix(tasks): the closure gate no longer demands a start time from tasks nobody starts — every `[Review]` closure on a billable project was being refused** — caught by Eta on the *first real field use* of the #1086 gate, hours after it shipped to prod. `[Review]` tasks are auto-created by the GitHub webhook (`upsertReviewTask`, `createdBy: "system"`) directly at `status: "todo"`, and **nothing ever calls `start_task` on them** — they structurally never carry a `startedAt`. The gate was therefore demanding proof of a clock that never runs, and every reviewer, every time, on every billable project, was blocked with `TASK_NEVER_STARTED_BILLABLE`. **The root cause is upstream of the gate, not in it** — so the path is fixed, not the guard. An automation-created task is internal process bookkeeping, not a timed engagement performed for a client; it has **no billable duration by construction**. The signal is already in the row and is READ, not re-typed (`derive-never-type`): `createdBy === "system"` (`convex/lib/taskClosureGate.ts:104-124`). **Eta's refusal to work around it is the reason this was fixed properly**: he declined to call `start_task` immediately before closing, because `startedAt = now` would have produced a ~0 duration and therefore a FALSE billing figure — exactly what the gate exists to prevent, *while leaving the gate green*. He declared the divergence instead of dressing it up. **Deliberately NOT done: widening the `// allow-no-time-line:` escape hatch.** Had every reviewer been made to stamp an exemption for life, the exemption would have become a **reflex** — and an exemption applied by reflex has stopped being a decision. That is how a guard gets hollowed out from the inside without ever being switched off. The fix removes the *need* for an exemption on this path rather than making it easier to reach for. Human-authored work is untouched: a missing `startedAt` there still means nobody can vouch for how long it actually took. **No new mandatory config key**, deliberately — one would have reopened the deploy→seed fail-closed window in which every task closure carrying a `project` is blocked fleet-wide; the exemption reads a field the webhook already writes. **Bite-proved by mutation, each mutation asserted LANDED before any verdict was read** (a red from a syntax error proves nothing; both mutations compiled): forcing the detection to `false` reddens `AUTO-1` + `AUTO-2` (the gate over-blocks again), forcing it to `true` reddens `AUTO-3` (human-created work stops being gated). `AUTO-3` is the case that matters most — it proves the fix did not simply disarm the gate. A guard placed but never proven to bite is indistinguishable from an absent one. Tests in `convex/__tests__/taskClosureGate.test.ts`; suite **2411 passed / 0 failed** (was 2408 — +3 cases, no regressions). Eta's verdict on #1086 stands unchanged: APPROVED @06787da — the gate is good, the upstream path was incomplete. Task `k17dhcmzqafve1ayzvh833kf558ae019`.
- **fix(webhook): `[Review]` tasks now close when their PR closes, and stop multiplying on every push** — measured on Eta's live queue, not estimated: of **28** `todo` review tasks, **~20 were dead** (their PR already MERGED — every VantagePeers PR #1071→#1082) and **6 were strict duplicates** (PR #1073 alone had **4** copies: 1 open + 3 pushes). The reviewer was picking from a queue where 5 tasks in 6 were corpses, which is why real reviews sat unstarted underneath them. Two defects, both in `convex/http.ts`. **(1)** The `pull_request` + `action === "closed"` handler *existed* (~line 498) but only ever created a deploy task — it never closed the matching `[Review]` task. The handler ran, on every merge, and did nothing; corpses accumulated forever. Now calls `internal.tasks.closeReviewTasksForPr` on **both** paths, merged and closed-without-merge (a review of a closed PR is moot either way; the `completionNote` records which case — `[PR-MERGED]` vs `[PR-CLOSED-NO-MERGE]`). **(2)** `opened`/`synchronize` (~line 445) called `api.tasks.create` on every push with **no dedup key**, so N pushes produced N rows. Now `internal.tasks.createOrUpdateReviewTask` dedups on **`(repoFullName, prNumber)`** — not `prNumber` alone — and refreshes the existing row in place on re-push. This **mirrors `createDeployTaskWithDedup`**, already in the same file for deploy tasks: the pattern existed, reviews just never used it; nothing was invented. **Two suites, and the second is the load-bearing one.** `convex/__tests__/reviewTaskDedupClose.test.ts` covers the two new `internalMutation`s in isolation (RED 6/6 on missing exports → GREEN). `convex/__tests__/reviewTaskWebhookWiring.test.ts` (new) drives the **real `POST /github/webhook` route** via convex-test's `t.fetch` with synthetic GitHub payloads, replaying the exact #1073 sequence (1 `opened` + 3 `synchronize`) and asserting **one** task, not four. **Testing the mutations alone would NOT have caught this bug**: the original defect was a handler that *ran and did nothing*, and a mutation-level suite stays green straight through exactly that failure — proving the parts and skipping the wiring reproduces the bug one level up. The wiring suite needs a `githubRepoMapping` fixture (`active: true`), else the webhook returns `OK - unmapped repo` early (`http.ts:90`) and every assertion is green-by-vacuum. **Mutation-proved at the wiring level, both clauses isolated**: removing the close call — i.e. *restoring the original bug* — reddens exactly **3** wiring tests; neutralising the dedup lookup *inside* `createOrUpdateReviewTask` (leaving the call signature intact) reddens exactly **1** (the duplicate-count assertion), the other 5 staying green. The wiring suite was written *after* the fix, so it is not TDD in the strict sense; restoring the bug and watching it redden proves the same thing the red-first would have. Convex suite **761/761**, `npx vitest run convex/` **EXIT=0** (the exit code, not the "N passed" line — a run can print N passed and still exit non-zero), `tsc --noEmit` 0. **Backend `convex/` only — `mcp-server/` untouched, no npm republish; activation = `convex deploy`.** The ~20 existing corpses are Eta's to close (RULE #31 — the assignee closes with their own proof); this fix stops the queue re-polluting. Task `k175bk5jamt2t08j90we50rqw58aan11`.
- **fix(recurringTasks): the whole by-id surface answers a wrong-table ID with an actionable error, not `Server Error` (#1064 reads, slice-6 — FINAL)** — sixth and last slice of the by-id migration after #1072/#1075/#1076/#1077/#1078/#1079, same `requireId` helper (`convex/lib/ids.ts`) unchanged, completing every inventoried single-id handler. All **five** single-id handlers in `convex/recurringTasks.ts` (table `recurringTasks`): `getById` (query, arg `recurringTaskId`), `update`/`pause`/`resume`/`remove` (mutations; `update`+`getById` use `recurringTaskId`, `pause`/`resume`/`remove` use `taskId`). Each relaxed from `v.id("recurringTasks")` to `v.string()` then narrowed via `requireId(ctx, "recurringTasks", <raw>, <argName>, hint)` at the top of the handler, before any `ctx.db.get`/`patch`/`delete`. Hint is a module-level constant on the source side, but the tests assert a **copied local literal** (never the imported constant — the #1074 F1 blind-spot): `"Use the full 32-char id returned by list_recurring_tasks or create_recurring_task."` (per-arg full message, e.g. `recurringTaskId is not a valid recurringTasks ID. …`). Pre-existing null-checks ("Recurring task not found" in `update`/`resume`) left intact — they now operate on the narrowed `Id<"recurringTasks">`; no `returns:` validators changed. **No `ctx.scheduler` in this module** (verified by reading the file), so no fake timers needed. Strict TDD: `convex/__tests__/recurringTasks.wrongTable.test.ts` (new, 13 tests — wrong-table + positive control per handler, plus deleted-doc controls on getById/update/resume) — RED **5 failing / 13** against unmodified handlers → GREEN **13/13**. Mutation-proved: corrupting the **source** hint constant reddens exactly 5 tests (one per handler, proving the tests use their own literal, not the mutant-following constant), revert restores 13/13. Convex suite 749/749, `npx vitest run convex/` **EXIT=0**, `tsc --noEmit` 0. **Backend `convex/` only — `mcp-server/` untouched, no npm republish; activation = `convex deploy`.** Fix-pattern `m97ac8v` Layer 2, reads. With this slice the wrong-table actionable-error class covers every inventoried single-id read/by-id handler across the schema. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.
- **fix(errorMonitor, fixPatterns): `getError` + `get` answer a wrong-table ID with an actionable error, not `Server Error` (#1064 reads, slice-5)** — fifth slice of the by-id migration after #1072/#1075/#1076/#1077/#1078, same `requireId` helper (`convex/lib/ids.ts`) unchanged. Two read queries: `convex/errorMonitor.ts` `getError` (arg `errorId`, table **`errorLogs`**) and `convex/fixPatterns.ts` `get` (arg `patternId`, table `fixPatterns`), each relaxed from `v.id("<table>")` to `v.string()` then narrowed via `requireId(ctx, "<table>", raw, "<arg>", hint)`. Hints locked as **test literals**: errorMonitor `"Use the full 32-char errorId returned by list_errors."`, fixPatterns `"Use the full 32-char patternId returned by list_fix_patterns or search_fix_patterns."`. Null-preserving contract kept (both return `union(..., v.null())`; `returns:` validators unchanged). Strict TDD: `convex/__tests__/errorMonitor.getError.wrongTable.test.ts` + `fixPatterns.get.wrongTable.test.ts` (new, full-message `.toBe` incl. hint, tolerant `decodePayload`) — RED **6 failing / 8** against unmodified handlers → GREEN **8/8**. Mutation-proved: corrupting the errorMonitor hint literal reddens exactly 1 test, targeted revert restores 8/8. Convex suite 736/736, `npx vitest run convex/` **EXIT=0** (verified, not just the passed count — the slice-4 lesson), `tsc --noEmit` 0. **Test hygiene**: `fixPatterns.get.wrongTable` uses `vi.useFakeTimers()`/`useRealTimers()` because `fixPatterns:create` (called by the positive control) schedules `ragSync.addFixPatternRagEntry` — on real timers that scheduled write leaks post-transaction as an UNHANDLED rejection (`Write outside of transaction …_scheduled_functions`), green assertions but non-zero CI exit; fake timers keep it under control (memory `j57ac4gggeae4r03zjf0pmj31h8a8far`). **Backend `convex/` only — `mcp-server/` untouched, no npm republish; activation = `convex deploy`.** Fix-pattern `m97ac8v` Layer 2, reads. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.
- **fix(memories): `getMemory` + `softDeleteMemory` answer a wrong-table ID with an actionable error, not `Server Error` (#1064 reads, slice-4)** — fourth slice of the by-id migration after `tasks:getById` (#1072), slice-1 (#1075), slice-2 (#1076) and slice-3 missions/mandates (#1077), same `requireId` helper (`convex/lib/ids.ts`) unchanged. Two handlers in `convex/memories.ts`, both keyed on `memoryId`: `getMemory` (the query behind the MCP `get_memory` tool — episodes are fetched through it too, per the `episodes.ts` note) and `softDeleteMemory` (mutation). Both relaxed from `v.id("memories")` to `v.string()` (the `v.id()` validator runs *before* the handler and Convex redacts its rejection in prod) then narrowed in-handler via `requireId(ctx, "memories", raw, "memoryId", hint)`. Hint locked as a **test literal** (both handlers): `"Use the full 32-char memoryId returned by recall or store_memory."`. **Two distinct contracts preserved and tested independently**: `getMemory` returns `union(..., v.null())` so a valid same-table id for an absent doc still returns `null`; `softDeleteMemory` keeps its pre-existing `if (memory === null) throw new Error("Memory … not found")` path — `requireId` throws only on wrong-TABLE ids *before* the get, so a valid-but-absent id still hits the original `Error`, NOT the `ConvexError` (a dedicated test pins this). Strict TDD: `convex/__tests__/memories.getMemory.wrongTable.test.ts` + `memories.softDeleteMemory.wrongTable.test.ts` (new, mirroring `missions.get.wrongTable` — full-message `.toBe` incl. hint, tolerant `decodePayload`) — RED **2 failing / 6** against the unmodified handlers → GREEN **6/6**. Mutation-proved: corrupting the `getMemory` hint literal reddens exactly 1 test, targeted revert restores 6/6. Convex suite 728/728, `tsc --noEmit` 0. **Test hygiene**: both new files use `vi.useFakeTimers()`/`useRealTimers()` (kb-ingest pattern) — `storeMemory`/`softDeleteMemory` schedule `ragSync` functions that are excluded from the hermetic convex-test module set, so on real timers they fire after the test and reject with "Could not find module for: ragSync" as UNHANDLED errors: green assertions but a non-zero CI vitest job. Fake timers keep the scheduler under test control so nothing leaks. **Backend `convex/` only — `mcp-server/` untouched, no npm republish; activation = `convex deploy`.** Fix-pattern `m97ac8v` Layer 2, reads. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.
- **fix(missions, mandates): `get` answers a wrong-table ID with an actionable error, not `Server Error` (#1064 reads, slice-3)** — third slice of the by-id read migration after `tasks:getById` (#1072), `briefingNotes/businessUnits.get` (slice-1 #1075) and `messages` (slice-2 #1076), same `requireId` helper (`convex/lib/ids.ts`) unchanged. Two plain single-id read queries: `convex/missions.ts` `get` and `convex/mandates.ts` `get`, each relaxed from `v.id("<table>")` to `v.string()` (the `v.id()` validator runs *before* the handler, leaving no seam, and Convex redacts its rejection in prod) then narrowed in-handler via `requireId(ctx, "<table>", raw, "<arg>", hint)`. Hints locked as **test literals**, not imported constants: missions `"Use the full 32-char missionId returned by list_missions or create_mission."`, mandates `"Use the full 32-char mandateId returned by list_mandates or create_mandate."`. **Null-on-missing contract preserved**: both `get` return `union(..., v.null())` — `requireId` throws only on wrong-TABLE ids, so a valid same-table id for an absent doc still returns `null`; each new test file carries a positive control asserting this. `returns:` validators left unchanged (output types). Strict TDD: `convex/__tests__/missions.get.wrongTable.test.ts` + `mandates.get.wrongTable.test.ts` (new, mirroring `briefingNotes.get.wrongTable`) — RED **4 failing / 4** against the unmodified handlers (`Validator error … Expected ID` instead of `ConvexError`) → GREEN **8/8**. Mutation-proved: corrupting the missions hint literal reddens exactly 1 test, targeted revert restores 8/8. Convex suite 722/722, `tsc --noEmit` 0. **Backend `convex/` only — `mcp-server/` untouched, no npm republish; activation = `convex deploy`, a Railway rebuild does NOT activate it.** Fix-pattern `m97ac8v` Layer 2, reads. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.
- **fix(messages): `getById` + `markAsRead` answer a wrong-table ID with an actionable error, not `Server Error` (#1064 reads, slice-2)** — extends the wrong-table class from `tasks:getById` (#1072) to `messages`, same `requireId` helper (`convex/lib/ids.ts`), continuing the by-id read slices (141 sites inventoried, done in slices not big-bang). Two handlers in `convex/messages.ts`: `getById` relaxed from `v.id("messages")` to `v.string()` then narrowed with `requireId(ctx, "messages", raw, "messageId", hint)` — the `v.id()` validator runs *before* the handler, leaving no seam, and Convex redacts its rejection in prod (`Server Error`, `error.data` undefined); only an explicitly-thrown `ConvexError`'s payload survives on `error.data` (preferred by `mcpConvexError` since #1065). `markAsRead` migrated from its inline `ctx.db.normalizeId` + hand-thrown `ConvexError` (#1069) to the same `requireId` helper — behaviour-preserving (per-index `receiptIds[i]` path naming, idempotent mark, batch handling intact), one code path instead of two. Hints locked as **test literals**, not imported constants (an imported constant follows the mutant and proves nothing): getById `"Use the full 32-char messageId returned by list_messages or checkNewMessages."`, markAsRead `"Use the full 32-char receiptId returned by check_messages."`. Strict TDD: `convex/__tests__/messages.getById.wrongTable.test.ts` (new) RED 2/4 against original code (`Validator error … Expected ID` instead of `ConvexError`) → GREEN; with the existing `messages.markAsRead.wrongTable.test.ts` → 9/9. Mutation-proved: corrupting the getById hint literal reddens 1 test, reverting restores 9/9 with empty `git diff`. Convex suite 706/706, `tsc --noEmit` 0. **Backend `convex/` only — `mcp-server/` untouched (0 files), no npm republish; activation = `convex deploy`, a Railway rebuild does NOT activate it.** Fix-pattern `m97ac8v` Layer 2, reads. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.
- **fix(convex): wrong-table ID guard extended to `briefingNotes.get` + `businessUnits.get` (#1064, reads slice 1)** — the second slice of the by-id read migration after `tasks:getById`, applying the same `convex/lib/ids.ts` `requireId()` helper unchanged. Both `get` queries relaxed their arg validator from `v.id("<table>")` to `v.string()` (the `v.id()` validator runs **before** the handler, leaving no seam) and narrow in-handler via `requireId(ctx, "<table>", raw, "<arg>", hint)`, throwing a structured `ConvexError({ path, expectedTable, receivedId, message })` naming the offending id and how to obtain the right one (e.g. *"Use the full 32-char noteId returned by list_briefing_notes or create_briefing_note."*). Strict TDD: two new files `convex/__tests__/briefingNotes.get.wrongTable.test.ts` + `businessUnits.get.wrongTable.test.ts` (mirroring the `tasks.getById` reference, with the tolerant `decodePayload` accepting both the convex-test string and prod object shapes of `ConvexError.data`) — RED **4 failing / 8** against the unmodified handlers, GREEN **8/8** after fix; full `convex/__tests__` suite **399/399**, `tsc --noEmit` 0. The hint is asserted as a **copied literal** plus `not.toBe(<base-without-hint>)`, not an imported constant — mutation-proved of my own hand: emptying the hint at the `briefingNotes` call-site reddens exactly 1 test (a subimported-constant assertion would have followed the mutant — the #1072 F1 blind spot, guarded here from the start). Null-on-missing contract preserved on both (a valid same-table id for a deleted doc returns `null`, does **not** throw). **Scope discipline: two sites, declared not four** — `diary.get` (composite `{date, orchestrator}` key) and `components.get` (composite `{name, type}` key) are not single-id gets and were skipped, not forced. **Activation: lives in `convex/` — `convex deploy` activates it, nothing else; `mcp-server/` untouched → no npm republish.** Fix-pattern `m97ac8v` Layer 2. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.
- **test(tasks): assert the hint — the only sentence that tells the caller what to do (#1072 follow-up, Eta F1)** — `#1072` promised an *actionable* error; its tests only measured a *well-labelled* one. `requireId` builds `message` as `` `${base} ${hint}` `` where `base` is `"taskId is not a valid tasks ID."`. The assertions were `toContain("taskId")` and `toContain("tasks")` — both already satisfied by `base` alone. Deleting the hint entirely left **4/4 green**, reproduced here before being believed. The hint is the last clause of a composed message, so a substring assertion on the prefix structurally cannot see it vanish; #1069 had pinned its equivalent (`toContain("check_messages")`) and the extraction into a helper lost it. Fixed by asserting the hint as a **literal in the test**, not against a constant imported from the source — an imported constant moves with the mutant and the assertion proves nothing — plus `expect(message).not.toBe(base)`. Three mutants now die where none did: hint removed (1 red), hint replaced by empty text (1), hint replaced by a different sentence (1); baseline restores to 4/4 with an empty `git diff`. Convex suite 700/700, `tsc --noEmit` 0. Tests-only; `convex/lib/ids.ts` and `convex/tasks.ts` unchanged. **This lands before the `convex deploy` of #1072**, so the message served in prod is not well-labelled and useless. Found by Eta on the #1072 gate. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.
- **fix(tasks): `getById` answers a wrong-table ID with an actionable error, not `Server Error` (#1064, reads)** — the wrong-table class was closed on `markAsRead` (a write) in #1069 and deliberately left open everywhere else. It bit within the hour: on Day 127 Pi cited a well-formed `taskId` that does not decode to `tasks`; `complete_task` answered `[Request ID: 2239960d2fbbbfdb] Server Error` with `error.data` **undefined**, and the round trip cost a full exchange. A read error that cannot be acted on costs as much as a write error — hence Pi's scope ruling: extend the pattern to reads. Same structural cause as #1069: `v.id("tasks")` runs **before** the handler, so its rejection cannot be intercepted and enriched, and Convex redacts it in prod; only an explicitly-thrown `ConvexError`'s payload survives, on `error.data`, which `mcpConvexError` has preferred since #1065. New helper `convex/lib/ids.ts` — `requireId(ctx, table, raw, path, hint)` — extracted rather than inlined because **141 by-id read sites** remain, to be done in slices, not a big-bang. Applied to `tasks:getById` only here. Strict TDD: `convex/__tests__/tasks.getById.wrongTable.test.ts` (new) — RED 2 failing against the original code (`expected Error: Validator error … to be an instance of ConvexError`), the two controls already passing → GREEN 4/4. Mutation-proved, six mutants, all dead: neutralised `requireId` (2 red), hardcoded `path` (1), hardcoded `expectedTable` (2), `ConvexError`→`Error` (2), emptied `receivedId` (2), and — guarding the contract that must be **preserved** — turning a deleted-document miss into a throw (1). Widening a validator must not convert a benign `null` into an error. Convex suite 700/700, `tsc --noEmit` 0. `mcp-server/` untouched (0 files) so **no npm republish**; `convex/schema.ts` untouched. **Activation: lives in `convex/` — a Railway rebuild does NOT activate it; `convex deploy` does, and nothing else.** Fix-pattern `m97ac8v` Layer 2, extended from writes to reads. Task `k1709ps4d90fvs9v5fa4aqgr898a981k`.

- **test(messages): cover BOTH runtime shapes of `ConvexError.data` — and correct what #1069 claimed** — the `decodePayload` helper in `convex/__tests__/messages.markAsRead.wrongTable.test.ts` tolerates `data` as either a JSON string or the thrown object, but only the string branch was exercised, and its comment (echoed into the #1069 entry below) asserted a single measured shape. Measured, not argued: under `convex-test`, `typeof error.data === "string"`; against prod `compassionate-goldfinch-737` via `ConvexHttpClient`, after the #1069 deploy (`26003803410fcf043fc17b78bc11749427b8f3f0`), `typeof error.data === "object"`. Reviewing #1069, Sigma and Eta each probed exactly one runtime and concluded the opposite of the other (finding F1) — each correct about the surface measured, each wrong to generalise from it. The tolerant decoder is therefore not decorative prudence: **both shapes are real, neither branch is dead**. Tests-only fix: a new `describe("decodePayload — both runtime shapes")` pins the string branch and the object branch, using the verbatim prod payload as fixture. Mutation-proved rather than merely added — deleting the string branch reddens 3 tests, forcing `JSON.parse` (killing the object branch) reddens 1; baseline restores to 5/5 with an empty `git diff`. File 5/5, convex suite 698/698, `tsc --noEmit` 0. **Tests-only: no `convex/` function changed, so `convex deploy` is a strict no-op here — and per the #1068 gate, merging this PR must emit zero deploy task.** Task `k172ccr0bhw16t96z2gxx0hptn8a9ba8`.

- **fix(ci): the npm publish workflow could never install — `npm ci` is impossible in this repo** — `.github/workflows/npm-publish.yml` ran `npm ci` in `mcp-server/`, which fails with `EUSAGE: The npm ci command can only install with an existing package-lock.json`. Every publish this repo has shipped was therefore done by hand. Root cause established **with a control**, not inferred: the root `package.json` declares `workspaces: ["apps/*", "mcp-server"]`, so npm resolves `mcp-server` as a workspace **child** and looks for the lockfile at the workspace **root**, which holds only `bun.lock`; the child's own `mcp-server/package-lock.json` is ignored. In a sandbox reproducing the tree, the **only** difference between failure and success was the presence of the root `package.json` — with it, `npm ci` → `EUSAGE`; without it, `added 250 packages`, exit 0. (`apps/*` matches nothing; the directory does not exist.) Fix is minimal and flips no policy: the workflow now runs `npm install --no-audit --no-fund` (measured: 257 packages, exit 0). **The alternative was measured and deliberately not taken**: committing a root `package-lock.json` makes `npm ci` succeed at the root *and* in `mcp-server` (320 entries, both paths exit 0), but `.gitignore:5` ignores `package-lock.json` on purpose — `bun.lock` is this repo's source of truth — and dropping the `workspaces` key to satisfy npm makes `bun install --dry-run` report `1 package removed`, i.e. it unlinks the `mcp-server` workspace. Flipping the lockfile policy is Pi's call, not a workflow patch. Two measurements worth recording: the peer conflict (`@convex-dev/rag@0.7.2` wants `@convex-dev/workpool@^0.3.0`, root declares `^0.4.6`) appears **only against a stale local `node_modules`** — on a clean tree npm resolves `rag@0.7.5`, which dropped that peer, and no conflict exists; and #1060's removal of `publishConfig.provenance` did **not** break CI provenance, because the workflow passes `--provenance` explicitly where OIDC is available. Task `k1760430nv0z96ptzjg00685e18a9avz`; root cause originally found by Eta. **Lockfile policy ruled by Pi (Day 128): `bun.lock` remains the single source of truth — no reversal.** As a corollary the orphan `mcp-server/package-lock.json` is removed here: a child lockfile that npm can never consume (it is ignored under the workspace resolution above) is a false witness — it is exactly what made `npm ci` *look* possible. One repo, one dependency source of truth.

- **fix(messages): a well-formed ID from the WRONG table now yields an actionable error, not `Server Error` (#1064)** — `mark_as_read` accepted a 32-char lowercase ID that *passes* the boundary regex shipped in #1065 but belongs to another table (a `messages._id` where a `messageReceipts._id` is required). The caller saw `[Request ID: …] Server Error` with nothing to act on. **The initial spec — "intercept the `ArgumentValidationError` at the MCP boundary and parse it" — is structurally impossible, and this was measured, not argued.** A `ConvexHttpClient` probe against prod `compassionate-goldfinch-737` calling `messages:markAsRead` with a real `messageId` returns `constructor=Error`, **`data` absent**, `message="[Request ID: 24c693d8c7cb2dab] Server Error"`: Convex **redacts** non-`ConvexError` throws in prod, so the text `Found ID "…" from table messages, which does not match … v.id("messageReceipts"). Path: .receiptIds[3]` **never reaches the client**. `parseConvexError` (`mcp-server/src/tools.ts:719`) already knows how to build the right hint from that text — it simply never sees it. What *does* survive redaction is the payload of an explicitly-thrown `ConvexError`, on `error.data`, which `mcpConvexError` already prefers since #1065. The two fixes compose, and the boundary needs **no change at all**. Fix is server-side (`convex/messages.ts`): `receiptIds` relaxed from `v.array(v.id("messageReceipts"))` to `v.array(v.string())` — the `v.id()` validator runs *before* the handler, leaving no seam to intercept — then each element is checked with `ctx.db.normalizeId("messageReceipts", raw)` (the canonical Convex API for table membership; used **nowhere** in `convex/` before this commit), throwing `ConvexError({ path: "receiptIds[i]", expectedTable, receivedId, message })` naming the exact array position and the `check_messages` provenance. Strict TDD: `convex/__tests__/messages.markAsRead.wrongTable.test.ts` (new) — RED 2 failing against the original code (`expected Error: Validator error … to be an instance of ConvexError`) → GREEN 3/3, with a positive control (a real receiptId still returns the count) and a third-table negative control (a `taskId` at `[0]`). Assertions target **each payload field independently**: a first version used `JSON.stringify(payload).toContain("receiptIds[1]")` and a mutant hardcoding `path: "receiptIds[0]"` **survived**, because `message` still carried the true index — the same "the guard exists, but is it wired?" blind spot as #1065/#1067. Six mutants now die (hardcoded `path`, neutralised `normalizeId`, wrong `expectedTable`, dropped `receivedId`, dropped `check_messages` hint, `ConvexError`→`Error`). Measured on the way: under `convex-test` `ConvexError.data` arrives as a **JSON string**, not the thrown object — typing the generic as an object compiles and still yields `undefined` at runtime, so the test decodes both shapes. (Superseded in scope by the entry above: the *prod* runtime hands back the **object**. Both shapes are real; this entry originally generalised one measurement to both surfaces.) Convex suite 696/696, `tsc --noEmit` 0. **Scope discipline: `markAsRead` only.** The other `v.array(v.id(…))` argument validators (`tasks.dependsOn` ×5, `briefingNotes.linkedMemoryIds`, `mandates.linkedTaskIds`, `kbMutations.chunkIds`, `missionTemplates.taskIds`) are **inventoried and deliberately left**, exactly as #1065 (truncated-ID class) was kept separate from #1064 (wrong-table class). **Activation note: this lives in `convex/`, not `mcp-server/` — a Railway rebuild does NOT activate it; `convex deploy` does, and nothing else. `mcp-server/` is untouched, so no npm republish.** Fix-pattern `m97ac8v` Layer 2 (table membership), complementing Layer 1 (format). Task `k173w4y4mjvvxerb6gzrnc9j2x8a9hdh`.

- **fix(security): the `[Deploy]` task generator no longer orders unattended PROD deploys** — the GitHub `pull_request.closed + merged` handler (`convex/http.ts`) created, on **every** qualifying merge, a task with `priority: "urgent"`, assigned to an **autonomous orchestrator**, whose description embedded a paste-ready `npx convex deploy --yes`, plus a message saying *"Deploy to prod now"*. That is the exact condition of the Day 103 incident (~50 PROD indexes wiped). Fourteen such tasks reached Epsilon's queue in one day; he refused each one — the generator was at fault, not him. **Root cause is NOT "the handler ignores the diff"** (the initial hypothesis): a "Day 102" gate already fetched the PR file list, but its predicate `/^convex\//` matched **test files too**. PR #209 changed only `scripts/uc1/joeai-csv-interne.cjs`, `scripts/uc1/lib/joeai-csv-lib.cjs` and `convex/tests/uc1/joeai_csv.test.ts` → `hasConvex === true` → urgent PROD-deploy order, although **no Convex function was served**. Fix: new pure module `convex/githubDeployGate.ts` with `prTouchesDeployableConvex()` (excludes `convex/tests/`, `convex/__tests__/`, `convex/_generated/`, and the same three under `apps/<pkg>/convex/`) and `buildDeployTaskPayload()` (title `[Deploy?] …`, `priority: "medium"`, `assignedTo: "laurent"`, **no paste-ready command**, and the literal `PROD-DEPLOY-AUTHORIZED` token gate per `publish-protocol.md`). The notify message drops the command too. Short-circuit + fail-open-on-page-cap semantics preserved verbatim; dedup/supersede logic untouched. Strict TDD: `convex/__tests__/githubDeployTaskGate.test.ts` — RED (module absent) → GREEN 16/16, including the **real PR #209 payload asserting zero task**. `autoTaskDedup` 26/26, convex suite 786 passed, `tsc -p convex` 0. Task `k174khqgkhgps846dhypwfz8b58a4fe1`, reported by Epsilon (friction), escalated by Pi.

- **fix(mcp): surface the real `ConvexError` instead of an opaque `Server Error` (all 121 tools)** — every `ConvexError` thrown by a Convex mutation reached the MCP server with its `message` **redacted by Convex** to `"[Request ID: … ] Server Error"`, while the actual actionable payload survived on **`error.data`**. `mcpConvexError` (`mcp-server/src/tools.ts:787`) read only `error.message` — so *every* tool answered `Server Error`, and orchestrators worked around the symptom instead of reading the cause. Proven empirically, not inferred, via a `ConvexHttpClient` probe against prod: `constructor=ConvexError`, `message="[Request ID: 3e0eac668b68a1ef] Server Error"`, `data="TASK_START_BLOCKED: Cannot start task … — caller sigma has an unclosed in_progress task … Call complete_task with completionNote first"`. Concrete cost: the long-standing `start_task → Server Error` "friction" was never a bug in `start_task` — the IRP guard (`convex/tasks.ts:876`) was correctly refusing because of a stale `in_progress` task, and the boundary hid the reason for days. Fix: `mcpConvexError` now prefers `error.data` (string as-is, otherwise JSON-stringified) and falls back to `error.message` when absent; `parseConvexError` is untouched, so `ArgumentValidationError` / `Path:` parsing and the existing fallback shape are unchanged. Strict TDD: `mcp-server/src/__tests__/convex-error-surfacing.test.ts` — RED 4 failing (the two `.data` cases) / 7 non-regression passing → GREEN 11/11; full mcp-server suite 1545 passed | 12 skipped (+11 = exactly the new file, no regressions); `tsc --noEmit` 0. **Activation note: this lives in `mcp-server`, not `convex/` — a `convex deploy` does NOT activate it; the Railway MCP rebuild does.**
- **fix(mcp): Convex document IDs validated at the MCP boundary — closes the `tasks:getById` error class (#996..#1063)** — 50 auto-opened GitHub issues (27 June → 10 July) all traced to one hole: MCP tools accepted Convex document IDs as bare `z.string()`. Peers routinely quote **short taskIds** (8–9 chars, e.g. `k173j35p`), which reached Convex's `v.id("tasks")` validator, threw, and surfaced as an opaque raw **`Server Error`** instead of a clean boundary rejection. Reproduced first-hand before any code: `get_task taskId="k173j35p"` → `Error: [Request ID: c89a6a144ccf2a48] Server Error`. **Verdict (a), not (b)** — the validation was *never applied*, not merged-but-inactive: `convexIdPattern` (`mcp-server/src/tools.ts:171`) existed but was wired into only `receiptIdSchema` (`:172`) and `memoryIdSchema` (`:179`); no `taskIdSchema` ever existed and `get_task.taskId` (`:8783`) was bare. Fix: `convexIdSchema(field)` factory + 12 exported per-entity schemas (task/mission/message/note/component/mandate/pattern/error/recurringTask/bu/episode/diary) applied to **every tool INPUT arg** carrying a real Convex document ID, including array forms (`dependsOn`, `blockedBy`). **Scope discipline**: params that are *not* Convex document IDs are deliberately left bare, each verified against its Convex validator — `docId` (KB UUID with dashes), `orgId`, `orchestratorId`, `instanceId`, `tenantId` (`v.optional(v.string())` filter, 10 call-sites), `issueId` (GitHub ref), `storageId` (`v.union(v.id("_storage"), …)`); a blanket sweep would have broken the KB ingest path shipped in #1059. `*OutputSchema` return validators (`:868-1397`) stay bare — not the boundary. A first sweep silently missed the **multi-line** declaration form (`missionId: z` ⏎ `.string()`); an AST-ish scanner caught 18 further unguarded input args (`create_task.dependsOn`, `update_task.dependsOn`, `add_task_dependency.dependsOn`, `get_mission.missionId`, `delete_message.messageId`, …), all closed — scanner now reports 0 unguarded input args. Strict TDD: `mcp-server/src/__tests__/convex-id-validation.test.ts` RED 55 failing / 5 non-regression passing → GREEN 60/60; full mcp-server suite 1534 passed | 12 skipped; `tsc --noEmit` 0. Boundary now answers `taskId must be a 32-char lowercase alphanumeric Convex ID`; negative control: a valid 32-char id passes. **Activation note — this fix lives in `mcp-server`, not `convex/`: a `convex deploy` does NOT activate it.** Activation = Railway rebuild of the MCP server from `main`. Schema tightening on published tools ⇒ **npm bump 2.15.0 → 2.16.0, republish required**. Fix-pattern `m97ac8v` Layer 1. Task `k179ytke9kzm2671pd6h8kr8hh8a9k9t`.
- **fix(kb): document chunks now embedded + searchable** — `store_document_chunked` (`convex/kb.ts` `storeDocumentChunked`) inserted each chunk into the `memories` table but never scheduled `ragSync.addRagEntry`, so uploaded KB documents were stored yet **invisible to `recall` / `text_search` / `hybrid_search`**. Reproduced live Day 122 on prod `compassionate-goldfinch-737` (sentinel via `storeMemory` found; sentinel via document path invisible even by exact token). Root cause: the chunk-insert loop lacked the `ctx.scheduler.runAfter(0, internal.ragSync.addRagEntry, …)` call that `storeMemory` (`convex/memories.ts:84`) and `storeEpisode` (`convex/episodes.ts:70`) already have; the justifying comment ("convex-test does not support the scheduler") was obsolete (convex-test ^0.0.44 supports it). Fix schedules `addRagEntry` per chunk (mirror of `storeMemory`), `type="reference"`. **Backend Convex only — `mcp-server/` unchanged, no npm republish required.** Strict TDD: `convex/__tests__/kb.document-indexing.test.ts` (new) asserts N `addRagEntry` scheduled = chunkCount via the `_scheduled_functions` system table (real embedding retrieval covered by prod e2e, not unit-testable without seeded embeddings); `convex/__tests__/kb-ingest.test.ts` upgraded with scheduler-drain (`vi.useFakeTimers` + `finishAllScheduledFunctions`) to absorb the newly-scheduled jobs. Tests: kb.document-indexing 2/2, kb-ingest 11/11 (0 unhandled). Mission `k57dh3jjaz1n3hgd0wdwyvmx8189w1z0`.

### Chores
- chore(oauth): clean inherited biome violations in mcp-server/server-http.ts (touch-it-clean-it discipline)

### Added
- **feat(kb): `generateUploadUrl` — the missing KB document upload entrypoint** — Day 123 discovery (Sigma): NO function or route anywhere in the system exposed `ctx.storage.generateUploadUrl()` (`grep -rniE generateUploadUrl` = 1 comment only; `convex/http.ts` had no upload route), so `store_document_chunked` — which requires an already-uploaded `storageId` — was **unreachable end-to-end**: no client (Nadia included) could obtain a `storageId`, hence prod `compassionate-goldfinch-737` held **0 documents** (`npx convex data _storage`/`kbUploads` both empty). Root gap **upstream** of the Day 122 indexing fix (`#1056`, already in prod at `99c5cbf`), which was correct but inert without an upload path. Fix: new **org-authenticated Convex mutation `generateUploadUrl`** in `convex/kbMutations.ts` (V8 runtime — mutations are forbidden in the `"use node"` `convex/kb.ts`, enforced at deploy time by Convex `InvalidModules`, caught by a real dev deploy not `tsc`), gated by the shared `assertOrgArgs` (extracted to new runtime-agnostic `convex/kbShared.ts`), returning `ctx.storage.generateUploadUrl()`. New **MCP tool `generate_upload_url`** in `mcp-server/src/tools/kbIngest.ts` resolves `orgId` from `oauthCtx.namespaceWritePrefixes[0]` (B4 #915 pattern) and calls `kbMutations:generateUploadUrl`. Strict TDD: `convex/__tests__/kb.generate-upload-url.test.ts` (new, 3/3 — org-auth returns URL, empty orgId → `AUTH_NO_ORG_ID`, non-`team/` namespace → reject; fake-ctx stub since convex-test does not implement `storage.generateUploadUrl`). Full e2e proven on DEV `efficient-guineapig-356`: `generate_upload_url` → POST binary → `storageId` → `store_document_chunked` (docId, chunkCount 1) → `hybrid_search` **FOUND** the sentinel (rrfScore 1) — inverse of Day 122 invisibility. Full convex suite 699/699, tsc convex + mcp-server 0. **New MCP tool ⇒ npm `vantage-peers-mcp` REPUBLISH REQUIRED** (unlike #1056 which was Convex-only). Mission `k571vk3cc265w8777g3z54vnd989w8k1`.
- **M1 storageId org-binding defense-in-depth (B5 follow-up)** — `convex/schema.ts` gains `kbUploads` table `{storageId, orgId, createdAt}` indexed `by_storageId`. New `bindOrAssertStorageOwnership` internalMutation in `convex/kbMutations.ts` (V8 runtime) implements TOFU (trust-on-first-use) binding: first ingest of a storageId records `{storageId, orgId}`; subsequent ingest from a different org throws `AUTH_STORAGE_NOT_OWNED`. `convex/kb.ts` `storeDocumentChunked` calls the gate AFTER `assertOrgArgs` and BEFORE `ctx.storage.get` — closes the cross-tenant storageId attack vector atomically within the mutation transaction (no TOCTOU window). `softDeleteDocument` unchanged (operates on docId, not storageId). Preserves the oauthCtx → explicit-args pattern (no `ctx.auth.getUserIdentity()` reintroduced). Eta FIRST recommended post-PR #992 follow-up. Tests: `convex/__tests__/kb-ingest.test.ts` 11/11 PASS (+2 new: TOFU first-ingest binding, cross-tenant second-ingest reject), `mcp-server/src/__tests__/kb-ingest-e2e.test.ts` 6/6 PASS (+1 new: MCP layer surfaces AUTH_STORAGE_NOT_OWNED). `npx tsc --noEmit` exit 0. Task `k17bb95jxg85dqkm19nkjb2xr589jxgk`. Base: main 22929cc.
- **B5 Knowledge Base ingest backend** — `convex/kb.ts` (Node runtime) + `convex/kbMutations.ts` (V8 runtime) implement the full KB ingest pipeline: upload binary → Convex storage → text extract → paragraph-aware chunk (~512 tokens/chunk, ~50-char overlap) → `store_memory(type=reference, namespace=team/<orgId>/<docId>)`. Re-ingest of the same `docId` supersedes prior chunks (`isLatest` flip, idempotent). `softDeleteDocument` action marks all active chunks `isLatest=false` (soft-delete; rows retained for audit). mimeType support: `application/pdf` (pdf-parse, with stub fallback), `text/markdown`, `text/plain`. Auth: requires Clerk JWT with `org_id` claim — no master-fallback into `team/*` namespace. MCP tools: `store_document_chunked` + `soft_delete_document` registered in `mcp-server/src/tools/kbIngest.ts`. Tests: `convex/__tests__/kb-ingest.test.ts` (9/9 PASS) — 8 scenarios covering PDF/MD/TXT happy paths, cross-tenant isolation (`AUTH_NAMESPACE_DENIED`), soft-delete propagation, no-org bearer rejection, chunking determinism, idempotent re-ingest. tsc CLEAN. Mission `k5779qbxhwrfjmj02t31yvehns8911jp`, task `k17bdmhr2hffhz2t96p65j70nh891wcp`, B4 dep PR #915 squash `64ca2ba`. Unblocks Kappa OKF UI PR #29 + F3 KB feature.
- **B4 RAG namespace team/<orgId> tenant enforcement** — `bearerAuthMiddleware` gains a new layer 2.5: Clerk JWTs with an `org_id` claim are verified via JWKS (`CLERK_DOMAIN/.well-known/jwks.json`, 10-min in-process cache) and resolve to `scopeProfile="team-member"` with `namespaceRead/WritePrefixes=["team/<orgId>"]`. Cross-tenant namespace access is rejected before any Convex call. New `convex/memoriesScoped.ts` provides `listMemoriesScoped` / `storeMemoryScoped` Convex functions with identity-level `AUTH_NAMESPACE_DENIED` enforcement. Tests: `mcp-server/test/team-namespace-cross-tenant.test.ts` (16 predicate tests), `convex/__tests__/auth-namespace-deny.test.ts` (9 Convex-layer deny tests). Task `k17528bya5wnbxm0x3cebrf9vh8915n0`, mission `k5779qbxhwrfjmj02t31yvehns8911jp`. PR #915.
- **test(security): clerk org_id↔clerkOrgSlug parity + no-org→master guard** — `convex/__tests__/clerk-org-id-slug-parity.test.ts` (+422 LoC, 9/9 PASS, 933ms). PR #915 (B4 RAG namespace) Eta APPROVED follow-up split — 2 non-blocking MINOR items. TEST 1 asserts namespace tenant key derived by `auth.ts` (`team/${org_id}`) resolves through `memoriesScoped.resolveOrgId` to the SAME identifier on both enforcement layers (3 sub-assertions including MISMATCH DETECTION documenting fail-closed over-denial hazard). TEST 2 asserts no-org regular team member CANNOT bypass MCP boundary to gain cross-tenant master access — `AUTH_NAMESPACE_DENIED` at MCP layer for `team/<other-org>` writes (4 sub-assertions covering Convex-direct master fallback + MCP `checkNamespaceWrite/Read` denial + master-scope context positive case). No production code touched (`mcp-server/src/auth.ts`, `convex/memoriesScoped.ts`, `convex/schema.ts` untouched). Identity mocked via `t.withIdentity({...})` — no real Clerk hit. Source: PR #915 c547dc2 Eta APPROVED comment. Task `k17539hq2p3gxq5d0h9d02sd3d891800`.
- **PR-F — `bulk_complete_tasks` MCP tool** — bulk-close cron-spam tasks with dry-run-default safety and RBAC gate (PR-F).
- **PR-J — Tool descriptions sync canonical 114-tool snapshot + 15 list_* paging qualifiers** — new fleet-quality gate `mcp-server/src/__tests__/tools-descriptions-canonical.test.ts` (4 it() blocks): Section A inventory floor ≥100 tools, Section B description length floor ≥60 chars, Section C placeholder ban (`/\b(TODO|FIXME|XXX|TBD)\b/` case-sensitive + `/\b(placeholder|coming soon)\b/i` prose-only), Section D category contracts (list_* tools MUST mention `limit` + paging qualifier `cap` / `default 20` / `default 100`; recall-class tools MUST carry verbatim VP-Sources doctrine from PR-H). T-RED `259a8d4` surfaced 15 list_* tools missing the paging qualifier (`list_episodes`, `list_memories`, `list_peers`, `list_broadcast_status`, `list_tasks`, `list_tasks_by_mission`, `list_missions`, `list_diaries`, `list_briefing_notes`, `list_recurring_tasks`, `list_mandates`, `list_repo_mappings`, `list_issues`, `list_fix_patterns`, `list_errors`) — VP-Sources doctrine 0 violations (5/5 recall-class tools already carry PR-H doctrine, zero drift). T-GREEN `41944dc` appends canonical paging-qualifier sentence (`Default limit N. cap M.`) to all 15 descriptions, mirroring PR-A/B/C/E precedent — args schemas unchanged (`list_broadcast_status` already had `limit: max(200).optional()` line 3183; only description string lacked the qualifier). Tests: 4/4 PASS canonical + 5/5 PASS PR-H regression (`tools-descriptions.test.ts`) + 6/6 PASS chatgpt-annotations regression — total 15/15 zero regression. VP mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A, 10th of 10 PRs), audit section 27. T-RED `259a8d4` · T-GREEN `41944dc`.
- **PR-I — `improvisation_digest` weekly digest query + MCP tool** — V1 Option C scanning VP tasks/messages/memories for durable-artifact fleet/state claims missing VP-Sources footer (ADVISORY-only). Convex query `improvisationDigest:scanWindow` + MCP tool `improvisation_digest` (`readOnlyHint=true`, `openWorldHint=false`, `destructiveHint=false`). Args: `windowDays` (default 7) + optional `orchestrators` array. Returns `{ countsByOrch, countsByCategory, samples[] }`. Detection heuristic: Eta A5 scope filter (excludes `system`/`cron-*`/webhook entries); flags records with a durable-artifact token (commit SHA, PR#, Convex ID, decisive verb) and no `VP-Sources:` footer. T-RED `cd6cda3`, T-GREEN `b9414dc`. Mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A). Also fixes T-GREEN friction: annotation was temporarily set `readOnlyHint: false` to pass the hardcoded `READ_ONLY_TOOLS` allowlist in `chatgpt-tool-annotations.test.ts`; this PR-I doc commit adds `"improvisation_digest"` to that allowlist and flips the annotation to the semantically correct `readOnlyHint: true`. Tests: chatgpt-tool-annotations 6/6 PASS, improvisation_digest.tool.test 2/2 PASS, improvisationDigest.test 7/7 PASS.
- **PR-H — VP-Sources doctrine embedded in 5 tool descriptions** — `recall`, `hybrid_search`, `text_search`, `list_briefing_notes`, and `search_briefing_notes_by_keyword` now carry two verbatim advisory doctrine substrings appended as separate paragraphs after each tool's existing description. Substring 1: "VP-Sources doctrine: MUST be called before any factual claim about fleet state, audits, dette tooling, mission/task/client status, incident history, doctrine references." Substring 2: "Cite returned ids in the answer footer as 'VP-Sources: recall(\"<q>\")→[ids] | none-needed:<reason>'.". Doctrine is advisory-only — no hook blocks on absence; intentional design so client implementations can adopt it gradually. Client LLMs read the doctrine at tool-list time because it is embedded inline in the description field. New exported constants in `mcp-server/src/tools.ts`: `RECALL_TOOL_DESCRIPTION`, `HYBRID_SEARCH_TOOL_DESCRIPTION`, `TEXT_SEARCH_TOOL_DESCRIPTION`, `LIST_BRIEFING_NOTES_TOOL_DESCRIPTION`, `SEARCH_BRIEFING_NOTES_BY_KEYWORD_TOOL_DESCRIPTION`. Tests: 5 new snapshot tests in `mcp-server/src/__tests__/tools-descriptions.test.ts` asserting both doctrine substrings are present in each description constant; full suite 2010/2010 zero regression. Mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A), audit sections 27+28.4, doctrine source Eta Q1 msg `k977bvf03qzas7v7g0zqca9c7n8937zh`. T-RED `0b4dc84`, T-GREEN `908fd67`.
- **PR-G — `block-delete-on-prod.py` hook pull from VR canonical + presence smoke** — vantage-memory workspace now ships the fleet-canonical `block-delete-on-prod` PreToolUse hook (RULE #30 Day 109 sha256 alignment). Hook blocks destructive MCP ops (`delete_task` / `delete_mission` / `delete_message`) before they hit Convex. New smoke `qa/smoke-block-delete-on-prod-presence.sh` verifies presence on disk AND sha256 match vs VR `contentHash` (orchestrator/CI passes `EXPECTED_SHA256` from `mcp__vantage-registry__get_hook_content`). Hook path force-added (`.claude/hooks/` is gitignored repo-wide); CI / cloned workspaces now ship with the RULE #30-pinned version. Local sha256 = VR contentHash = `c5b99cf7c76829f89cfe9eb6a1a76bcaa34c2498cf98466d572cf2abef72d4c9`. VP mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A, PR-G). Audit ref `analysis/mcp-crud-baseline-vp-audit-2026-06-14.md` section 14. T-RED commit `d9feb64`, T-GREEN `b260dc5`.

### Fixed
- fix(oauth): DCR rejects empty/missing/malformed redirect_uris (zombie-client class fix, TDD-strict RED-then-GREEN, RFC 7591 §3.2.2 invalid_redirect_uri) [PR pending]
- **Day 116 B1 — `tasks:list` Server Error on dashboard (Pi escalation req ae789fe820822c93)** — Root cause: dashboard `TaskBoard` calls `usePaginatedQuery(api.tasks.list, { status, priority, orgId }, ...)` which injects `paginationOpts` (via Convex React hook) + passes `priority` and `orgId` fields that `tasks.list` did not declare in its args validator. Convex throws `ArgumentValidationError: unexpected field "paginationOpts"` on EVERY paginated render → "Server Error". NOT a Buffer/V8 issue (Pi's initial hypothesis was wrong — `tasks.ts` has no Buffer usage). Fix (two parts): (1) `tasks.list` now accepts `priority: v.optional(priorityValidator)` and `orgId: v.optional(v.string())` — priority is applied as an in-memory filter after page-fetch; orgId is accepted-and-ignored (scoping handled by withOrgScope Clerk JWT path). (2) New `tasks.listPaginated` query that properly accepts `paginationOptsValidator` + `status` (single enum) + `assignedTo` + `priority` + `orgId`, uses `.paginate()`, and returns `{ page, isDone, continueCursor }` — the PaginationResult shape `usePaginatedQuery` expects. Dashboard must be updated to call `api.tasks.listPaginated` instead of `api.tasks.list` (one-line change in `components/tasks/task-board.tsx`). CI guard: new hook `.claude/hooks/enforce-no-buffer-in-v8-convex.py` blocks commits adding `\bBuffer\b` to V8-runtime `convex/*.ts` files (excludes `_generated/`, `__tests__/`, and files with `"use node"`). Registered in `.claude/settings.local.json` as PreToolUse Bash hook. Tests: `convex/__tests__/tasks.listPaginated.test.ts` 8/8 PASS (T1-T8: PaginationResult shape, status filter, priority filter, assignedTo filter, compound filter, orgId passthrough, tasks.list backward-compat arg acceptance, tasks.list priority end-to-end). Existing suite zero regression: 107/107 PASS. tsc: 0 errors. Branch: `fix/tasks-list-prod-crash-day116-v2`. Task `k171pmtayzthngd5syqbrgnqsd89kw2n`.
- **CRITICAL — V8-runtime `Buffer` crash — `businessUnits:list`, `githubRepoMapping:list`, `components:list` all broken (Day 116, 18-day dashboard outage)** — `encodeCursor`/`decodeCursor` (and their repo/component variants) used `Buffer.from(...).toString("base64")` / `Buffer.from(cursor, "base64").toString("utf8")`, which is a Node.js-only global absent from the Convex V8 runtime. All three query functions threw `ReferenceError: Buffer is not defined` on every call, crashing the `/dashboard` feed and the MCP `list_bus` / `list_repo_mappings` / `list_components` tools. Fix: replaced `Buffer.from` calls with the V8-safe `btoa`/`atob` builtins in `convex/businessUnits.ts` (L195, L201), `convex/githubRepoMapping.ts` (L46, L52), and `convex/components.ts` (L102, L108). All surrounding type-guard logic preserved verbatim. Files using `"use node"` (`convex/okfBundleNode.ts`, `convex/kb.ts`, `convex/gumroadWebhook.ts`) were not at risk. Blocker task `k17fxr3vy0p3qj0kgrz9zv9b0n89jrx5`.
- **CRITICAL — `list_memories` + `list_episodes` MCP tools were silently returning `items: []` on every call** — Day-114 audit (`projects/vantage-peers/mcp-pagination-audit-day114.md`) surfaced the bug: both handlers read `memories?.page` from the Convex `listMemories` return shape `{value, continueCursor, isDone}`. `page` is undefined → `items: []` was returned on EVERY call regardless of seeded data. `nextCursor` was never emitted. Functional breakage, not pagination drift. **Invalidates PR-J's "19/19 covered" claim** — the envelope-format check only verified wrapper shape, not the data extraction path (envelope-coverage tests must use seeded-data assertions going forward: insert N rows → assert `items.length === N`). Patch (`mcp-server/src/tools.ts`): `list_episodes` handler L2161-2188 + `list_memories` handler L2515-2547 now read `memories.value` (not `?.page`), read `continueCursor` + `isDone`, and emit `{items, nextCursor}` envelope via `encodeCursor({backendCursor})` — same shape the decode path at L2476 / L2129 was already expecting (the bug was encode/emit asymmetric to decode). Mirrors PR-A/B/C/E precedent (shared `mcp-server/src/paging.ts`). Test coverage: NEW `mcp-server/src/__tests__/list_memories_episodes_pagination.test.ts` (11/11 PASS, 5 list_memories + 6 list_episodes — seeded-data assertion across no-pagination / first-page-with-cursor / full-pagination-chain / empty-backend). RED-before evidence: 8 failed with `AssertionError: result must have an items array: expected false to be true` + `TypeError: Cannot read properties of undefined (reading 'length')`. GREEN-after: 11/11 PASS. Full mcp-server suite zero regression: 27 files / 380 tests PASS. Convex suite zero regression: 35 files / 328 tests PASS. tsc baseline delta = 0 vs 176. Zero `convex/` modification (backend was already correct; only MCP response assembly bug). Mission `k57bxpa2wcp7f8xdwne8g3dpfx89f27k` (vp-mcp-pagination-fix-day114-v1). Tasks T0 `k17cxmgxkfvakq3kse87c82stn89ecnn` + T1 `k170vgkh5ftj3bveea8wwc8yv189erwb`. Pi-dispatched audit task `k17a2vygmfbhwc8tkyxwft28c989egrc` + Laurent verbatim 2026-06-27 "crééé les taches pour fixer ça sans délai!!!".
- **Day-101 sweep — `briefingNotes.get` + `missions.get` full-doc returns-validators now include `orgId`** — Eta-flagged during PR #973 review (PR #973 fixed tasks-table only; identical #360 drift was latent in 2 other tables). `convex/briefingNotes.ts:51` (`get` handler) + `convex/missions.ts:159` (`get` handler) each now declare `orgId: v.optional(v.string())`. Counter-to-zero proof: `grep -n "orgId" convex/schema.ts` enumerates 3 orgId-bearing tables (missions L210, tasks L254, briefingNotes L306); all 3 now have orgId-clean full-doc returns validators (`tasks` fixed in #973, `briefingNotes` + `missions` fixed here, `list` handlers on all 3 deliberately omit returns validators so no 500 risk). `mandates` and `missionTemplates` have no orgId field — not at risk. Identical class to vantage-registry #226 and vantage-peers #973. Test coverage: `convex/__tests__/briefingNotes.fullDocReturnsValidator.test.ts` (5/5 PASS, T1-T5) + `convex/__tests__/missions.fullDocReturnsValidator.test.ts` (6/6 PASS, T1-T6 including updateStatus path). RED-before evidence: 5 failures with `Return value validation failed for query "briefingNotes:get"/"missions:get": Validator error: Expected one of object, null, got {..., "orgId":"<client-org>",...}`. GREEN-after: 11/11 PASS. Full convex/__tests__ suite zero regression (35 files / 328 tests PASS). tsc baseline delta = 0 (176 errors pre + post). Task `k17a8k7me042ddp3ks76h3y2fd89dswf` (Eta-dispatched ROOT-FIX Day-101).
- **fix(tasks): full-doc returns-validators now include `orgId` — fleet 500 blocker resolved** — `convex/tasks.ts` `taskFullValidator` (line 126) + `get` inline returns (line 215) + `getById` inline returns (line 255) each now declare `orgId: v.optional(v.string())`. PR #360 commit `44f0a93` (`feat(scope): client_org_mapping table + withOrgScope helper`) added `orgId: v.optional(v.string())` to the tasks-table schema but the three full-doc returns-validators were not updated — Convex returns-validator threw `Return value validation failed for query "tasks:getById": Validator error: Expected one of object, null, got {...,"orgId":"<client-org>",...}` so every `get_task` / `complete_task` / `update_task` against a multi-tenant doc returned 500. Impact: `complete_task` and `update_task` (which share `taskFullValidator`) AND `get` / `getById` (which had inline returns) blocked evidence-bound closure fleet-wide (Pi, Omega, Xi, Theta, Sigma). Identical class to vantage-registry #226. Repro evidence cited by Omega: `get_task k17ejy6w / k172g0kf / k176rerqn → 500`. Patch is `v.optional(v.string())` (forward + backward compatible — pre-#360 docs without the field still pass). New regression `convex/__tests__/tasks.fullDocReturnsValidator.test.ts` (9/9 PASS, 926ms) covers tasks with-and-without `orgId` on all 3 surfaces + list_tasks projection unchanged. Existing tasks-suite zero regression: `tasks.list_tasks_exclude_cron.test.ts` (13/13), `tasksMutationConvexErrors.test.ts` (11/11), `tasks.bulk_complete.test.ts` (11/11). Task `k173wtkytmc4f8me1rkv977v0589dnrj` (Pi-dispatched URGENT).
- **Day 108 — VP mutations no longer mask errors as generic "Server Error"** — `convex/tasks.ts` (14 throws) + `convex/lib/auth.ts` (2 throws) now emit `ConvexError("<CODE>: <message> :: <details-json>")` with string payload so the Convex cloud privacy guard no longer anonymizes to `errorMessage="Server Error"`. New codes: `TASK_NOT_FOUND`, `RBAC_DENIED`, `TASK_START_BLOCKED` (the Hephaistos `start_task` symptom — caller already has an in_progress task), `DEPENDENCY_NOT_DONE` (newly enforced — `start` previously allowed starting tasks whose `dependsOn` chain was unsatisfied), `COMPLETION_NOTE_REQUIRED`. Repro test in `convex/__tests__/tasksMutationConvexErrors.test.ts` (17/17 PASS) — asserts the original Hephaistos scenario surfaces `TASK_START_BLOCKED:` not "Server Error". Existing suite 541/541 PASS. Mission `k578wphazwhxamggbxnn2wr5r98911vr` (Sigma rattrapage Day 108). Follow-up queued: mcp-server `parseConvexError` / `mcpConvexError` cleanup so callers can move off string-prefix parsing.

### Added
- **PR-E — `list_tasks` `excludeAutoGenerated` filter (vantage-peers-mcp@2.13.0 target)** — new optional `excludeAutoGenerated: boolean` arg on the `list_tasks` Convex query and MCP tool. When `true`, filters out tasks where `createdBy` matches `/^cron-/i` (dash mandatory — `cron-bot` is filtered, `cronus` is not) **or** `title` matches `/^\/?check-messages$/i` (whole-string match, optional leading slash, case-insensitive). Default `false` — omitting the arg or passing `false` returns all tasks unchanged, preserving full backward-compatibility with all existing callers. Filter applied in-memory in the `list` query handler after existing filters (`createdBy`, `updatedSince`, `createdBefore`) and before `filterByOrgScope` and envelope assembly. **Trade-off**: post-filter pages may be smaller than `limit` because filtered rows do not count toward limit — acceptable given the cron-spam catalog is small and narrowly targeted. MCP tool exports `LIST_TASKS_TOOL_DESCRIPTION` (description string mentions `excludeAutoGenerated` with usage hint) and `listTasksArgsSchema` (zod). Use case: "Pi cron-spam queue cleaning" — closes audit §13 (152 cron-spawned tasks polluting Pi's queue). Tests: 7 new in `convex/__tests__/tasks.list_tasks_excludeAutoGenerated.test.ts` + 2 new in `mcp-server/src/__tests__/list_tasks.tool.test.ts` (9/9 GREEN); full suite 2014/2056 zero regression. VP mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A), audit section 13, RED `eb78cfa`, GREEN `74dea44`.

### Changed
- **PR-C — `list_repo_mappings` envelope safety hardening (vantage-peers-mcp@2.13.0 target)** — extends the PR-A/PR-B paging helper reuse pattern (shared `mcp-server/src/paging.ts`) to `list_repo_mappings`. Convex `githubRepoMapping.list` now returns `{ items, nextCursor }` envelope (was flat array); default `limit` lowered to 20 (was 50), capped at 200 (was unbounded). `fields='lite'` projects to `{_id, _creationTime, repo, orchestrator, project}` (5 keys; excludes `active`, `lastDeployedSHA`, `lastDeployedAt`). Default `fields='full'` returns all schema fields. Opaque base64 `cursor` encodes `{time, id}` to survive same-millisecond inserts; hybrid cursor decode in MCP handler preserves S3.3 B8 old-format `{createdBefore}` cursors by decoding and forwarding as `createdBefore` (back-compat for S3.3 B8 batch 2 callers). `createdBefore` arg kept on the Convex query for the same reason. MCP tool exports `LIST_REPO_MAPPINGS_TOOL_DESCRIPTION` and `listRepoMappingsArgsSchema` (zod: `limit` 1–200, optional `cursor` string, optional `fields` enum `'lite'|'full'`, plus existing filters). Tests: 7 new in `convex/__tests__/githubRepoMapping.list_repo_mappings.test.ts` + 2 new in `mcp-server/src/__tests__/list_repo_mappings.tool.test.ts` (9/9 GREEN, full suite 2005/2047 zero regression). Eta non-blocking follow-up `k17fbpf4` + fix-pattern `m97c0z53`: over-fetch + walk-by-id strategy can dead-end deep paging past ~90 rows; catalogues are small, fix is queued. VP mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A), audit section 9, RED `eed6ae5`, GREEN `4ddca2b`.
- **PR-B — `list_components` envelope safety hardening (vantage-peers-mcp@2.13.0 target)** — extends the PR-A paging helper reuse pattern (shared `mcp-server/src/paging.ts`) to `list_components`. Convex `components.list` now returns `{ items, nextCursor }` envelope (was flat array); default `limit` lowered to 20 (was 100), capped at 200 (was unbounded). `fields='lite'` projects to `{_id, _creationTime, name, type, team}` (was no-op — returned full row regardless). Opaque base64 `cursor` encodes `{creationTime, id}` to survive same-millisecond inserts; hybrid cursor decode in MCP handler preserves S3.3 B8 old-format `{createdBefore}` cursors by decoding and forwarding as `createdBefore` (back-compat for S3.3 B8 callers). `createdBefore` arg kept on the Convex query for the same reason. MCP tool exports `LIST_COMPONENTS_TOOL_DESCRIPTION` and `listComponentsArgsSchema` (zod: `limit` 1–200, optional `cursor` string, optional `fields` enum `'lite'|'full'`, plus existing `type`/`team` filters). Tests: 7 new in `convex/__tests__/components.list_components.test.ts` + 2 new in `mcp-server/src/__tests__/list_components.tool.test.ts` (9/9 GREEN, full suite 1996/2038 zero regression). VP mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A), audit section 9, RED `cb8b8fa`, GREEN `39f8d08`.
- **PR-A — `list_bus` envelope safety hardening (vantage-peers-mcp@2.13.0 target)** — extends the S3.3 B8 follow-up batch 1 cursor rollout (which gave `list_bus` opaque cursor support) with strict defaults and actual `fields=lite` projection. Convex `businessUnits.list` now returns `{ items, nextCursor }` envelope (was flat array); default `limit` lowered to 20 (was 50), capped at 200 (was unbounded). `fields='lite'` projects to `{_id, _creationTime, name, status, orchestratorId}` (was no-op since v2.4.12 — accepted the arg without applying projection). Opaque `cursor` now encodes `{creationTime, id}` to survive same-millisecond inserts. MCP-server `list_bus` tool exports `LIST_BUS_TOOL_DESCRIPTION` and `listBusArgsSchema` for snapshot testing. New shared helper `mcp-server/src/paging.ts` (`applyPagingDefaults`, `pagingArgsSchema`, `DEFAULT_PAGING`) reusable for PR-B `list_components` and PR-C `list_repo_mappings`. Tests: 7 new in `convex/__tests__/businessUnits.list_bus.test.ts` + 2 new in `mcp-server/src/__tests__/list_bus.tool.test.ts` (9/9 GREEN, full suite 1987/2029 zero regression). VP mission `k571gcctka8mq5jbkgpj0a0b2n892ctg` (VP-MCP top level Bloc A). Audit ref `analysis/mcp-crud-baseline-vp-audit-2026-06-14.md` section 9.
- **Day 108 OKF Phase 2 B3 — `export_okf_bundle` generalized (drop `project/elpi-corp` hard lock)** — `assertCanExportNamespace` in `convex/okfBundleNode.ts` no longer rejects non-Phase-1 namespaces; any non-empty prefix is accepted as long as identity-attached callers match the namespace tail (cross-tenant deny preserved). Path-traversal segments (`..`) are rejected for defence-in-depth. Multi-tenant `team/<orgId>/*` and other `org/<slug>` namespaces can now export their own bundles, unblocking VP Cloud Dashboard tenant Settings UI (F8). MCP tool `export_okf_bundle` description + Zod arg schema updated. Mission `k5779qbxhwrfjmj02t31yvehns8911jp`, task `k17f3407sg7cn6gswn5qs9j5b5891581`. Tests: `convex/__tests__/okfBundleExportGeneralize.test.ts` 8/8 PASS (including `project/elpi-corp` regression case).

### Added
- **Day 108 OKF Phase 2 B2 — `import_okf_bundle` MCP tool + Convex action (mutation)** — New action `okfBundleNode:importOkfBundle` accepts `{bundleUrl|storageId, targetNamespace, mode:"dry-run"|"merge"|"replace", idempotencyKey?}` and returns `{ imported:{memories,briefings,tasks}, skipped, conflicts[] }`. Pipeline: `assertCanImport` (delegates to fail-closed `assertCanExportNamespace` from #888 iter-2, same null-identity / no-org → AUTH_NO_IDENTITY / AUTH_NO_ORG guard applied to writes) → source resolution (storageId XOR bundleUrl, SSRF-gated reused) → `unpackTarball` → `validateBundle` (must pass before any write) → per-entry `parseEntry` → content-equality dedup via 3 internal queries on V8 side (`_findMemoryByContent` / `_findBriefingByTitleAndContent` / `_findTaskByTitleAndDescription`) → insert via 3 internal mutations (`_insertImportedMemory` / `_insertImportedBriefing` / `_insertImportedTask`). `dry-run` short-circuits before any write; `merge` inserts new + skips dedup-hits; `replace` reserved for a follow-up PR. Wired as MCP tool `import_okf_bundle` (`readOnlyHint=false`) in `mcp-server/src/tools/importOkfBundle.ts`. Closes OKF Phase 2 spec end-to-end (export ↔ validate ↔ import). Mission `k5779qbxhwrfjmj02t31yvehns8911jp`, task `k17fja9v7pgnf25yvzkwrj5ch5891bb3`. Tests: `convex/__tests__/okfBundleImport.test.ts` 4/4 PASS (dry-run / merge dedup / idempotency / cross-tenant deny).
- **Day 108 OKF Phase 2 B1 — `validate_okf_bundle` MCP tool + Convex action (read-only)** — New action `okfBundleNode:validateOkfBundle` accepts `{bundleUrl|storageId}`, fetches and untar's the bundle, runs the pure `validateBundle()` validator from `convex/okfValidator.ts` (RFC §3.5), and returns `{ valid, schemaVersion, errors?, stats: {memoryCount, briefingCount, taskCount} }`. Read-only — never mutates the DB. Wired as MCP tool `validate_okf_bundle` (`readOnlyHint=true`) in `mcp-server/src/tools/validateOkfBundle.ts`. Unblocks import preview UX in the VP Cloud Dashboard (mission `k5779qbxhwrfjmj02t31yvehns8911jp`, task `k1796g7g7y03gn9rd6z7psenk98910vt`). Tests: `convex/__tests__/okfBundleValidate.test.ts` 5/5 PASS.
- **Day 98 F4 — webhook `issue_comment` Bridge-only path** — `convex/http.ts` webhook handler for `issue_comment` events now routes exclusively through the Bridge tenant path. Prevents double-processing on multi-tenant deployments. PR #796, commit `f0c133c`.
- **Day 98 F3 — stub-aware guard on webhook cron hook** — `convex/hooks.ts` cron now includes a stub-presence guard so stubs registered during tests do not fire production webhook paths. PR #796, commit `f0c133c`.
- **DOCS-CONTEXT-LOOP (RULE #25)** — Docs-lockstep discipline now live on this repo. Any PR touching tools, API, or install paths must update `README.md` + `CHANGELOG.md` in the same PR. Exemptions require explicit `docs-skip: <reason>` in PR body.
- **Tool count reaches 116** — `validate_okf_bundle` (B1) and `import_okf_bundle` (B2) added in Day 108 OKF Phase 2 push, lifting the surface from 114 to 116.
- **Tool count reaches 114** — 17 tools added since v2.5.0 including episodes API (`get_episode`, `list_episodes`, `search_episodes_by_keyword`, `search_episodes_by_semantic`), single-entity getters (`get_task`, `get_message`, `get_fix_pattern`, `get_mandate`, `get_recurring_task`, `get_repo_mapping`, `get_briefing_note`), search tools (`search_tasks_by_keyword`, `search_messages_by_keyword`, `search_briefing_notes_by_keyword`), template tool (`instantiate_template_into_mission`), identity tool (`whoami`), and validator tool (`validate_task_payload`). Aliases: `recall`, `text_search`, `update_summary`, `create_diary`, `create_task_dependency`, `create_fix_attempt`, `check_fix`, `check_mandate_spending`, `register_deployment`, `delete_deployment`, `register_repo_mapping`, `delete_repo_mapping`, `search_components`, `search_fix_patterns`.

### Changed
- **README HTTP/Railway transport** — Quick Start now documents both Option A (stdio) and Option B (HTTP/SSE via Railway) transports. HTTP transport is required for Claude.ai, ChatGPT, and any non-local MCP client.
- **README tool count** — All occurrences updated from 97 to 114.

## [v2.5.0] — 2026-06-06 — Day 92 VP MCP quality overhaul (mission k57a36y8w5t085bqr23dsmvb2d882506)

Day 92 delivered a comprehensive quality overhaul of the VP MCP server across fourteen deliverables (C0–C4, A1–A4, B1–B3, F1–F2). C0 closed all 14 P0 zero-auth write tools identified in the A1 audit matrix (commit `d03d2d7`) by wiring master-only gates (`guardMasterOnly`) or identity ownership checks (`checkFromAllowed`) into every affected handler. C1 shipped 87 Zod `outputSchema` exports following the per-family envelope standard (`whoamiOutputSchema` at `tools.ts:576` commit `5231811` as the canonical precedent). C2 enforced Unicode NFC normalization at all write paths and case-insensitive orchestrator-ID matching at all filter comparisons, closing the NFD/NFC mismatch class that caused the Hélios/helios production regression (PR #654, commit `00b95f0`). C3 standardized 97 tool descriptions (1-line summary + WHEN clause + concrete EXAMPLE, 80–500 chars each) and aligned 10 tool names to the `verb_noun_snake` whitelist. C4 removed all `claude-peers` legacy references from source and added a grep-gate CI check. A3 shipped the `whoami` LECTURE tool (PR #661, commit `5231811`) for automatic skill identity resolution. F1 shipped the `validate_task_payload` MCP validator tool (commit `cf6c961`). B1–B3 delivered the tools-quality-standard doc, scope-filter framework documentation, and onboarding-customer guide respectively.

Authorized under `PI_AUTHORIZED_TASK_ID=k1751nfs27t9f9mpvg3ppd6xad884r59`, mission `k57a36y8w5t085bqr23dsmvb2d882506`. Release branch: PR #678. For the full per-PR list see `mcp-server/CHANGELOG.md`.

### Security
- **S2.3 D8 — VP MCP master-gate constant-time hardening via `@vantageos/cloud-identity@0.1.0`** — `mcp-server/src/auth.ts` `masterOnlyMiddleware` now consumes `validateMasterBearer` from the shared npm brick `@vantageos/cloud-identity@0.1.0` (replaces the prior non-constant-time `token !== masterToken` direct string compare). Brick sha256-hashes both the presented Bearer token and the configured `BEARER_SECRET_MASTER`, then constant-time-compares the digests via the brick's `timingSafeEqual` — closing both the byte-oracle and length-oracle leaks on every `/admin/*` route (including the S2.2 D5 `PATCH /admin/scope-profiles/:id` emergency endpoint and all OAuth admin routes). Local `mcp-server/src/crypto.ts` and `mcp-server/src/scope-filter.ts` deleted; 7 importers rewired to the brick (3 source + 4 test files). Algorithm unchanged (XOR-accumulate + dummy HMAC on length mismatch). Tests: 254/254 PASS (zero regression vs S3.3 follow-up batch 1 baseline). Test report: `docs/test-reports/s2.3-d8-vp-mcp-migration-cloud-identity-0.1.0-2026-06-04.md`. RED `fbe73e0` · GREEN `ce9f7ba`.

### Added
- **S2.2 D5 — `PATCH /admin/scope-profiles/:id` master-gated HTTP wrapper** — new admin endpoint in `mcp-server/server-http.ts` exposing the existing `oauth:patchScopeProfileEmergency` Convex mutation (S1.2-mutation + S2.1) over REST. Mounted under the already-master-gated `/admin/*` Hono sub-app (`masterOnlyMiddleware` → 401 missing / 403 wrong bearer); Convex layer re-asserts master via constant-time `timingSafeEqual` in `requireMasterAuth`. Body schema: `cascadeRevokeTokens: boolean` and `reason: string` required; `rename` / `fromAllowList` / `namespaceReadPrefixes` / `namespaceWritePrefixes` optional `string[]`. Response on 200: `{ patchedProfileId, cascadeRevokedCount, clientsRetargeted, auditLogId }`. Convex throws mapped to HTTP: `profile not found` → 404, `D4 violation` → 400, `reason must be at least 40 characters` → 400, others → 500. Token re-issue stays on the standard `/authorize` + `/token` path — emergency surface is revoke-and-audit only. No new master-token utility introduced; the underlying Convex mutation is unchanged. Tests: 13 new (13/13 PASS), full mcp-server suite 205→218 PASS, zero regression. Test report: `docs/test-reports/s2.2-d5-admin-scope-profiles-patch-2026-06-04.md`. RED `f86fe75` · GREEN `ca2d2dd`.
- **S3.3 B8 — `list_*` cursor paging + envelope cap protection** — new shared `mcp-server/src/paging.ts` utility (`DEFAULT_LIMIT=50`, `MAX_LIMIT=200`, `ENVELOPE_TARGET_BYTES=50_000`, `clampLimit`, `encodeCursor`/`decodeCursor`, `enforceEnvelopeCap`, `buildPageResult`). MCP `list_tasks`, `list_memories`, `list_briefing_notes` now accept an opaque `cursor` argument and emit `nextCursor` on full pages so callers can drain past the 200-row newest-first page-cap. Convex `tasks.list()` + `briefingNotes.list()` accept optional `createdBefore` for forward pagination; `memories.listMemories` forwards `paginationOpts.cursor` end-to-end. Backward-compatible: callers without `cursor` / `limit` keep the legacy `?? 20` envelope-safe default. Friction origin: Sigma stale-task cleanup 2026-06-04 required 6 identical 132-task batches because no cursor existed. Tests: 28 new (28/28 PASS), full mcp-server suite 177→205 PASS, zero regression. Test report: `docs/test-reports/s3.3-b8-list-tools-cursor-paging-2026-06-04.md`.
- **S3.3 B8 follow-up batch 1 — cursor paging rollout to 6 more `list_*` tools** — additive rollout of the shared `mcp-server/src/paging.ts` utility (PR #635) to `list_missions`, `list_diaries`, `list_components`, `list_recurring_tasks`, `list_mandates`, `list_bus`. Each MCP tool gains an opaque `cursor` arg + emits `nextCursor` on full pages; each Convex query (`missions:list`, `diary:list`, `components:list`, `recurringTasks:list`, `mandates:list`, `businessUnits:list`) gains a `createdBefore: v.optional(v.number())` arg + post-take filter mirroring the briefingNotes pattern. Backward-compatible: pre-batch callers (no `cursor` / no `limit`) keep the legacy `?? 20` envelope-safe default. Tests: 36 new (36/36 PASS), full mcp-server suite 218→254 PASS, zero regression. Test report: `docs/test-reports/s3.3-followup-batch-1-cursor-paging-2026-06-04.md`.

### Changed
- **S3.3 B8 follow-up batch 3 FINAL — cursor paging rollout complete (19 / 19 coverage)** — closes the S3.3 B8 rollout by migrating `list_peers` to cursor paging (MCP wrapper + `convex/profiles.ts: listProfiles` gains `limit` + `createdBefore` args, refactored from unbounded `.collect()` to `.order("desc").take(limit)` + post-take filter) and pinning explicit `@cursorPagingException` JSDoc markers on the three remaining tools whose shape is incompatible with `createdBefore` cursors: `list_broadcast_status` (single-object shape), `search_components` (relevance-ranked semantic search), `search_fix_patterns` (semantic-action ranker). Cumulative coverage post-merge: **16 migrated + 3 documented exceptions = 19 / 19 covered** — every list/search tool in the MCP surface has either cursor paging or a documented exception. Backward-compatible: pre-batch `list_peers` callers keep the legacy `?? 20` envelope-safe default; `listProfiles` server-side default `take=50` replaces the unbounded `.collect()` (doctrine-aligned ceiling matching the `githubRepoMapping:list` fix from batch 2). Tests: 12 new (12/12 PASS, RED→GREEN delta of 3 on list_peers cursor-decode + full-page nextCursor + invalid cursor); full mcp-server suite 290→302 PASS, zero regression. Test report: `docs/test-reports/s3.3-followup-batch-3-final-cursor-paging-2026-06-04.md`.
- **S3.3 B8 follow-up batch 2 — cursor paging rollout to 6 more `list_*` tools** — additive rollout of the shared `mcp-server/src/paging.ts` utility (PRs #635 + #637) to `list_messages`, `list_tasks_by_mission`, `list_repo_mappings`, `list_issues`, `list_fix_patterns`, `list_errors`. Each MCP tool gains an opaque `cursor` arg + emits `nextCursor` on full pages; eight Convex queries gain a `createdBefore: v.optional(v.number())` arg + post-take filter (`messages:listMessages`, `tasks:listByMission`, `githubRepoMapping:list`, `issues:listByProject` + `issues:listByOrchestrator` + `issues:listByStatus`, `fixPatterns:listAll` + `fixPatterns:listByProject`, `errorMonitor:listErrors`). `githubRepoMapping:list` additionally gains a `limit: v.optional(v.number())` arg (was previously unbounded `.collect()`), with a server-side default of 50 preserving prior behavior. Backward-compatible: pre-batch callers (no `cursor` / no `limit`) keep the legacy `?? 20` envelope-safe default. Cumulative coverage post-merge: **15 / 21** known `list_*` tools. Tests: 36 new (36/36 PASS), full mcp-server suite 254→290 PASS, zero regression. Test report: `docs/test-reports/s3.3-followup-batch-2-cursor-paging-2026-06-04.md`.

### Fixed
- **D90 kill-switch hardening** — `AUTO_IRP_PAUSED` env-var guard now applied at all three auto-IRP pipeline entries (`pollAllDeployments` cron, `createGitHubIssue` action, `http.ts` webhook `issues.opened` for `[Auto]`-prefixed titles), not only the cron entry shipped in PR #609. Adds `convex/errorMonitorKillSwitch.ts` (`KILL_SWITCH_VARS`, `isKillSwitchActive()`, `assertKillSwitchHealth()` startup warning), a transient retry-class filter rule (`Server Error\nRequest ID:` envelope, severity `skip`) in `DEFAULT_FILTER_RULES`, an `isTransientErrorMessage()` pure classifier, and wildcard `functionName: "*"` support in `evaluateFilter()`. Closes issue #632 false-positive root cause. Tests: 10 new (10/10 PASS) in `convex/error-monitor-kill-switch-harden.test.ts`; errorMonitor suite 92→102; full suite 1358→1368 PASS, zero regression. Test report: `docs/test-reports/d90-kill-switch-harden-2026-06-04.md`.
- `seedDefaultProfiles` (`convex/oauth.ts`) now UPSERTS (patch-on-diff) instead of skip-on-exists, with `oauth_audit_log` entries (eventType `seed_upsert`) per actual update. Operator-created profiles outside the catalog are preserved (no destructive sync). Return shape changed to `{ inserted, updated, skipped }` for caller visibility. Obsoletes the bespoke catalog-drift migration pattern shown in `convex/migrations/patch_marie_iris_rh_scope.ts` for future catalog edits (S3.4 B4). Test report: `docs/test-reports/s3.4-b4-seed-default-profiles-upsert-2026-06-03.md`.

## [2.4.14] — 2026-06-03

### Security — D6 + D7 + patchScopeProfileEmergency + oauth_audit_log + S3.1 Waves A+B

**Scope:** v2.4.14 closes the S2.4b security cascade: OAuth 2.1 hardening, master-gated emergency tenant maintenance, append-only audit ledger, and the first two waves of the scope-aware filter framework.

**Changes:**
- **D6 — confidential `client_secret` at `/token`** — `mcp-server/server-http.ts` L382-585. Constant-time `crypto.timingSafeEqual` comparison. PKCE-only path preserved for public clients. PR #621, commit `5fd6354`.
- **D7 — `redirect_uri` exact-match at `/authorize`** — `mcp-server/server-http.ts` L298-376. Byte-identical match against the client's registered URIs; no prefix / host-only / scheme-normalized acceptance. PR #621, commit `5fd6354`.
- **`patchScopeProfileEmergency`** — `convex/oauth.ts`. Master-token-gated mutation enforcing D4 (no `*` in `cloud-*` profiles), D9 cascade rename, cascade-update on `oauth_clients`, cascade-revoke on `oauth_tokens`, and append-only audit write. PR #622, commit `9a1b8cf`.
- **D9 cascade-update `oauth_clients`** — full enforcement parity. PR #623, commit `2f5c974`.
- **S3.1 — scope-aware filter framework (D3) Waves A+B** — `mcp-server/src/scope-filter.ts` applied to `list_memories`, `get_memory`, `list_briefing_notes`, `list_messages`, `list_peers`. Wave A merged at main `251d183` (PR #624). Wave B in PR #625.
- **`oauth_audit_log`** — append-only emergency-action ledger in `convex/schema.ts`. No update / delete path.

**Doctrine reminder:** Cloud (multi-tenant) and Self-host are distinct products. Runbooks: `docs/cloud/` (Cloud) and `docs/getting-started/` (Self-host) — never mixed.

**References:** PRs #621, #622, #623, #624, #625. Test reports: `docs/test-reports/s1.5-oauth-d6-d7-2026-06-03.md`, `docs/test-reports/s1.2-mutation-2026-06-03.md`, `docs/test-reports/s2.1-d9-cascade-clients-2026-06-03.md`, `docs/test-reports/s3.1.a-scope-aware-filter-wave-a-2026-06-03.md`, `docs/test-reports/s3.1.b-scope-aware-filter-wave-b-2026-06-03.md`.

---

## [2.3.3] — 2026-05-28

### Feature — `createdBy` + `updatedSince` filters + auto-clamp on list queries

**Root cause / Motivation :** 2026-05-27 Pi runtime overflow on `list_tasks limit=50` (~79k chars / 838 lines). Pi pull-cycle HUMAN MODE could not filter to its own dispatched tasks. Need `createdBy` (Pi-dispatched pattern) + `updatedSince` (recent-window pattern) on list queries + auto-clamp safeguard against accidental `fields=full` mega-scans.

**Fix :**
- `convex/tasks.ts` `list` + `listByMission` : new args `createdBy: v.optional(creatorValidator)` + `updatedSince: v.optional(v.number())`. Auto-clamp `effectiveLimit = 30` when `fields="full"` AND no explicit `limit` (with `console.warn`).
- `convex/missions.ts` `list` : new arg `updatedSince` + auto-clamp 30.
- `convex/briefingNotes.ts` `list` : new arg `updatedSince` + auto-clamp 15 (default 20 for this collection).
- `mcp-server/src/tools.ts` : 4 list tools forward new params. New export `updatedSinceSchema` (positive integer ms timestamp). Removed `.default(50)`/`.default(20)` on `limit` so absent value reaches backend (required for auto-clamp).

**Backward compatibility :** all new args optional. Existing callers (no new args) get unchanged behavior. `taskStatusSchema` + `missionStatusSchema` still exported and used by create/update/start operations.

**Tests :**
- `mcp-server/src/__tests__/list-queries-v2.3.3-createdby-updatedsince.test.ts` (NEW) : 15 cases (positive int ms accepted, negative/zero/float/string/null rejected, edge cases NaN/Infinity rejected).
- `convex/tests.test.ts` : 6 new round-trip cases (createdBy filter accuracy, combinatorics, updatedSince window, auto-clamp triggered/bypassed branches).
- Suites : 175/175 MCP + 73/73 Convex full PASS, 0 regression.

**References :**
- PR : https://github.com/vantageos-agency/vantage-peers/pull/539
- Merge commit : `567c6a59` (squash from `6ec70ff96ab53c2b20204489ac96ba09f7fb5923`)
- npm : `vantage-peers-mcp@2.3.3` shasum `26517e5fcf876f5f9c68722e5c66ec963925b489`
- Pi pull-cycle quickstart : `list_tasks createdBy="pi" status="review" fields="lite" limit=30`

---

## [2.3.2] — 2026-05-28

### Hotfix — MCP wrapper exposes `fields=lite` + `status` aliases/arrays

**Root cause :** 2026-05-26 sprint `vp-list-queries-fields-lite-status-multi-v1` (v2.3.1) shipped Convex backend support for `fields="lite"` projection + status aliases (`open`/`active`/`all`) + multi-status arrays on the 4 list queries. The MCP wrapper Zod schemas in `mcp-server/src/tools.ts` were NOT updated, so MCP clients (Claude Code, claude.ai web, orchestrators) could not pass those params — the MCP validator rejected them before reaching backend. 2026-05-27 Pi runtime overflow on `list_tasks limit=50` made the gap painful.

**Fix :**
- `mcp-server/src/tools.ts` : new exports `taskStatusFilterSchema`, `missionStatusFilterSchema`, `fieldsSchema`. Wired into 4 list tools (`list_tasks`, `list_tasks_by_mission`, `list_missions`, `list_briefing_notes`). Aliases NOT permitted inside arrays (matches backend rejection contract).

**Backward compatibility :** zero behavioral change for existing callers. The single-value `taskStatusSchema` + `missionStatusSchema` are still used by all non-filter operations.

**Tests :**
- `mcp-server/src/__tests__/list-queries-schema-v2.3.2.test.ts` (NEW) : 17 cases (single/alias/array variants accepted, array-with-alias rejected, empty array rejected, invalid enum rejected).
- Suite : 160/160 full PASS, 0 regression.

**References :**
- PR : https://github.com/vantageos-agency/vantage-peers/pull/537
- Merge commit : `9772091` (squash from `7373e54b0cfd9858153492d7c88e49a6feda4960`)
- npm : `vantage-peers-mcp@2.3.2` shasum `7fcfa38b2cc3f478bd65607c3e06f420de096c11`

**Fix-pattern fleet-wide :** When backend query supports a new param, ALWAYS update the MCP wrapper tool schema in the SAME PR.

---

## [2.3.1] — 2026-05-26

### Feature — `fields="lite"` projection + status array/aliases on list queries (backend)

**Root cause / Motivation :** MCP list queries (tasks, missions, briefingNotes) returned full Convex documents by default. Large workspaces hit token-budget ceiling on `list_tasks limit=50` (78k+ char payloads). Need compact projection + multi-status filtering.

**Fix :**
- `convex/tasks.ts` `list` + `listByMission` : new args `fields: v.optional(v.union(v.literal("lite"), v.literal("full")))` + `status: v.optional(v.union(v.string(), v.array(v.string())))`. New helper `expandTaskStatuses()` handles aliases: `"open"` → `["todo","in_progress","review","blocked"]`, `"active"` → `["todo","in_progress"]`, `"all"` → `undefined`. Aliases NOT permitted inside arrays.
- `convex/missions.ts` `list` : same shape. `"open"` → `["brainstorm","plan","execute","validate"]`, `"active"` → `["plan","execute"]`, `"all"` → `undefined`.
- `convex/briefingNotes.ts` `list` : `fields` only (no status param).
- `fields="lite"` projects compact fields : tasks `{_id,_creationTime,title,status,priority,assignedTo,missionId}`, missions `{_id,_creationTime,name,status,pilot,priority,project}`, briefingNotes `{_id,_creationTime,topic,title,participants,createdBy}` — typical 5-10x smaller payload.

**Backward compatibility :** `fields` defaults to `"full"`. `status` accepts single string (existing behavior) OR array OR alias. Old callers untouched.

**Tests :** Convex round-trip tests pin contract (alias expansion, array variant, lite projection field set). Full suite green.

**References :**
- PR : https://github.com/vantageos-agency/vantage-peers/pull/530
- Sprint : `vp-list-queries-fields-lite-status-multi-v1`
- 2026-05-26 Pi flag : "On a fait un sprint pour fixer!" (gap closed in v2.3.2 hotfix above).

---

## [2.3.0] — 2026-05-21

### Bug Fix — recall()/hybrid_search() returning [] for self-host with direct OpenAI key

**Root cause (self-host incident, 2026-05-18):** When a self-host operator placed
a direct OpenAI key (`sk-*`) into the `AI_GATEWAY_API_KEY` environment variable, the
embedding provider was routed to `https://ai-gateway.vercel.sh/v1` (the Vercel gateway
base URL). The gateway rejected the direct key with 401 → embeddings silently returned
empty vectors → `recall()` and `hybrid_search()` returned `[]` even after reindexing.
`text_search` (BM25) was unaffected because it does not call the embedding endpoint.

**Fix (`convex/lib/aiClient.ts`):**
- New `isDirectOpenAIKey(key: string): boolean` — detects `sk-*` prefix.
- New `resolveEmbeddingPath()` — explicit routing function with three outcomes:
  `openai-direct` (OPENAI_API_KEY or sk-* in AI_GATEWAY_API_KEY),
  `gateway` (non-sk-* AI_GATEWAY_API_KEY),
  `missing` (neither set).
- `getAITextEmbeddingProvider()` and `getEmbeddingModelName()` now delegate to
  `resolveEmbeddingPath()` — sk-* key in AI_GATEWAY_API_KEY routes to `api.openai.com/v1`
  with the bare model name `text-embedding-3-small` (not the `openai/` prefixed name).

**Backward compatibility:** existing Vercel AI Gateway keys (non-sk-* tokens) and
`OPENAI_API_KEY` behavior are fully preserved.

**Tests:** 19 unit tests in `convex/lib/aiClient.test.ts` covering all routing branches
including the sk-*-in-AI_GATEWAY_API_KEY regression case (direct path).

**Remediation for affected self-host clients:** see
`docs/self-host/recall-empty-troubleshooting.md` — set `OPENAI_API_KEY`, redeploy,
run reindex mutation.

**References:** self-host support ticket 2026-05-18 , 2026-05-20 reindex PR #483,
Sigma task k17defa52nzyp7z03198ne9ay186ygss.

## [2.2.0] — 2026-05-07

### Added
- 4 fix-pattern MCP tools wrapping existing Convex backend functions:
  - `create_fix_pattern` — create a new validated fix pattern in the KB
  - `add_fix_attempt` — log a fix attempt against an existing pattern
  - `validate_fix` — promote a candidate fix to validated status
  - `link_issue_to_pattern` — link a GitHub issue to a fix pattern
- Schema exports for testing: `creatorSchema`, `severitySchema`, `flexArray` are now exported from `tools.ts`
- 41 new Zod input-validation unit tests (`mcp-server/src/__tests__/fix-pattern-tools-validation.test.ts`)

### Why
Enables the agent improvement cycle: orchestrators (Pi, Sigma, Eta, Chi, Iota, Psi, Victor, Phi) can now capitalize learnings via MCP rather than shelling out to `npx convex run fixPatterns:*`. Powers the `/capitalize-fix` skill and the `inject-fix-patterns` hook.

## [v2.1.1] — 2026-05-04

### Bug Fixes
- Defense-in-depth memoryIdSchema for briefingNotes linkedMemoryIds (closes #386, #387)
- Adds Zod regex validation at MCP boundary so wrong-table IDs get a clear error before reaching Convex validator
- Applies to both create_briefing_note and update_briefing_note tools
- 5 regression tests added in briefing-note-memory-id-validation.test.ts

### Refs
- Closes #386 (canonical), #387 (duplicate)
- Pattern reuse: PR #328 mark_as_read fix (m97ewrrqczew67kc6at3a59e7985ea7h)

## [v2.1.0] — 2026-04-25

### Added
- update_briefing_note MCP tool — partial update for briefing notes with RBAC (createdBy or system only)
- briefingNotes.updatedAt + briefingNotes.updatedBy schema columns (both v.optional, backward compatible)

### Refs
- Closes issue #333
- Mission k5708d9xxwj81v92e0x3hwv36985g4d7

## v11 — 2026-04-08

### New Features
- **Dynamic broadcast** — broadcast channel now queries profiles table instead of hardcoded list (#219)
- **7 new MCP tools** — block_task, add_task_dependency, get_mission, update_component, delete_component, search_components, update_recurring_task (#121-#127)
- **PR review webhook** — pull_request_review events auto-notify pilot via VantagePeers (#191)
- **82 MCP tools total** (up from 75)

### Bug Fixes
- Inline orchestrator enums replaced with z.string() (#149)
- McpServer version synced to 1.0.1 (#150)
- completionNote made required in complete_task (#153)
- update_profile fields made optional for partial updates (#154)
- Deduplicated missionPrioritySchema and componentTypeSchema (#172, #173)
- Schema comment corrected: errorLogs → issueStats (#217)

### Documentation
- README tool counts updated to 82, table count to 20
- Orchestrator roles documented including system RBAC bypass (#174)
- CONTRIBUTING.md org URL corrected (#160)
- Settings path corrected to ~/.claude.json (#175)
- Added convex/_generated/ to .gitignore (#179)

### Testing
- MCP smoke tests expanded from 75 to 82 tools
- Broadcast unit test updated for dynamic profiles
- Test reports committed to tests/

## v10 — Public Launch Cleanup — 2026-04-07

- PR #100: Deep repo cleanup for public launch
- Removed internal orchestrator instructions and person names from CLAUDE.md
- Fixed plugin license mismatch (MIT → FSL-1.1-Apache-2.0)
- Replaced `mcp__vantage-memory__*` with `mcp__vantage-peers__*` across all plugin files
- Fixed wildcard permissions in plugin/templates/settings.json
- Updated CONTRIBUTING.md and README.md with current tool and test counts
- Removed absolute internal server paths from docs

## v9 — README Rewrite + 3 New Tools — 2026-04-07

- PR #96: README rewritten for public consumption
- Added `get_memory` tool (fetch single memory by ID)
- Added `text_search` tool (BM25 full-text search)
- Added `hybrid_search` tool (RRF fusion of vector + BM25)
- Total tool count: 75

## v8 — MCP Smoke Tests Expanded to 75 — 2026-04-07

- PR #95: Expanded MCP smoke test suite from 29 to 75 tests (all 75 tools covered, 75/75 pass)
- PR #94: string-based API calls for all 75 tools
- PR #90: enforce-signature hook (portable across machines)
- PR #89: RAG integration tests (6/6 pass)
- PR #87: standalone MCP server, schema validators, soft_delete_memory

## v7 — Open Source Release — 2026-03-25

- README rewritten for public consumption (27 MCP tools documented)
- MCP integration tests: 29/29 covering all tools
- Convex unit tests: 34/34 with vitest + convex-test
- Added LICENSE (MIT), CONTRIBUTING.md, CHANGELOG.md
- Added .github templates (bug report, feature request, PR template)
- Added .env.example
- Package.json metadata (keywords, repository, engines, scripts)

## v6 — Schema Migration + Hardening — 2026-03-25

- Removed deprecated `to` field from messages table
- Made `channel` field required on messages
- Fixed `listMessages` return validator (missing `_creationTime`, `fromInstanceId`)
- Added `review` status to task lifecycle
- Task dependencies (`dependsOn`) with priority sorting
- Mandatory `completionNote` on task completion
- Global string-to-array tolerance for all MCP array fields (tags, participants, highlights, etc.)
- Cleaned up test data residue from MCP integration tests

## v5 — Multi-Instance Support — 2026-03-24

- Added `instanceId` to profiles, messages, and task claiming
- `set_summary` supports instance-level registration
- `check_messages` routes to role-level and instance-level recipients
- `fromInstanceId` on messages for sender identification
- `recipientInstanceId` on message receipts for instance routing

## v4 — Messaging with Receipts — 2026-03-23

- Replaced claude-peers with native messaging
- `send_message`, `check_messages`, `mark_as_read`, `list_messages` tools
- Channel-based routing: broadcast, role DM, instance DM, multi-target
- Per-recipient read receipts via `messageReceipts` table

## v3 — Tasks, Missions, Diary — 2026-03-22

- Task management: create, update, start, complete with `completionNote`
- Task dependencies (`dependsOn`) and priority sorting
- Review status in task lifecycle
- Missions: project grouping with lifecycle (brainstorm → complete)
- Daily diary entries with highlights and blockers
- Briefing notes for structured topic discussions
- RAG threshold fix for semantic search

## v2 — Profiles and Episodes — 2026-03-21

- Orchestrator profiles with static identity and dynamic session state
- Episodic memory (context → goal → action → outcome → insight)
- Severity levels for episodes (critical, major, minor)
- Memory graph relations (updates, extends, derives)

## v1 — Initial Release — 2026-03-20

- Core memory storage with 5 types (user, feedback, project, reference, episode)
- Semantic vector search via `@convex-dev/rag` and OpenAI embeddings
- Scoped namespaces (global, orchestrator/*, project/*)
- MCP server for Claude Code integration
- 8 database tables on Convex
